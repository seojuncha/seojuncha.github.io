<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://seojuncha.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://seojuncha.github.io/" rel="alternate" type="text/html" hreflang="ko" /><updated>2025-09-15T13:00:07+00:00</updated><id>https://seojuncha.github.io/feed.xml</id><title type="html">Hacking the System, One Bit at a Time</title><subtitle>Cha Seojun&apos;s journey into the heart of computers—from transistor to terminal. Writing compilers, emulating hardware, and uncovering how machines truly work. A blog for hackers, builders, and curious minds.</subtitle><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><entry xml:lang="ko"><title type="html">ARM 어셈블리 #3 - 부호를 유지하는 시프트인 산술시프트 (ASR)</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/15/arm-arithmetic-shift.html" rel="alternate" type="text/html" title="ARM 어셈블리 #3 - 부호를 유지하는 시프트인 산술시프트 (ASR)" /><published>2025-09-15T12:53:00+00:00</published><updated>2025-09-15T12:53:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/15/arm-arithmetic-shift</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/15/arm-arithmetic-shift.html"><![CDATA[<p>CPU의 중요한 모듈중 하나인 ALU는 산술논리장치입니다. 이름에서 유추할 수 있듯이 논리연산 뿐 아니라 산술연산도 가능해야 하죠.
산술연산에서 유의해야 할 것은 피연산자의 부호 유무입니다. <a href="/ko/arm/assembly/tutorial/2025/09/09/arm-logical-shift.html">앞선 포스팅</a>에서는 논리 시프트를 사용한 비트 단위의 연산을 고려했다면,
이번 포스팅에서는 부호를 고려한 산술 시프트 연산자인 <code class="language-plaintext highlighter-rouge">asr</code>의 특징과 사용법을 살펴보겠습니다.</p>

<h2 id="산술-시프트">산술 시프트</h2>
<p>ARM에서는 산술 시프트를 위한 하나의 명령어를 제공합니다.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">명령어</th>
      <th style="text-align: left">설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">asr</code></td>
      <td style="text-align: left">오른쪽 산술 시프트</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">asr</code>은 아래와 같이 사용할 수 있습니다:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">asr #&lt;imm&gt;</code> : 즉시값만큼 시프트</li>
  <li><code class="language-plaintext highlighter-rouge">asr &lt;Rs&gt;</code> : 레지스터값만큼 시프트</li>
</ul>

<h3 id="산술-시프트의-부호-유지-방법">산술 시프트의 부호 유지 방법</h3>
<p>산술 시프트에서 기존 값의 부호를 유지하기 위해,</p>

<p><strong><em>“시프트 이전 데이터의 최상위 비트값으로 시프트 공간을 채운다.”</em></strong></p>

<p>라는 규칙을 가지고 있습니다.</p>

<p>예를 들어, 최상위 비트가 0이고 3비트 만큼 시프트 한다면, 최상위 3비트는 <code class="language-plaintext highlighter-rouge">000</code>이 되고,
최상위 비트가 1이면, 최상위 3비트는 <code class="language-plaintext highlighter-rouge">111</code>이 됩니다.</p>

<p><strong>최상위 비트가 0인 경우</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before (32-bit):  
0 1 0 1 1 1 0 0 ...  

ASR #3 →  

After (32-bit):  
0 0 0 0 1 0 1 1 ...
^ ^ ^  
| | |  
+---- 시프트로 새로 채워진 상위 비트 (부호 유지: 0)
</code></pre></div></div>

<p><strong>최상위 비트가 1인 경우</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before (32-bit):  
1 0 1 0 0 0 1 1 ...

ASR #3 →  

After (32-bit):  
1 1 1 1 0 1 0 ...
^ ^ ^  
| | |  
+---- 시프트로 새로 채워진 상위 비트 (부호 유지: 1)
</code></pre></div></div>

<h3 id="산술-시프트-예제">산술 시프트 예제</h3>

<p><strong>asr.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#-</span><span class="mi">32</span>         <span class="no">@</span> <span class="o">-</span><span class="mi">32</span> <span class="o">=</span> <span class="mh">0xffffffe0</span>
</span><span class="hll">  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">asr</span> <span class="o">#</span><span class="mi">1</span>   <span class="no">@</span> <span class="nv">R1</span> <span class="o">=</span> <span class="mh">0xfffffff0</span> <span class="o">=</span> <span class="o">-</span><span class="mi">16</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p><code class="language-plaintext highlighter-rouge">-32</code>는 16진수로 <code class="language-plaintext highlighter-rouge">0xffffffe0</code> 입니다. 여기서 부호를 유지한채 오른쪽 1비트를 시프트하면:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0xffffffe0 &gt;&gt; 1 = 0xfffffff0 (-16)
</code></pre></div></div>
<p>1비트 시프트 한다는 것은 <code class="language-plaintext highlighter-rouge">숫자/2</code>동작이므로 <code class="language-plaintext highlighter-rouge">-32/2 = -16</code> 이라는 부호를 유지한 산술연산이 가능해집니다.</p>

<h2 id="왜-오른쪽-시프트만-있을까">왜 오른쪽 시프트만 있을까?</h2>
<p>결과부터 얘기하면, 왼쪽 시프트는 최상위 비트가 바뀌더라도 그것이 의도한 산술 연산일 수 있기 때문입니다.</p>

<p><strong>left-shift-for-negative.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x40000000</span>
</span><span class="hll">  <span class="nb">movs</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">1</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p><code class="language-plaintext highlighter-rouge">0x40000000 &lt;&lt; 1</code>은 <code class="language-plaintext highlighter-rouge">0x80000000</code>이므로 테이블로 작성하면 아래와 같습니다:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">2진수</th>
      <th style="text-align: center">16진수</th>
      <th style="text-align: center">10진수</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0b0100…</td>
      <td style="text-align: center"><strong>0x4000</strong>0000</td>
      <td style="text-align: center">1073741824</td>
    </tr>
    <tr>
      <td style="text-align: center">0b1000…</td>
      <td style="text-align: center"><strong>0x8000</strong>0000</td>
      <td style="text-align: center">2147483648 혹은 -2147483648</td>
    </tr>
  </tbody>
</table>

<p>보다시피 <code class="language-plaintext highlighter-rouge">0x80000000</code>이라는 데이터는 정수의 표현법에 따라 양수가 될 수도 있고 음수가 될 수도 있습니다.
그래서 ARM 프로세서는 <code class="language-plaintext highlighter-rouge">0x80000000</code>과 같이 최상위 비트가 1일 경우 부호 있는 정수 표현의 음수라는 것을 나타내기 위해 CPSR의 음수(Negative) 플래그를 설정합니다.</p>

<p>프로그래머는 CPSR의 플래그를 확인하여 <code class="language-plaintext highlighter-rouge">R1</code>의 데이터를 부호 있는 정수로 활용할 것인지 아니면 부호 없는 정수로 활용할 것인지 선택할 수 있죠.</p>

<blockquote>
  <p>나중에 살펴볼 산술 연산(예: <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code>, 등)에서는 부호 있는 정수의 오버플로우를 나타내기 위해 <strong>오버플로우(V) 플래그</strong>라는 것도 설정합니다.
지금은 논리연산(<code class="language-plaintext highlighter-rouge">lsl</code>)만 사용하였으므로 오버플로우 플래그를 설정하지 않습니다.
산술연산에서의 플래그 설정 및 확인은 향후 산술 연산을 다루는 포스팅에서 자세히 살펴볼 예정입니다.</p>
</blockquote>

<figure style="text-align: center;">
  <img src="/assets/img/non-add-sub-v-left-unchanged.png" alt="ARM 매뉴얼에 명시된 오버플로우 플래그의 설정방법 두가지" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">덧셈이나 뺄셈연산이 아닌 경우 보통 오버플로우 플래그를 변경하지 않는다고 ARM매뉴얼에 명시되어있다.</figcaption>
</figure>

<p>결과적으로 왼쪽 시프트는 오히려 부호를 유지한다는 개념을 가지면 안되고 논리 시프트와 같은 역할을 하기 때문에 <code class="language-plaintext highlighter-rouge">lsl</code>명령어 하나로만 유지되는 것 입니다.</p>

<h3 id="gdb로-플래그-확인하기">GDB로 플래그 확인하기</h3>
<p><strong>left-shift-for-negative.s</strong> 파일을 컴파일하고 실행한 후 GDB로 음수 플래그를 확인해보겠습니다.</p>

<h4 id="1-컴파일">1. 컴파일</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
    <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
    <span class="nt">-nostdlib</span> <span class="se">\</span>
    left-shift-for-negative.s <span class="se">\</span>
    <span class="nt">-o</span> left-shift-for-negative.elf
</code></pre></div></div>
<h4 id="2-qemu-실행">2. QEMU 실행</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
    <span class="nt">-machine</span> versatilepb <span class="se">\</span>
    <span class="nt">-nographic</span> <span class="se">\</span>
    <span class="nt">-S</span> <span class="se">\</span>
    <span class="nt">-s</span> <span class="se">\</span>
    <span class="nt">-kernel</span> left-shift-for-negative.elf
</code></pre></div></div>
<h4 id="3-gdb-연결-후-플래그-확인">3. GDB 연결 후 플래그 확인</h4>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ gdb-multiarch left-shift-for-negative.elf
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-gdb" data-lang="gdb">(gdb) target remote :1234
0x00010000 in _start ()    
(gdb) si                   # mov r0, #0x40000000 
0x00010004 in _start ()    
(gdb) p ($cpsr &gt;&gt; 31) &amp; 1
$2 = 0
<span class="hll">(gdb) si                   # movs r1, r0, lsl #1
</span>0x00010008 in _start ()
<span class="hll">(gdb) p ($cpsr &gt;&gt; 31) &amp; 1
</span><span class="hll">$2 = 1
</span></code></pre></figure>

<h2 id="마무리">마무리</h2>
<p>이번 글에서는 산술시프트 연산자인 <code class="language-plaintext highlighter-rouge">asr</code>을 활용하여 부호를 유지하고 시프트하는 방법을 알아봤습니다.</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">asr</code>의 사용</li>
  <li>산술 시프트에서 왼쪽 시프트가 없는 이유</li>
  <li>음수 플래그를 확인하는 방법</li>
</ul>

<p>다음 포스팅에서는 시프트 연산의 마지막 챕터로, 비트를 순환시키는 <code class="language-plaintext highlighter-rouge">ror</code>과 <code class="language-plaintext highlighter-rouge">rrx</code> 명령어를 다룰 예정입니다.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="Assembly" /><category term="ASR" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[CPU의 중요한 모듈중 하나인 ALU는 산술논리장치입니다. 이름에서 유추할 수 있듯이 논리연산 뿐 아니라 산술연산도 가능해야 하죠. 산술연산에서 유의해야 할 것은 피연산자의 부호 유무입니다. 앞선 포스팅에서는 논리 시프트를 사용한 비트 단위의 연산을 고려했다면, 이번 포스팅에서는 부호를 고려한 산술 시프트 연산자인 asr의 특징과 사용법을 살펴보겠습니다.]]></summary></entry><entry xml:lang="ko"><title type="html">ARM 어셈블리 #2 - 논리 시프트 이해하기(LSL, LSR)</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/09/arm-logical-shift.html" rel="alternate" type="text/html" title="ARM 어셈블리 #2 - 논리 시프트 이해하기(LSL, LSR)" /><published>2025-09-09T15:13:00+00:00</published><updated>2025-09-09T15:13:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/09/arm-logical-shift</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/09/arm-logical-shift.html"><![CDATA[<p>컴퓨터는 비트 단위로 데이터를 조작하는 연산을 자주 수행합니다. 그 중에서도 <strong>시프트(Shift) 연산</strong>은 매우 중요하죠. 이번 글에서는 ARM 어셈블리에서 사용하는 논리 시프트(Logical Shift) 를 중심으로 설명합니다.</p>

<p>추상적인 개념 설명보다는 실습 예제를 통해 쉽게 접근하고, 시프트 연산이 실제로 어떤 효과를 가지는지, 그리고 그 과정에서 캐리 플래그(Carry Flag) 가 어떻게 작동하는지도 함께 살펴보겠습니다.</p>

<blockquote>
  <p>실습 코드는 <a href="https://github.com/seojuncha/arm-assembly-tutorial/tree/main/02_moving-data-around">GitHub 저장소</a>에서 확인하실 수 있습니다.</p>
</blockquote>

<h2 id="시프트-피연산자란">시프트 피연산자란?</h2>
<p>ARM 어셈블리에서는 <code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code> 같은 명령어를 사용할 때, 피연산자(operand) 중 하나에 시프트 연산을 미리 적용한 값을 넣을 수 있습니다. 이런 피연산자를 시프트 피연산자(shift operand) 라고 부릅니다.</p>

<p>이 기능 덕분에 별도의 시프트 명령 없이도 다양한 연산을 더 간결하게 표현할 수 있습니다.</p>

<p>예:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span>    <span class="no">@</span> <span class="nv">R0</span><span class="err">를</span> <span class="err">왼쪽으로</span> <span class="mi">2</span><span class="err">비트</span> <span class="err">시프트</span> <span class="err">→</span> <span class="nv">R1</span><span class="err">에</span> <span class="err">저장</span>
</code></pre></div></div>
<p>이렇게 명령어 한 줄로 시프트 연산과 대입을 동시에 처리할 수 있습니다.</p>

<h2 id="논리-시프트란">논리 시프트란?</h2>
<p><strong>논리 시프트(Logical Shift)</strong> 는 단순히 비트를 왼쪽이나 오른쪽으로 이동시키는 연산입니다. 이때 빈자리는 항상 0으로 채워집니다.</p>

<p>예를 들어 4비트 값 <code class="language-plaintext highlighter-rouge">0110</code>을 기준으로:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>왼쪽 시프트: 0110 &lt;&lt; 1 = 1100
오른쪽 시프트: 0110 &gt;&gt; 1 = 0011
</code></pre></div></div>
<p>ARM에서는 다음 두 가지 논리 시프트 연산자를 제공합니다:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">명령어</th>
      <th style="text-align: left">설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">lsl</code></td>
      <td style="text-align: left">왼쪽 논리 시프트</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">lsr</code></td>
      <td style="text-align: left">오른쪽 논리 시프트</td>
    </tr>
  </tbody>
</table>

<p>시프트 양은 즉시값(#3)이나 레지스터 값(R2)으로 지정할 수 있습니다.</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">lsl #n</code>, <code class="language-plaintext highlighter-rouge">lsr #n</code> : 즉시값 (Immediate)</li>
  <li><code class="language-plaintext highlighter-rouge">lsl rN</code>, <code class="language-plaintext highlighter-rouge">lsr rN</code> : 레지스터 값 (Register)</li>
</ul>

<h3 id="예제-코드-논리-시프트-실습">예제 코드: 논리 시프트 실습</h3>
<p><strong>logical-shift.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="nv">r0</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #3</code> : r0에 숫자 3을 저장합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">mov r1, r0, lsl r0</code> : r0를 r0(=3)만큼 왼쪽 시프트한 값을 r1에 저장합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">mov r2, r1, lsr #1</code> : r1을 오른쪽으로 한 칸 밀어 r2에 저장합니다.</li>
</ul>

<p>예시 흐름:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>r0 = 0x3 = 0b0...0011

# r1 = r0 &lt;&lt; 3
r1 = 0x18 = 0b0...011000

# r1 = 0x18 &gt;&gt; 1
r1 = 0xC = 0b0...001100
</code></pre></div></div>

<h3 id="gdb-디버깅으로-확인하기">GDB 디버깅으로 확인하기</h3>
<p>QEMU와 GDB를 통해 동작을 직접 확인해봅니다.</p>

<h4 id="1-qemu-실행">1. QEMU 실행</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
    <span class="nt">-machine</span> versatilepb <span class="se">\</span>
    <span class="nt">-nographic</span> <span class="se">\</span>
    <span class="nt">-S</span> <span class="se">\</span>
    <span class="nt">-s</span> <span class="se">\</span>
    <span class="nt">-kernel</span> logical-shift.elf
</code></pre></div></div>
<h4 id="2-gdb-연결">2. GDB 연결</h4>
<pre><code class="language-gdb">$ gdb-multiarch logical-shift.elf
(gdb) target remote localhost:1234
</code></pre>
<h4 id="3-메모리-확인">3. 메모리 확인</h4>
<pre><code class="language-gdb">(gdb) x/10i 0x10000
</code></pre>
<h4 id="4-레지스터-확인">4. 레지스터 확인</h4>
<pre><code class="language-gdb">(gdb) info registers
(gdb) i r r0 r1 r2
</code></pre>
<h4 id="5-명령어-한-줄-실행">5. 명령어 한 줄 실행</h4>
<pre><code class="language-gdb">(gdb) stepi
</code></pre>
<p>실행 후 다시 <code class="language-plaintext highlighter-rouge">info registers</code>를 확인하면 레지스터 값이 변한 걸 확인할 수 있습니다.</p>

<h2 id="시프트-연산에서-캐리-플래그는-언제-설정될까">시프트 연산에서 캐리 플래그는 언제 설정될까?</h2>
<p>시프트 연산을 하면 비트들이 밀려나면서 비트가 하나 버려지게 됩니다. 이때 밀려나간 비트는 CPSR 레지스터의 캐리 플래그(C) 에 저장될 수 있습니다.</p>

<p>다만, 일반 <code class="language-plaintext highlighter-rouge">mov</code> 명령어는 캐리 플래그를 업데이트하지 않습니다. 대신 <code class="language-plaintext highlighter-rouge">movs</code> 명령어를 사용하면 캐리 플래그도 업데이트됩니다.</p>

<h3 id="예제-코드-캐리-플래그-확인">예제 코드: 캐리 플래그 확인</h3>

<p><strong>logical-right-shift-carry.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="mh">0x1</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
  <span class="nb">movs</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov</code>는 캐리 플래그를 갱신하지 않음</li>
  <li><code class="language-plaintext highlighter-rouge">movs</code>는 결과뿐 아니라 CPSR의 캐리 플래그도 업데이트</li>
</ul>

<p>GDB에서 캐리 플래그를 확인하려면:</p>
<pre><code class="language-gdb">(gdb) target remote localhost:1234
(gdb) info registers cpsr
(gdb) p ($cpsr &gt;&gt; 29) &amp; 1
</code></pre>
<p>CPSR의 하위 비트를 보면 C, Z, N 등의 상태를 확인할 수 있습니다.</p>

<h2 id="논리-시프트의-한계와-활용">논리 시프트의 한계와 활용</h2>
<p>논리 시프트는 항상 0으로 채워지기 때문에 부호 없는 정수 처리에 적합합니다. 하지만 부호 있는 정수(예: 음수)는 부호 비트가 손상될 수 있어 주의가 필요합니다.</p>

<p>예:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">mov r1, r0, lsr #1</code> 에서 오른쪽 시프트 결과로 부호 비트가 손실됩니다.<br />
이런 이유로 부호 있는 정수에는 ASR (Arithmetic Shift Right)를 사용합니다.</p>

<p>그럼에도 불구하고 논리 시프트는 다음과 같은 상황에서 매우 유용합니다:</p>
<ul>
  <li>배열 인덱스 계산</li>
  <li>메모리 주소 계산</li>
  <li>비트 마스킹</li>
</ul>

<h2 id="마무리">마무리</h2>
<p>이번 글에서는 ARM 어셈블리에서 사용되는 논리 시프트 연산에 대해 알아보았습니다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">lsl</code>, <code class="language-plaintext highlighter-rouge">lsr</code>을 통해 비트를 이동</li>
  <li>시프트 피연산자를 사용해 명령어 안에서 시프트 적용</li>
  <li>movs를 통해 캐리 플래그 확인</li>
  <li>부호 있는 정수 처리 시에는 주의 필요</li>
</ul>

<p>다음 글에서는 <code class="language-plaintext highlighter-rouge">asr</code>, <code class="language-plaintext highlighter-rouge">ror</code> 등 다른 시프트 유형에 대해 더 살펴보겠습니다.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="Assembly" /><category term="Embedded" /><category term="low-level programming" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[컴퓨터는 비트 단위로 데이터를 조작하는 연산을 자주 수행합니다. 그 중에서도 시프트(Shift) 연산은 매우 중요하죠. 이번 글에서는 ARM 어셈블리에서 사용하는 논리 시프트(Logical Shift) 를 중심으로 설명합니다.]]></summary></entry><entry xml:lang="ko"><title type="html">ARM 어셈블리 #1 - mov 명령어로 레지스터에 값 저장하기</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html" rel="alternate" type="text/html" title="ARM 어셈블리 #1 - mov 명령어로 레지스터에 값 저장하기" /><published>2025-09-08T13:24:00+00:00</published><updated>2025-09-08T13:24:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/08/arm-mov-instruction</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html"><![CDATA[<p>컴퓨터는 연산을 하기 위한 기계입니다.</p>

<p>이런 연산은 결국 CPU가 수행하죠.
CPU가 연산을 하기 위해서는 두 가지가 필요합니다:
바로 피연산자(operand) 와 연산자(operator) 입니다.
그리고 이 피연산자들은 대부분 레지스터(register) 에 저장된 값을 사용합니다.</p>

<p>어셈블리 언어는 CPU의 레지스터를 직접 제어할 수 있는 가장 저수준의 언어 중 하나입니다.
오늘은 이 레지스터에 값을 저장하는 기본적인 방법들을 소개합니다.</p>

<blockquote>
  <p>실습에 사용한 코드는 <a href="https://github.com/seojuncha/arm-assembly-tutorial/tree/main/02_moving-data-around">GitHub 저장소</a>에서 확인할 수 있습니다!</p>
</blockquote>

<h2 id="mov명령어로-레지스터에-값-복사하기">mov명령어로 레지스터에 값 복사하기</h2>

<h3 id="즉시값immediate-value-저장">즉시값(Immediate Value) 저장</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">mov r0, #3</code>: 숫자 3을 레지스터 R0에 저장합니다.</p>

<p><code class="language-plaintext highlighter-rouge">mov</code> 문법은 단순합니다:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mov 목적지 레지스터, 값
</code></pre></div></div>
<p>여기서 <code class="language-plaintext highlighter-rouge">#3</code>처럼 숫자 앞에 # 기호가 붙은 값은 <strong>즉시값(immediate value)</strong> 이라고 부릅니다.
즉시값은 <code class="language-plaintext highlighter-rouge">#3</code>처럼 10진수 또는 <code class="language-plaintext highlighter-rouge">#0x3</code>처럼 16진수로도 표현할 수 있습니다.</p>

<blockquote>
  <p>컴파일과 QEMU, GDB설치는 <a href="hello-arm-assembly">이전 포스팅</a>를 참고하세요.</p>
</blockquote>

<h3 id="컴파일">컴파일</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="nt">-nostdlib</span> <span class="nt">-Ttext</span><span class="o">=</span>0x10000 imm-to-reg.s <span class="nt">-o</span> imm-to-reg.elf
</code></pre></div></div>

<h3 id="qemu-실행-및-gdb-디버깅">QEMU 실행 및 GDB 디버깅</h3>

<h4 id="qemu-실행">QEMU 실행</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>qemu-system-arm <span class="se">\</span>
  <span class="nt">-machine</span> versatilepb <span class="se">\</span>
  <span class="nt">-nographic</span> <span class="se">\</span>
  <span class="nt">-S</span> <span class="nt">-s</span> <span class="se">\</span>
  <span class="nt">-kernel</span> imm-to-reg.elf
</code></pre></div></div>
<p>qemu의 공식문서를 살펴보면 굉장히 많은 옵션들을 제공하고 있는 것을 볼 수 있습니다.
우선은 꼭 필요한 옵션들만 사용해보겠습니다.</p>

<p>간단히 각 옵션을 설명하자면:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">옵션</th>
      <th style="text-align: left">설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-machine versatilepb</code></td>
      <td style="text-align: left">QEMU에서 ARM 보드 중 하나인 VersatilePB 사용</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-nographic</code></td>
      <td style="text-align: left">GUI 창 없이 콘솔에서 실행</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-S</code></td>
      <td style="text-align: left">CPU 실행을 멈추고 대기 (GDB 연결용)</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-s</code></td>
      <td style="text-align: left">포트 1234번에서 GDB 서버 실행</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-kernel</code></td>
      <td style="text-align: left">실행할 ELF 파일 지정</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>처음 실행 시 QEMU가 잠시 멈춘 것처럼 보일 수 있습니다. 하지만 이는 정상이며, GDB 연결을 기다리는 중입니다.</p>
</blockquote>

<h4 id="gdb-연결">GDB 연결</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gdb-multiarch imm-to-reg.elf
</code></pre></div></div>
<p><img src="" alt="GDB실행 이후 화면" /></p>

<p>GDB에 들어간 후 다음 명령으로 QEMU의 GDB 서버에 연결합니다.</p>
<pre><code class="language-gdb">(gdb) target retmote localhost:1234
</code></pre>

<p><img src="" alt="GDB 서버 접속 이후 화면" /></p>

<p>QEMU와 연결되었다면, 프로그램이 메모리의 코드 영역에 제대로 로드됐는지 확인해 봅니다.
특정 메모리 주소의 값을 확인하는 명령어는 <code class="language-plaintext highlighter-rouge">x/{숫자}{타입}</code>의 포맷을 사용합니다.</p>

<pre><code class="language-gdb">(gdb) x/10i 0x10000
</code></pre>

<p><img src="" alt="시작주소부터 명령어 10개 출력" /></p>

<p><code class="language-plaintext highlighter-rouge">info registers</code> 명령으로 현재 레지스터 상태를 확인할 수 있습니다.</p>

<pre><code class="language-gdb">(gdb) info registers   # 또는 간단히 i r
(gdb) i r pc r0        # PC(프로그램 카운터)와 R0 값만 출력
</code></pre>

<p><img src="" alt="PC와 R0 레지스터 출력 결과" /></p>

<p>현재 PC는 <code class="language-plaintext highlighter-rouge">0x10000</code>을 가리키고 있으므로 다음에 실행할 명령어는 <code class="language-plaintext highlighter-rouge">mov r0, #3</code>이라는 것을 알 수 있습니다.
PC가 가리키는 명령어를 실행하기 위해선 다음 명령어 실행을 의미하는 <code class="language-plaintext highlighter-rouge">stepi</code>를 사용합니다.</p>

<h4 id="stepi로-한-줄씩-실행">stepi로 한 줄씩 실행</h4>
<pre><code class="language-gdb">(gdb) stepi   # 혹은 간단히 s i
</code></pre>
<p>다시 레지스터를 확인해 보면 다음과 같은 변화가 있습니다:</p>
<pre><code class="language-gdb">(gdb) i r pc r0
</code></pre>
<ul>
  <li>PC: <code class="language-plaintext highlighter-rouge">0x10000</code> -&gt; <code class="language-plaintext highlighter-rouge">0x10004</code></li>
  <li>R0: <code class="language-plaintext highlighter-rouge">0</code> -&gt; <code class="language-plaintext highlighter-rouge">3</code></li>
</ul>

<p><code class="language-plaintext highlighter-rouge">step</code>를 한 번 더 실행하면 <code class="language-plaintext highlighter-rouge">b .</code> 명령어로 돌아가며 무한 루프 상태가 됩니다.<br />
<code class="language-plaintext highlighter-rouge">b .</code>은 현재 PC 위치로 계속 이동하므로 PC값은 변하지 않고, 따라서 프로그램은 무한루프 상태로 유지됩니다.</p>

<h3 id="레지스터-간-값-복사">레지스터 간 값 복사</h3>
<p>이전 예제에서는 <code class="language-plaintext highlighter-rouge">#3</code> 으로 표현하는 즉시값을 레지스터 <code class="language-plaintext highlighter-rouge">R0</code> 에 할당했습니다.
이제 레지스터 내부의 값을 다른 레지스터로 복사하는 방법을 알아보겠습니다.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">mov r1, r0</code>: <code class="language-plaintext highlighter-rouge">R0</code>의 값을 <code class="language-plaintext highlighter-rouge">R1</code>에 복사합니다.</p>

<blockquote>
  <p>디버깅 과정은 동일하므로 생략합니다.</p>
</blockquote>

<h2 id="mov-vs-mvn-은-어떤-차이점이-있을까">mov vs mvn 은 어떤 차이점이 있을까?</h2>
<p><code class="language-plaintext highlighter-rouge">mov</code>와 유사한 <code class="language-plaintext highlighter-rouge">mvn</code> 명령어를 하나 더 살펴보겠습니다.
둘다 동일하게 레지스터에 값을 복사하는 역할지만, <code class="language-plaintext highlighter-rouge">mvn</code>은 입력값의 <a href="https://ko.wikipedia.org/wiki/1%EC%9D%98_%EB%B3%B4%EC%88%98">1의 보수</a>를 저장하는 명령입니다.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">mvn</th>
      <th style="text-align: left">mov</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">mvn r0, #0</td>
      <td style="text-align: left">mov r0, #-1</td>
    </tr>
    <tr>
      <td style="text-align: left">mvn r0, #-1</td>
      <td style="text-align: left">mov r0, #0</td>
    </tr>
    <tr>
      <td style="text-align: left">mvn r0, #0xF</td>
      <td style="text-align: left">mov r0, #-16</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>팁! 저는 이런 보수계산과 16진수 변환들은 파이썬 REPL을 활용합니다.</p>
  <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="o">~</span><span class="mh">0xf</span>
<span class="o">-</span><span class="mi">16</span>
</code></pre></div>  </div>
</blockquote>

<h2 id="다음-포스팅-예고-shift-operand">다음 포스팅 예고: Shift Operand</h2>
<p><code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">mvn</code>과 같은 명령어는 ARM 아키텍처에서 <strong>데이터 처리 명령어(Data Processing Instructions)</strong> 에 속합니다.
이들 명령어는 피연산자에 쉬프트를 적용할 수 있는 특징이 있습니다.</p>

<p>이 기능은 <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code> 같은 연산에도 동일하게 사용되며, 다음 포스팅에서 자세히 다룰 예정입니다.</p>

<p><img src="" alt="ARM 레퍼런스 매뉴얼의 쉬프트 피연산자들" /></p>

<h2 id="마무리">마무리</h2>
<p>이번 포스팅에서는 <code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">mvn</code> 명령어를 중심으로 레지스터에 값을 저장하고 복사하는 기본적인 방법을 알아보았습니다.
또한 QEMU와 GDB를 활용한 실습을 통해 우리가 작성한 코드가 실제로 어떻게 동작하는지 확인해보았습니다.</p>

<p>처음엔 복잡하게 느껴질 수 있지만, 앞으로 계속해서 이 구조를 반복적으로 사용하게 될 것입니다.
이번 포스팅이 앞으로 나올 더 복잡한 예제의 기반이 되길 바랍니다.</p>

<p>다음 포스팅에서는 <strong>Shift Operand</strong>에 대해 구체적으로 다룰 예정입니다.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="assembly" /><category term="embedded" /><category term="low-level" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[컴퓨터는 연산을 하기 위한 기계입니다.]]></summary></entry><entry xml:lang="ko"><title type="html">ARM 어셈블리 #0 - 개발환경과 첫 코드</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/03/hello-arm-assembly.html" rel="alternate" type="text/html" title="ARM 어셈블리 #0 - 개발환경과 첫 코드" /><published>2025-09-03T14:24:00+00:00</published><updated>2025-09-03T14:24:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/03/hello-arm-assembly</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/03/hello-arm-assembly.html"><![CDATA[<p>이번 포스팅에서는 ARM 기반의 어셈블리 코드를 작성하고 실행하는 방법을 소개합니다. 향후 이어질 시리즈의 기본이 되는 내용이므로, 차근차근 따라오시길 추천드립니다. 간단한 어셈블리 코드를 작성하고, 컴파일, 실행, 디버깅하는 기본적인 흐름을 익히는 것이 목표입니다. 또한 이후 포스팅에서 사용할 QEMU 가상환경과 GDB 디버깅 도구도 함께 준비합니다.</p>

<h2 id="왜-어셈블리를-배우는가">왜 어셈블리를 배우는가?</h2>
<p>제가 어셈블리를 공부하게 된 계기는 <a href="https://github.com/seojuncha/ftt-c-compiler">C 컴파일러</a>의 코드 생성(codegen) 단계를 직접 구현하려 했던 경험에서 비롯됐습니다.<br />
컴파일러는 소스 코드를 파싱한 뒤, 중간코드(IR)나 어셈블리 코드를 생성하는데, 이 과정에서 “C 코드가 어떤 어셈블리 코드로 변환되어야 하는가?”에 대한 이해가 부족해 구현이 어려웠습니다.</p>

<figure style="text-align: center;">
  <img src="/assets/img/ast-output-of-my-compiler.png" alt="자체 제작 컴파일러의 AST" width="70%" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">AST는 만들었는데… 도대체 여기서 어떤 어셈블리를 만들어야 하지?</figcaption>
</figure>

<p>어셈블리를 알게 되면, 최적화된 코드 작성, 디버깅, 리버스 엔지니어링 등에서도 큰 도움이 됩니다.</p>
<blockquote>
  <p>“어셈블리를 알면 모든 프로그램이 오픈소스다.” 라는 말이 있죠.<br />
물론 현실에서는 ISA(Instruction Set Architecture)를 함께 이해해야 가능한 이야기입니다.</p>
</blockquote>

<h2 id="간단한-어셈블리-코드-예시">간단한 어셈블리 코드 예시</h2>
<p>이제 각 시리즈에서 사용될 기반 코드의 가장 간단한 형태를 살펴보겠습니다.</p>

<p><strong>only-main.s</strong></p>
<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nf">.text</span>
  <span class="nf">.global</span> <span class="nv">_start</span>
<span class="nl">_start:</span>
  <span class="nf">b</span> <span class="nv">.</span>
</code></pre></div></div>

<p>이 코드는 아무런 기능도 수행하지 않지만, ARM 어셈블리 프로그래밍의 시작점으로 아주 훌륭합니다.
이렇게 짧은 코드 한 줄로도 무한 루프를 만들 수 있습니다.</p>

<blockquote>
  <p>C 언어나 Python에서 “Hello, World!“를 출력하는 것도 어셈블리로는 꽤 복잡합니다.
따라서 먼저 최소한의 어셈블리 코드로부터 시작해보는 것이 좋습니다.</p>
</blockquote>

<h2 id="환경-설정">환경 설정</h2>
<p>개발은 Debian Linux 환경에서 진행되었으며, Ubuntu에서도 동일하게 적용 가능합니다.
기타 리눅스 배포판 사용자는 패키지 매니저에 맞게 설치하시면 됩니다.</p>

<p>ARM 어셈블리 코드를 작성하고 실행하기 위해 필요한 도구는 다음과 같습니다:</p>
<ul>
  <li>arm-none-eabi-gcc (ARM용 크로스 컴파일러)</li>
  <li>qemu-system-arm (ARM 가상머신)</li>
  <li>gdb-multiarch (멀티 아키텍처 디버거)</li>
</ul>

<h3 id="1-arm용-gcc-설치">1. ARM용 GCC 설치</h3>
<p>우리가 사용하는 대부분의 PC는 x86 아키텍처 기반입니다.
그렇기 때문에 기존의 GCC로는 ARM용 바이너리를 만들 수 없습니다.</p>

<p>우리가 설치할 도구는 ARM용 크로스 컴파일러이며,
<a href="https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads">공식 툴체인</a>에서 다운로드 가능합니다.</p>

<ul>
  <li>OS: Linux (x86_64)</li>
  <li>Target: bare-metal</li>
  <li>Format: arm-none-eabi-gcc.tar.gz</li>
</ul>

<figure style="text-align: center;">
  <img src="/assets/img/arm-toolchain-download-page.png" alt="ARM툴체인 다운로드 페이지" style="display: block; margin: auto;" />
</figure>

<blockquote>
  <p>GUI 환경이 아니라면 <code class="language-plaintext highlighter-rouge">wget</code>으로 다운로드할 수도 있습니다:</p>
  <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>wget https://developer.arm.com/path/to/your/toolchain.tar.gz
</code></pre></div>  </div>
</blockquote>

<p>압축 해제 후, bin 디렉토리를 PATH에 등록합니다.</p>

<p>설치 확인:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="nt">--version</span>
</code></pre></div></div>

<h3 id="2-qemu-설치">2. QEMU 설치</h3>
<p>QEMU는 다양한 CPU 및 보드를 에뮬레이션할 수 있는 강력한 가상머신입니다.
ARM 하드웨어 없이도 ARM 프로그램을 실행할 수 있기 때문에, 어셈블리 학습에 매우 유용합니다.</p>

<p><a href="https://www.qemu.org/download/">QEMU 공식 다운로드 페이지</a></p>

<p>Debian/Ubuntu에서는 다음 명령으로 설치할 수 있습니다:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>qemu-system qemu-user-static
</code></pre></div></div>

<h3 id="3-gdb-multiarch-설치">3. GDB (Multiarch) 설치</h3>
<p>어셈블리 프로그램을 제대로 디버깅하기 위해 GDB가 필요합니다.
ARM 바이너리를 분석할 수 있는 gdb-multiarch를 설치합니다:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>gdb-multiarch
</code></pre></div></div>

<p>설치 후, GDB 내부에서 <code class="language-plaintext highlighter-rouge">target remote</code> 명령을 통해 QEMU에 연결할 수 있습니다.</p>

<h2 id="어셈블리-코드-컴파일">어셈블리 코드 컴파일</h2>
<p>작성한 <code class="language-plaintext highlighter-rouge">.s</code> 어셈블리 파일을 컴파일해 실행 가능한 바이너리(ELF)를 생성합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
	<span class="nt">-O0</span> <span class="se">\</span>
	<span class="nt">-nostdlib</span> <span class="se">\</span>
	<span class="nt">-march</span><span class="o">=</span>armv4 <span class="se">\</span>
	<span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
	only-main.s <span class="se">\</span>
	<span class="nt">-o</span> only-main.elf
</code></pre></div></div>

<p>사용한 컴파일 옵션은:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">-O0</code>: 최적화 비활성화</li>
  <li><code class="language-plaintext highlighter-rouge">-nostdlib</code>: 표준 라이브러리 미포함</li>
  <li><code class="language-plaintext highlighter-rouge">-march=armv4</code>: 타겟 아키텍쳐 지정</li>
  <li><code class="language-plaintext highlighter-rouge">-Ttext=0x10000</code>: text 섹션 시작 주소</li>
</ul>

<p>최종 결과물로 <code class="language-plaintext highlighter-rouge">only-main.elf</code>라는 파일이 생성됩니다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>file only-main.elf
only-main.elf: ELF 32-bit LSB executable, ARM, EABI5 version 1 <span class="o">(</span>SYSV<span class="o">)</span>, statically linked, not stripped
</code></pre></div></div>

<h2 id="코드-구조-설명">코드 구조 설명</h2>
<p>우리가 작성한 <code class="language-plaintext highlighter-rouge">only-main.s</code> 코드는 총 4줄에 불과하지만, 어셈블리의 핵심 개념이 모두 담겨 있습니다.
각 줄이 의미하는 바를 하나씩 살펴보겠습니다.</p>

<h3 id="text---코드-영역-지정">.text - 코드 영역 지정</h3>
<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">.text</span>
</code></pre></div></div>
<p>어셈블리에서는 .(dot)으로 시작하는 지시어(directive)를 사용하여 프로그램의 구조를 정의합니다.
<code class="language-plaintext highlighter-rouge">.text</code>는 이 아래에 작성될 명령어들이 <strong>코드 영역(Text Section)</strong>에 위치함을 의미합니다.
즉, 실제 실행 가능한 명령어들이 저장되는 메모리 영역을 가리킵니다.</p>

<blockquote>
  <p>향후 포스팅에서 다룰 ELF 구조나 메모리 맵에서도 <code class="language-plaintext highlighter-rouge">.text</code>, <code class="language-plaintext highlighter-rouge">.data</code>, <code class="language-plaintext highlighter-rouge">.bss</code> 같은 섹션은 중요한 개념입니다.</p>
</blockquote>

<figure style="text-align: center;">
  <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/5/50/Program_memory_layout.pdf/page1-234px-Program_memory_layout.pdf.jpg" alt="Wiki 프로그램 메모리 구조" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">출처: <a href="https://en.wikipedia.org/wiki/Data_segment#Program_memory">[Wikipedia - Program memory]</a></figcaption>
</figure>

<h3 id="global-_start---외부-심볼로-내보내기">.global _start - 외부 심볼로 내보내기</h3>
<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">.global</span> <span class="nv">_start</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">.global</code> 지시어는 해당 레이블(_start)을 외부 심볼로 노출합니다.
이것은 링커가 이 심볼을 <strong>프로그램의 진입점(entry point)</strong>으로 인식할 수 있도록 하기 위해 필요합니다.</p>

<p>C 프로그램에서는 <code class="language-plaintext highlighter-rouge">main()</code> 함수가 시작점이지만, 어셈블리에서는 <code class="language-plaintext highlighter-rouge">_start</code>와 같은 명시적인 entry point가 필요합니다.</p>

<h3 id="_start---레이블-정의">_start: - 레이블 정의</h3>
<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">_start:</span>
</code></pre></div></div>
<p>콜론(:)은 <strong>레이블(label)</strong>을 정의합니다.
레이블은 해당 위치에 이름을 붙여주는 것으로, 이후 분기(branch)나 함수 호출 등의 명령에서 참조할 수 있습니다.</p>

<p>여기서 <code class="language-plaintext highlighter-rouge">_start</code>는 <code class="language-plaintext highlighter-rouge">.global</code>로 외부에 노출되며, 링커가 프로그램의 시작 주소로 사용합니다.</p>

<h3 id="b----현재-위치로-분기-무한-루프">b . - 현재 위치로 분기 (무한 루프)</h3>
<div class="language-nasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">b</span> <span class="nv">.</span>
</code></pre></div></div>
<p>b는 분기(Branch) 명령어입니다.
실행 흐름을 지정된 주소(혹은 레이블)로 이동시킵니다.</p>

<p>.(dot)는 현재 명령어의 주소를 의미합니다. 즉, “지금 위치로 다시 분기하라”는 의미이며, 이로 인해 무한 루프가 형성됩니다.</p>

<blockquote>
  <p>이 무한 루프는 디버깅 시 프로그램이 끝나지 않고 멈춰있도록 하기 위한 의도적인 구조입니다.
추후 gdb로 연결해 레지스터나 메모리 상태를 확인할 때 유용합니다.</p>
</blockquote>

<h2 id="마무리">마무리</h2>
<p>이처럼 짧은 코드이지만, ARM 어셈블리에서 자주 사용하는 핵심 구조를 모두 담고 있습니다.
실제로 이 구조는 이후 장에서 다루게 될 대부분의 예제의 기본 템플릿이 될 것입니다.</p>

<p>이번 포스팅에서는 가장 단순한 ARM 어셈블리 코드를 작성하고, 컴파일 및 실행을 준비했습니다.
다음 포스팅에서는 이 코드를 확장하여 CPU 레지스터에 값을 저장하고, QEMU와 GDB를 통해 상태를 확인해보겠습니다.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="assembly" /><category term="embedded" /><category term="low-level" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[이번 포스팅에서는 ARM 기반의 어셈블리 코드를 작성하고 실행하는 방법을 소개합니다. 향후 이어질 시리즈의 기본이 되는 내용이므로, 차근차근 따라오시길 추천드립니다. 간단한 어셈블리 코드를 작성하고, 컴파일, 실행, 디버깅하는 기본적인 흐름을 익히는 것이 목표입니다. 또한 이후 포스팅에서 사용할 QEMU 가상환경과 GDB 디버깅 도구도 함께 준비합니다.]]></summary></entry><entry xml:lang="ko"><title type="html">로우레벨 프로그래밍이란?</title><link href="https://seojuncha.github.io/programming/low-level/2025/09/01/what-is-low-level-programming.html" rel="alternate" type="text/html" title="로우레벨 프로그래밍이란?" /><published>2025-09-01T12:48:00+00:00</published><updated>2025-09-01T12:48:00+00:00</updated><id>https://seojuncha.github.io/programming/low-level/2025/09/01/what-is-low-level-programming</id><content type="html" xml:base="https://seojuncha.github.io/programming/low-level/2025/09/01/what-is-low-level-programming.html"><![CDATA[<p>우리가 매일 사용하는 스마트폰, 노트북, 서버와 같은 모든 전자기기는 복잡한 소프트웨어와 하드웨어의 조합으로 이루어져 있습니다.
많은 사람들은 웹이나 앱 같은 눈에 보이는 부분만 접하지만, 그 아래에는 보이지 않는 세계가 있습니다.
바로 <strong>저수준 프로그래밍</strong>입니다.</p>

<p>저는 약 10여년의 프로그래밍 경력동안 다양한 언어와 환경을 접하였지만, 항상 그 내부의 동작이 궁금했고 명확하게 동작을 예상할 수 없는 코드가 점점 불편했습니다.
현업에서 가장 많이 사용한 C++도 비교적 추상화 수준이 덜함에도 불구하고 사용하면 사용할 수록, 도대체 내부에서 무슨일이 벌어지는지 직관적으로 이해하기 어렵다는 점이 맘에 들지 않았습니다.</p>

<blockquote>
  <p>물론, 파이썬과 같은 스크립트 언어들은 아주 유용하다는 것에 100% 동의합니다. 저도 굉장히 자주 사용하고 있습니다!</p>
</blockquote>

<p>그래서 비록 지금까지의 경력사항에서는 저수준 프로그래밍을 직접적으로 다루지 않았지만, 개인적인 호기심과 연구활동으로 이어가려합니다.</p>

<p>이 글에서는 저수준 프로그래밍이 무엇인지, 왜 중요한지, 그리고 우리가 왜 관심을 가져야 하는지를 살펴보겠습니다.</p>

<h2 id="저수준-프로그래밍의-정의">저수준 프로그래밍의 정의</h2>
<p>저수준 프로그래밍이란, <strong>하드웨어와 직접 맞닿아 있는 형태</strong>의 프로그래밍을 의미합니다.</p>
<ul>
  <li>CPU의 레지스터(register)를 다루고</li>
  <li>메모리 주소를 직접 지정하며</li>
  <li>하드웨어 동작 원리에 따라 프로그램을 설계하는 방식입니다.</li>
</ul>

<p>고수준 언어인 Python, Java, JavaScript등은 추상화가 강해서 개발자가 하드웨어 세부사항을 신경쓰지 않아도 됩니다.
반면에 C, Assembly 같은 언어는 하드웨어 동작과 거의 일대일로 대응하기 때문에 <strong>저수준 언어</strong>라고 부릅니다.</p>

<p>개인적인 생각으로는, C언어 역시 저수준 보다는 고수준 언어에 가깝다고 생각합니다.
하드웨어 세부사항을 신경쓰지 않아도 되고 하드웨어 동작과 거의 일대일로 대응한다는 것은 즉, 하드웨어가 변경될 때 코드를 수정할 필요가 없는(혹은 최소한의 수정만 필요한) 것을 뜻합니다.
C언어는 현대의 고수준 언어에 비해서 하드웨어 세부사항을 이해하고 코드를 작성해야하는 것이 맞지만,
C언어의 뛰어난 이식성 덕분에 많은 코드 수정이 필요하지 않은 것도 사실이기 때문입니다.</p>

<p>반면에 어셈블리 수준의 언어에서는 하드웨어(특히 CPU와 메모리)에 따라서 코드 자체가 수정되어야 합니다. 이 과정을 원활히 만들어 주는 것이 컴파일러의 역할이기도 하죠.</p>

<h2 id="저수준-프로그래밍의-특징">저수준 프로그래밍의 특징</h2>
<p>저수준 프로그래밍은 다음과 같은 특징을 가집니다.</p>

<h3 id="1-하드웨어-제어-능력">1. 하드웨어 제어 능력</h3>
<ul>
  <li>입출력 장치(I/O), 메모리, CPU 명령어 수준까지 제어할 수 있습니다.</li>
  <li>예: 특정 메모리 주소에 직접 값을 저장하거나, 레지스터 값을 조작하는 명령 실행</li>
</ul>

<h3 id="2-세밀한-최적화-가능">2. 세밀한 최적화 가능</h3>
<ul>
  <li>코드가 하드웨어와 직접 연결되기 때문에 속도와 메모리 사용량을 극한까지 최적화할 수 있습니다.</li>
  <li>임베디드 시스템, 운영체제, 실시간 제어 시스템에서 반드시 필요합니다.</li>
</ul>

<h3 id="3-복잡성과-난이도">3. 복잡성과 난이도</h3>
<ul>
  <li>추상화가 적어 다루기가 어렵습니다.</li>
  <li>하지만 그만큼 <strong>컴퓨터의 본질적인 동작</strong>을 깊이 이해할 수 있습니다.</li>
</ul>

<h2 id="왜-중요한가">왜 중요한가?</h2>
<p>저수준 프로그래밍은 단순히 “옛날 방식”이 아니라, 지금도 여러 분야에서 핵심적인 역할을 하고 있습니다.</p>

<ul>
  <li>운영체제와 커널: Linux, Windows, macOS의 가장 핵심 부분은 C와 어셈블리로 작성되어 있습니다.</li>
  <li>드라이버와 펌웨어: 키보드, 마우스, 그래픽 카드, 네트워크 카드 등 모든 ㅈ아치는 저수준 코드 없이는 동작하지 않습니다.</li>
  <li>보안과 해킹: 시스템 취약점을 분석하려면 메모리 구조와 어셈블리 레벨을 이해해야 합니다.</li>
  <li>임베디드 시스템: 자동차, IoT 기기, 가전제품 등은 저수준 프로그래밍으로 제어됩니다.</li>
</ul>

<p>즉, 우리가 매일 사용하는 모든 기술의 밑바닥에는 저수준 프로그래밍이 존재합니다.</p>

<p><img src="/assets/img/github-linux-kernel-lanauge.png" alt="github linux kernel language" /></p>

<h2 id="간단한-예시">간단한 예시</h2>
<ul>
  <li>ARM 어셈블리에서 <code class="language-plaintext highlighter-rouge">add r0, r1, r2</code>라는 명령은 단순히 “덧셈”이 아니라, CPU 내부의 산술논리연산장치(ALU)가 레지스터 값을 받아 연산한 뒤
결과를 다시 저장하는 과정을 뜻합니다.</li>
  <li>Verilog에서 <code class="language-plaintext highlighter-rouge">assign y = a &amp; b;</code> 라는 한 줄은, 실제로는 반도체 회로에서 전류가 흐르며 1과 0을 판별하는 동작과 연결됩니다.</li>
  <li>C언어에서 <code class="language-plaintext highlighter-rouge">printf("Hello");</code> 라는 한 줄은, 내부적으로는 운영체제의 시스템 콜을 거쳐 커널이 입출력 장치를 제어하는 과정으로 이어집니다.</li>
</ul>

<p>이처럼 우리가 흔히 쓰는 코드 한 줄 뒤에는 수많은 저수준 동작들이 숨어 있습니다.
저수준 프로그래밍은 단순히 어려운 기술을 배우는 것이 아닙니다. “컴퓨터를 더 깊이 이해하고, 본질을 탐구하려는 태도” 그 자체입니다.</p>

<h2 id="마치며">마치며</h2>
<p>저수준 프로그래밍은 눈에 잘 보이지 않는 영역이지만, 우리가 사용하는 모든 기술의 근간을 이루고 있습니다.
현대 컴퓨터 시스템의 저수준 동작을 직접 구현하고 눈으로 확인할 수 있는 George Hotz의 <a href="https://github.com/seojuncha/fromthetransistor">“From the Transistor” 프로젝트</a>를 큰 줄기로 삼고,
<strong>이진수 표현</strong>, <strong>ARM 어셈블리</strong>, <strong>Verilog</strong>, <strong>컴파일러 및 OS 제작</strong> 과 같은 주제를 다룰 에정입니다.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="programming" /><category term="low-level" /><category term="low-level" /><category term="assembly" /><category term="C" /><category term="ARM" /><category term="Verilog" /><category term="compiler" /><category term="operating-system" /><summary type="html"><![CDATA[우리가 매일 사용하는 스마트폰, 노트북, 서버와 같은 모든 전자기기는 복잡한 소프트웨어와 하드웨어의 조합으로 이루어져 있습니다. 많은 사람들은 웹이나 앱 같은 눈에 보이는 부분만 접하지만, 그 아래에는 보이지 않는 세계가 있습니다. 바로 저수준 프로그래밍입니다.]]></summary></entry></feed>
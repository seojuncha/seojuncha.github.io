<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://seojuncha.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://seojuncha.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2025-12-04T10:04:42+00:00</updated><id>https://seojuncha.github.io/feed.xml</id><title type="html">Hacking the System, One Bit at a Time</title><subtitle>Cha Seojun&apos;s journey into the heart of computers—from transistor to terminal. Writing compilers, emulating hardware, and uncovering how machines truly work. A blog for hackers, builders, and curious minds.</subtitle><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><entry xml:lang="en"><title type="html">How Does the CMP Instruction Work in ARM Assembly?</title><link href="https://seojuncha.github.io/arm/assembly/2025/12/04/arm-cmp-instruction.html" rel="alternate" type="text/html" title="How Does the CMP Instruction Work in ARM Assembly?" /><published>2025-12-04T09:00:00+00:00</published><updated>2025-12-04T09:00:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/2025/12/04/arm-cmp-instruction</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/2025/12/04/arm-cmp-instruction.html"><![CDATA[<p>The <code class="language-plaintext highlighter-rouge">CMP</code> instruction in ARM is used to compare two values.
<strong>Comparison is performed by subtracting</strong> one value from the other and checking the resulting condition flags.
For example, <code class="language-plaintext highlighter-rouge">5 - 3 = 2</code> is positive, which means <code class="language-plaintext highlighter-rouge">5</code> is greater than <code class="language-plaintext highlighter-rouge">3</code>.
<code class="language-plaintext highlighter-rouge">3 - 5 = -2</code> is negative, so <code class="language-plaintext highlighter-rouge">3</code> is less than <code class="language-plaintext highlighter-rouge">5</code>.
And <code class="language-plaintext highlighter-rouge">5 - 5 = 0</code> tells us the two values are equal.</p>

<p>The <code class="language-plaintext highlighter-rouge">CMP</code> instruction uses this arithmetic principle but stores only the status of the result in the <code class="language-plaintext highlighter-rouge">CPSR</code>, not the actual subtraction result.</p>

<p><a href="https://github.com/seojuncha/arm-assembly-tutorial/tree/main/06_comparing-values"><strong>GitHub Example Code</strong></a></p>

<h2 id="cpu-operations-and-the-cpsr">CPU Operations and the CPSR</h2>
<p>In ARM, the <code class="language-plaintext highlighter-rouge">CPSR</code> stores the status of all arithmetic and logical operations.
Its <strong>upper four bits</strong> hold the <code class="language-plaintext highlighter-rouge">N</code>, <code class="language-plaintext highlighter-rouge">Z</code>, <code class="language-plaintext highlighter-rouge">C</code>, and <code class="language-plaintext highlighter-rouge">V</code> flags, which summarize the outcome of an operation.</p>

<blockquote>
  <p>The actual computation result is stored in a register, while the <code class="language-plaintext highlighter-rouge">CPSR</code> records only the status of that result.</p>
</blockquote>

<p>For more details on the <code class="language-plaintext highlighter-rouge">CPSR</code>, refer to <a href="/arm/assembly/tutorial/2025/11/27/arm-cpsr-condition-flags.html">this post</a>.</p>

<h2 id="the-cmp-instruction">The CMP Instruction</h2>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">cmp</span> <span class="nv">Rn</span><span class="o">,</span> <span class="nv">Operand2</span>
</code></pre></div></div>

<p>Here, <code class="language-plaintext highlighter-rouge">Rn</code> must be a register, and <code class="language-plaintext highlighter-rouge">Operand2</code> is a shifter operand.
As described earlier, <code class="language-plaintext highlighter-rouge">CMP</code> effectively performs a subtraction (<code class="language-plaintext highlighter-rouge">Rn - Operand2</code>).</p>

<p>However, it does not store the result back into <code class="language-plaintext highlighter-rouge">Rn</code> and instead updates only the <code class="language-plaintext highlighter-rouge">CPSR</code> flags.
In other words, <code class="language-plaintext highlighter-rouge">cmp Rn, Operand2</code> behaves like <code class="language-plaintext highlighter-rouge">subs Rn, Rn, Operand2</code>, <strong>except the register value is not modified</strong>.</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">CMP</code> and other comparison instructions <strong>automatically</strong> update the <code class="language-plaintext highlighter-rouge">CPSR</code> <strong>without</strong> requiring the <code class="language-plaintext highlighter-rouge">s</code> suffix.</p>
</blockquote>

<h2 id="example-code">Example Code</h2>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
    <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">5</span>
    <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">5</span>
    <span class="nb">cmp</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r1</span>
    <span class="nb">beq</span> <span class="nv">equal</span>

<span class="nl">equal</span><span class="err">:</span>
    <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mh">0xff</span>
</code></pre></div></div>

<p>After executing <code class="language-plaintext highlighter-rouge">cmp r0, r1</code>, the <code class="language-plaintext highlighter-rouge">CPSR</code> flags will be as follows:</p>

<table>
  <thead>
    <tr>
      <th>CPSR Flag</th>
      <th>Value</th>
      <th>Reason</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>N</td>
      <td>0</td>
      <td>5 - 5 ≥ 0</td>
    </tr>
    <tr>
      <td>Z</td>
      <td>1</td>
      <td>5 - 5 = 0</td>
    </tr>
    <tr>
      <td>C</td>
      <td>1</td>
      <td>No borrow</td>
    </tr>
    <tr>
      <td>V</td>
      <td>0</td>
      <td>No overflow</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>In <code class="language-plaintext highlighter-rouge">CMP</code>, the <code class="language-plaintext highlighter-rouge">C</code> flag(Bit[29]) is used for unsigned comparisons.<br />
<code class="language-plaintext highlighter-rouge">C=1</code> when no borrow occurs, and <code class="language-plaintext highlighter-rouge">C=0</code> when a borrow does occur.</p>
</blockquote>

<h2 id="debugging">Debugging</h2>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">(</span>gdb<span class="o">)</span> i r cpsr
0x60000010
</code></pre></div></div>
<p>Running <code class="language-plaintext highlighter-rouge">i r cpsr</code> in GDB shows the updated <code class="language-plaintext highlighter-rouge">CPSR</code> value after executing <code class="language-plaintext highlighter-rouge">CMP</code>.
Although GDB prints all 32 bits, we are mainly interested in the upper four bits (<code class="language-plaintext highlighter-rouge">N</code>, <code class="language-plaintext highlighter-rouge">Z</code>, <code class="language-plaintext highlighter-rouge">C</code>, <code class="language-plaintext highlighter-rouge">V</code>).</p>

<p>You can extract these bits easily using Python:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cpsr</span> <span class="o">=</span> <span class="mh">0x60000010</span>
<span class="n">flags</span> <span class="o">=</span> <span class="p">(</span><span class="n">cpsr</span> <span class="o">&gt;&gt;</span> <span class="mi">28</span><span class="p">)</span> <span class="o">&amp;</span> <span class="mh">0xF</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">{:04b}</span><span class="sh">"</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">flags</span><span class="p">))</span>
</code></pre></div></div>

<h2 id="references">References</h2>
<ul>
  <li><a href="https://github.com/seojuncha/arm-assembly-tutorial/blob/main/doc/arm-architecture-reference-manual_DDI0100.pdf">ARM Architecture Reference Manual</a></li>
  <li><a href="https://docs.python.org/3/tutorial/inputoutput.html">Python3 Input and Output</a></li>
  <li><a href="https://docs.python.org/3/library/string.html#formatspec">Python3 Format Specification</a></li>
</ul>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="arm cmp" /><category term="arm cpsr" /><summary type="html"><![CDATA[The CMP instruction in ARM is used to compare two values. Comparison is performed by subtracting one value from the other and checking the resulting condition flags. For example, 5 - 3 = 2 is positive, which means 5 is greater than 3. 3 - 5 = -2 is negative, so 3 is less than 5. And 5 - 5 = 0 tells us the two values are equal.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #15 - Conditional Execution Using CPSR and Condition Flags</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/27/arm-cpsr-condition-flags.html" rel="alternate" type="text/html" title="ARM Assembly #15 - Conditional Execution Using CPSR and Condition Flags" /><published>2025-11-27T11:20:00+00:00</published><updated>2025-11-27T11:20:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/27/arm-cpsr-condition-flags</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/27/arm-cpsr-condition-flags.html"><![CDATA[<p>Until now, every instruction we examined executed unconditionally once its turn came.
However, the ARM architecture provides a unique feature: it can <strong>execute an instruction only when a specific condition is met</strong>, using the <strong>condition flags</strong> stored in the <code class="language-plaintext highlighter-rouge">CPSR</code> register.</p>

<p>This allows ARM to implement conditional logic without using branch instructions, which is one of the architecture’s defining characteristics.
Reducing branches also helps maintain pipeline efficiency.</p>

<h2 id="cpsr-current-program-status-register">CPSR (Current Program Status Register)</h2>
<p>The <code class="language-plaintext highlighter-rouge">CPSR</code> register stores the CPU’s program state and contains four condition flags in its upper 4 bits:</p>

<table>
  <thead>
    <tr>
      <th>Bit</th>
      <th>Flag</th>
      <th>Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>31</td>
      <td>N</td>
      <td>Negative result</td>
    </tr>
    <tr>
      <td>30</td>
      <td>Z</td>
      <td>Zero result</td>
    </tr>
    <tr>
      <td>29</td>
      <td>C</td>
      <td>Carry</td>
    </tr>
    <tr>
      <td>28</td>
      <td>V</td>
      <td>Overflow</td>
    </tr>
  </tbody>
</table>

<p>To update these flags, the instruction must include the <code class="language-plaintext highlighter-rouge">s</code> suffix, such as <code class="language-plaintext highlighter-rouge">adds</code>, <code class="language-plaintext highlighter-rouge">subs</code>, or <code class="language-plaintext highlighter-rouge">movs</code>.</p>

<h2 id="condition-flags">Condition Flags</h2>
<p>Almost every ARM instruction can be made conditional.
By appending a two-letter condition code (e.g., <code class="language-plaintext highlighter-rouge">EQ</code>, <code class="language-plaintext highlighter-rouge">NE</code>, <code class="language-plaintext highlighter-rouge">MI</code>), the instruction will execute only when the corresponding <code class="language-plaintext highlighter-rouge">CPSR</code> flags match the condition.</p>

<figure style="text-align: center;"> 
  <img src="/assets/img/condition-code-table-in-arm-document.png" alt="ARM condition code table" style="display: block; margin: auto;" /> 
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;"> 
  Figure 1. Condition code summary from the ARM Architecture Reference Manual
  </figcaption> 
</figure>

<h2 id="instruction--condition">Instruction + Condition</h2>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{instruction}{condition}
</code></pre></div></div>

<p>To execute an instruction only <strong>when the previous result is negative</strong>:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">movmi</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r1</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">MI</code>(Minus) executes only when the <code class="language-plaintext highlighter-rouge">N</code> flag is <code class="language-plaintext highlighter-rouge">1</code>, meaning the previous result was negative.</p>

<h2 id="example-code">Example Code</h2>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">movs</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#-</span><span class="mi">1</span>
  <span class="nb">addmi</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">5</span>
  <span class="nb">addpl</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">1</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>This code performs:</p>
<ul>
  <li>Add <code class="language-plaintext highlighter-rouge">5</code> when the value is negative</li>
  <li>Add <code class="language-plaintext highlighter-rouge">1</code> when the value is zero or positive</li>
</ul>

<p>Equivalent C code:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">r0</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
<span class="n">r0</span> <span class="o">+=</span> <span class="p">(</span><span class="n">r0</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="o">?</span> <span class="mi">5</span> <span class="o">:</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<h2 id="debugging-cpsr-flags">Debugging CPSR Flags</h2>
<p>Use GDB to inspect <code class="language-plaintext highlighter-rouge">CPSR</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">(</span>gdb<span class="o">)</span> p/x <span class="nv">$cpsr</span>
</code></pre></div></div>

<p>Extract each flag:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 31<span class="o">)</span> &amp; 1   <span class="c"># N</span>
p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 30<span class="o">)</span> &amp; 1   <span class="c"># Z</span>
p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 29<span class="o">)</span> &amp; 1   <span class="c"># C</span>
p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 28<span class="o">)</span> &amp; 1   <span class="c"># V</span>
</code></pre></div></div>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we explored conditional execution using <code class="language-plaintext highlighter-rouge">CPSR</code> flags and condition codes.
In the next article, we will cover conditional branches using <code class="language-plaintext highlighter-rouge">CMP</code>/<code class="language-plaintext highlighter-rouge">CMN</code> and discuss how to choose between conditional execution and branching.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="condition flags" /><category term="arm cpsr" /><summary type="html"><![CDATA[Until now, every instruction we examined executed unconditionally once its turn came. However, the ARM architecture provides a unique feature: it can execute an instruction only when a specific condition is met, using the condition flags stored in the CPSR register.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #14 - Using the BL Instruction That Automatically Saves the Return Address</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/26/arm-branch-with-return-address.html" rel="alternate" type="text/html" title="ARM Assembly #14 - Using the BL Instruction That Automatically Saves the Return Address" /><published>2025-11-26T09:30:00+00:00</published><updated>2025-11-26T09:30:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/26/arm-branch-with-return-address</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/26/arm-branch-with-return-address.html"><![CDATA[<p>The <code class="language-plaintext highlighter-rouge">BL</code> instruction is an extended version of the <code class="language-plaintext highlighter-rouge">B</code> instruction described in the <a href="/arm/assembly/tutorial/2025/11/23/arm-basic-branch.html">previous post</a>, adding function-call capability.
While the <code class="language-plaintext highlighter-rouge">B</code> instruction simply jumps to a target label, the <code class="language-plaintext highlighter-rouge">BL</code> instruction <strong>automatically stores the return address in the <code class="language-plaintext highlighter-rouge">LR</code></strong> (Register <code class="language-plaintext highlighter-rouge">R14</code>) so execution can return after the branch.
For this reason, <code class="language-plaintext highlighter-rouge">BL</code> is a core mechanism for implementing function calls in ARM and forms the foundation for concepts like the stack, calling conventions, and frame pointers.</p>

<figure style="text-align: center;">
  <a href="https://youtu.be/cMMjuVVKaS0" target="_blank">
  <img src="/assets/img/ARM Assembly - bl instruction.png" alt="Youtube Video to explain ARM BL instruction" style="display: block; margin: auto;" /> 
  </a>
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  <a href="https://www.youtube.com/@seojuncha" target="_blank">youtube.com/@seojuncha</a>
  </figcaption>
</figure>

<p><a href="https://github.com/seojuncha/arm-assembly-tutorial/blob/main/05_branching/branch-with-link.s"><strong>GitHub Sample Code</strong></a></p>

<h2 id="why-we-need-to-store-the-return-address">Why We Need to Store the Return Address</h2>
<p>When a function finishes executing, it must return to the point where it was called.
If the return address is not stored, the CPU won’t know which instruction to execute next, so the address must be saved somewhere.</p>

<h2 id="the-bl-instruction">The BL Instruction</h2>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">bl</span> <span class="nv">label</span>
</code></pre></div></div>
<p>The syntax looks identical to <code class="language-plaintext highlighter-rouge">b label</code>, but the internal behavior is different.
When executing <code class="language-plaintext highlighter-rouge">bl label</code>, the CPU jumps to the label and simultaneously stores the address of the next instruction (the return address) into <code class="language-plaintext highlighter-rouge">LR</code>.
After the subroutine finishes, the value stored in <code class="language-plaintext highlighter-rouge">LR</code> is loaded back into <code class="language-plaintext highlighter-rouge">PC</code> to restore the original control flow.</p>

<figure style="text-align: center;">
  <img src="/assets/img/bl-with-lr-and-pc.png" alt="Diagram of PC and LR behavior during BL instruction execution" style="display: block; margin: auto;" /> 
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 1. When the BL instruction executes, the PC jumps to the subroutine while the LR stores the return address for later execution.
  </figcaption>
</figure>

<h2 id="the-lr-link-register">The LR (Link Register)</h2>
<p>ARM uses the <code class="language-plaintext highlighter-rouge">LR</code> as a dedicated register to temporarily store the return address when making function calls.
In ARMv4, register <code class="language-plaintext highlighter-rouge">R14</code> is used as the <code class="language-plaintext highlighter-rouge">LR</code>.</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">bl</span> <span class="nv">foo</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>   <span class="no">@</span> <span class="nv">LR</span> <span class="nv">has</span> <span class="nv">this</span> <span class="nv">instruction</span> <span class="nv">address</span><span class="mf">.</span>
</code></pre></div></div>

<p>When the subroutine finishes, execution must return using the value stored in <code class="language-plaintext highlighter-rouge">LR</code> by writing it into <code class="language-plaintext highlighter-rouge">PC</code>.</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">pc</span><span class="o">,</span> <span class="nv">lr</span>
</code></pre></div></div>

<blockquote>
  <p>Since ARMv4 does not support the <code class="language-plaintext highlighter-rouge">bx</code> instruction, returning is done using <code class="language-plaintext highlighter-rouge">mov pc, lr</code>.
Starting from ARMv5, <code class="language-plaintext highlighter-rouge">bx lr</code> became the standard return instruction.</p>
</blockquote>

<h2 id="example-code">Example Code</h2>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>
  <span class="nb">bl</span> <span class="nv">foo</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span>
  <span class="nb">b</span> <span class="mf">.</span>
<span class="nl">foo</span><span class="err">:</span>
  <span class="nb">add</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
  <span class="nb">mov</span> <span class="nv">pc</span><span class="o">,</span> <span class="nv">lr</span>
</code></pre></div></div>

<h3 id="execution-flow">Execution Flow</h3>
<ol>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #2</code> → R0 = 2</li>
  <li><code class="language-plaintext highlighter-rouge">bl foo</code> → PC = foo, LR = address of <code class="language-plaintext highlighter-rouge">mov r1, r0</code></li>
  <li><code class="language-plaintext highlighter-rouge">add r0, r0, #3</code> → R0 = 5</li>
  <li><code class="language-plaintext highlighter-rouge">mov pc, lr</code> → PC jumps to <code class="language-plaintext highlighter-rouge">mov r1, r0</code></li>
  <li>run <code class="language-plaintext highlighter-rouge">mov r1, r0</code></li>
</ol>

<p>This example shows a simple flow where the subroutine foo adds <code class="language-plaintext highlighter-rouge">3</code> to <code class="language-plaintext highlighter-rouge">R0</code> and returns the result to be stored in <code class="language-plaintext highlighter-rouge">R1</code>.
Without <code class="language-plaintext highlighter-rouge">mov pc, lr</code>, the result would never be written to <code class="language-plaintext highlighter-rouge">R1</code> and the program flow would not continue properly.</p>

<h3 id="debugging">Debugging</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">(</span>gdb<span class="o">)</span> target remote :1234
<span class="o">(</span>gdb<span class="o">)</span> display/i <span class="nv">$pc</span>
<span class="o">(</span>gdb<span class="o">)</span> display/i <span class="nv">$lr</span>
</code></pre></div></div>

<p>By inspecting <code class="language-plaintext highlighter-rouge">PC</code> and <code class="language-plaintext highlighter-rouge">LR</code> right after <code class="language-plaintext highlighter-rouge">bl foo</code>, you can clearly see that <code class="language-plaintext highlighter-rouge">BL</code> stores the next instruction’s address in <code class="language-plaintext highlighter-rouge">LR</code>.
In particular, if <code class="language-plaintext highlighter-rouge">LR</code> matches the address of <code class="language-plaintext highlighter-rouge">mov r1, r0</code>, the behavior of <code class="language-plaintext highlighter-rouge">BL</code> becomes very intuitive.</p>

<h2 id="conclusion">Conclusion</h2>
<p>In the next post, we’ll examine condition codes for conditional execution.
Until now, every instruction executed unconditionally, but ARM allows adding two-letter condition codes so instructions run only under specific conditions.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="arm bl" /><category term="arm branch instruction" /><category term="link register" /><category term="arm lr" /><summary type="html"><![CDATA[The BL instruction is an extended version of the B instruction described in the previous post, adding function-call capability. While the B instruction simply jumps to a target label, the BL instruction automatically stores the return address in the LR (Register R14) so execution can return after the branch. For this reason, BL is a core mechanism for implementing function calls in ARM and forms the foundation for concepts like the stack, calling conventions, and frame pointers.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #13 - Branching Execution Flow with the B Instruction</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/23/arm-basic-branch.html" rel="alternate" type="text/html" title="ARM Assembly #13 - Branching Execution Flow with the B Instruction" /><published>2025-11-23T01:00:00+00:00</published><updated>2025-11-23T01:00:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/23/arm-basic-branch</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/23/arm-basic-branch.html"><![CDATA[<p>In this post, we will explore how the <code class="language-plaintext highlighter-rouge">B</code> instruction is used for branching, along with practical examples.
The <code class="language-plaintext highlighter-rouge">B</code> instruction unconditionally moves the execution flow to a specified location, forming the foundation for conditionals, loops, and broader program structure.</p>

<figure style="text-align: center;">
  <a href="https://youtu.be/BZamp0G0viA" target="_blank">
  <img src="/assets/img/ARM Assembly - Basic Branch Instruction.png" alt="Youtube Video to explain ARM B instruction" style="display: block; margin: auto;" /> 
  </a>
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  <a href="https://www.youtube.com/@seojuncha" target="_blank">youtube.com/@seojuncha</a>
  </figcaption>
</figure>

<ul>
  <li><a href="https://github.com/seojuncha/arm-assembly-tutorial/blob/main/05_branching/branch.s">Sample Code in GitHub</a></li>
</ul>

<h2 id="why-do-we-use-the-b-instruction">Why Do We Use the B Instruction?</h2>
<p>The <code class="language-plaintext highlighter-rouge">B</code> instruction simply moves the execution flow to another position.
It does not return like a function call; its behavior is similar to the <code class="language-plaintext highlighter-rouge">goto</code> statement in C.</p>

<p>Typical uses include:</p>
<ul>
  <li>Implementing loops</li>
  <li>Skipping specific code blocks</li>
  <li>Simple control flow switching</li>
</ul>

<h2 id="how-execution-flow-branching-works">How Execution Flow Branching Works</h2>
<p>The CPU stores the address of the next instruction in the Program Counter (PC).
A branch instruction changes the PC to another address, altering where the CPU fetches the next instruction.</p>

<blockquote>
  <p>Note: In ARMv4’s pipeline, the PC appears as current instruction address + 8 bytes.
However, the assembler handles branch offset calculation, so this does not affect typical coding.</p>
</blockquote>

<h2 id="branch-instruction-b">Branch Instruction: B</h2>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">b</span> <span class="nb">label</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">b label</code> simply jumps to the address indicated by the label.
Internally, this is equivalent to updating the PC with the label’s address.</p>

<h2 id="understanding-labels">Understanding Labels</h2>
<p>A label gives a name to a specific instruction address.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">label</span><span class="err">:</span>
    <span class="nb">instructions</span>
</code></pre></div></div>

<p>A colon (:) must be placed after the label name.
The label’s address is the address of the first instruction below it.</p>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">foo</span><span class="err">:</span>
    <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">1</span>   <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="mh">0x10000</span>
    <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>   <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="mh">0x10004</span>
</code></pre></div></div>
<p>Here, the address of foo is <code class="language-plaintext highlighter-rouge">0x10000</code>.</p>

<h2 id="example-code">Example Code</h2>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>
  <span class="nb">b</span>   <span class="nv">foo</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span>     <span class="no">@</span> <span class="nv">never</span> <span class="nv">executed</span>
<span class="nl">foo</span><span class="err">:</span>
  <span class="nb">add</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
  <span class="nb">b</span>   <span class="nv">_start</span>
</code></pre></div></div>

<p><strong>Execution Flow</strong></p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #2</code></li>
  <li><code class="language-plaintext highlighter-rouge">b foo</code> → jumps to foo</li>
  <li><code class="language-plaintext highlighter-rouge">add r0, r0, #3</code></li>
  <li><code class="language-plaintext highlighter-rouge">b _start</code> → jumps back to _start</li>
  <li>executes again</li>
</ol>

<p>Thus, the value of <code class="language-plaintext highlighter-rouge">R0</code> repeats as 2 → 5 → 2 → 5 indefinitely.
The instruction <code class="language-plaintext highlighter-rouge">mov r1, r0</code> is never executed due to the branch.</p>

<h2 id="debugging-observing-pc-changes-with-gdb">Debugging: Observing PC Changes with GDB</h2>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">(</span>gdb<span class="o">)</span> target remote :1234
<span class="o">(</span>gdb<span class="o">)</span> b _start
<span class="o">(</span>gdb<span class="o">)</span> b foo
<span class="o">(</span>gdb<span class="o">)</span> display/i <span class="nv">$pc</span>
<span class="o">(</span>gdb<span class="o">)</span> c
<span class="o">(</span>gdb<span class="o">)</span> si
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">display/i $pc</code> is useful for watching how the PC changes step-by-step.
You can clearly see the PC jump directly to the label when executing <code class="language-plaintext highlighter-rouge">b foo</code>.</p>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we explored the basic unconditional branch instruction <code class="language-plaintext highlighter-rouge">B</code> in ARMv4.
In the next post, we will look at <code class="language-plaintext highlighter-rouge">BL</code>, the instruction used for function calls.
BL automatically stores the return address in the Link Register (<code class="language-plaintext highlighter-rouge">LR</code>), making it essential for implementing function calls.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="arm b" /><category term="arm branch instruction" /><summary type="html"><![CDATA[In this post, we will explore how the B instruction is used for branching, along with practical examples. The B instruction unconditionally moves the execution flow to a specified location, forming the foundation for conditionals, loops, and broader program structure.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #12 - Stack Memory and Addressing Modes</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/18/arm-stack-address-modes.html" rel="alternate" type="text/html" title="ARM Assembly #12 - Stack Memory and Addressing Modes" /><published>2025-11-18T13:00:00+00:00</published><updated>2025-11-18T13:00:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/18/arm-stack-address-modes</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/18/arm-stack-address-modes.html"><![CDATA[<p>In this post, we will explore how the various addressing modes of <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code> are used for PUSH and POP operations on stack memory.
In the previous post, we only covered how to use <code class="language-plaintext highlighter-rouge">LDM</code>/<code class="language-plaintext highlighter-rouge">STM</code> to read and write multiple words at once.<br />
In this post, we will build on that by connecting addressing modes with the stack structure.</p>

<h2 id="what-are-addressing-modes">What Are Addressing Modes?</h2>
<p>In the previous post, all examples assumed that memory addresses moved <strong>upwards (increasing)</strong> during <code class="language-plaintext highlighter-rouge">LDM</code>/<code class="language-plaintext highlighter-rouge">STM</code> execution.<br />
However, in practice, memory addresses may <strong>increase or decrease</strong>, depending on the situation. You can also control whether the memory block starts <strong>at the address pointed to by the base register (<code class="language-plaintext highlighter-rouge">Rn</code>)</strong> or <strong>at the next address</strong>.</p>

<p>These two aspects—direction and starting position—are defined by ARM’s <strong>addressing modes</strong>.</p>
<h2 id="the-four-addressing-modes-ia-ib-da-db">The Four Addressing Modes: IA, IB, DA, DB</h2>
<p>ARM’s LDM/STM instructions provide the following four addressing modes.</p>

<table>
  <thead>
    <tr>
      <th>Address Mode</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IA</td>
      <td>Increment After</td>
    </tr>
    <tr>
      <td>IB</td>
      <td>Increment Before</td>
    </tr>
    <tr>
      <td>DA</td>
      <td>Decrement After</td>
    </tr>
    <tr>
      <td>DB</td>
      <td>Decrement Before</td>
    </tr>
  </tbody>
</table>

<p><strong>Increment / Decrement</strong> describe whether addresses move <strong>upwards or downwards</strong>.<br />
<strong>Before / After</strong> determine whether the address in the base register (<code class="language-plaintext highlighter-rouge">Rn</code>) is <strong>included in or excluded from</strong> the accessed memory block.</p>

<h2 id="ia-increment-after-default">IA (Increment After, Default)</h2>
<ul>
  <li><strong>Increment</strong>: moves from a lower address to a higher address.</li>
  <li><strong>After</strong>: includes the base address (<code class="language-plaintext highlighter-rouge">Rn</code>) as the starting address.</li>
</ul>

<h4 id="address-computation">Address Computation</h4>
<ul>
  <li>start address = <code class="language-plaintext highlighter-rouge">Rn</code></li>
  <li>end adderss = <code class="language-plaintext highlighter-rouge">Rn + (count of registers × 4) − 4</code></li>
  <li>After execution, <code class="language-plaintext highlighter-rouge">Rn = Rn + (count of registers × 4)</code></li>
</ul>

<p>For example, if the register list contains 3 registers and <code class="language-plaintext highlighter-rouge">Rn = 0x1000</code>, it works as follows.</p>
<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0x1008  : end address
0x1004
0x1000  : start address

Rn = 0x100C
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/img/ia-addr-mode-in-memory.png" alt="Memory layout of IA addressing mode" style="display: block; margin: auto;" /> 
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    Figure 1. In IA mode, the memory block starts at the base register and progresses upward through consecutive words.
    </figcaption>
</figure>

<h2 id="ib-increment-before">IB (Increment Before)</h2>
<ul>
  <li><strong>Increment</strong>: moves from a lower address to a higher address.</li>
  <li><strong>Before</strong>: starts from the address immediately after <code class="language-plaintext highlighter-rouge">Rn</code>.</li>
</ul>

<h4 id="address-computation-1">Address Computation</h4>
<ul>
  <li>start address = <code class="language-plaintext highlighter-rouge">Rn + 4</code></li>
  <li>end address = <code class="language-plaintext highlighter-rouge">Rn + (count of registers × 4)</code></li>
  <li>After execution, <code class="language-plaintext highlighter-rouge">Rn = Rn + (count of registers × 4)</code></li>
</ul>

<p>For example, if there are 3 registers and <code class="language-plaintext highlighter-rouge">Rn = 0x1000</code>, it works as follows.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0x100C  : end address
0x1008
0x1004  : start address
0x1000

Rn = 0x100C
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/img/ib-addr-mode-in-memory.png" alt="Memory layout of IB addressing mode" style="display: block; margin: auto;" />
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;"> 
    Figure 2. In IB mode, the memory block starts just after the base address and moves upward through memory.
    </figcaption>
</figure>

<h2 id="da-decrement-after">DA (Decrement After)</h2>
<ul>
  <li><strong>Decrement</strong>: moves from a higher address to a lower address.</li>
  <li><strong>After</strong>: includes the base address (<code class="language-plaintext highlighter-rouge">Rn</code>) as the top of the block.</li>
</ul>

<h4 id="address-computation-2">Address Computation</h4>
<ul>
  <li>start address = <code class="language-plaintext highlighter-rouge">Rn − (count of registers × 4) + 4</code></li>
  <li>end address = <code class="language-plaintext highlighter-rouge">Rn</code></li>
  <li>After execution, <code class="language-plaintext highlighter-rouge">Rn = Rn − (count of registers × 4)</code></li>
</ul>

<p>For example, if there are 3 registers and <code class="language-plaintext highlighter-rouge">Rn = 0x1000</code>, it works as follows.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0x1000  : end address
0x0FFC
0x0FF8  : start address
0x0FF4
0x0FF0

Rn = 0x0FF4
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/img/da-addr-mode-in-memory.png" alt="Memory layout of DA addressing mode" style="display: block; margin: auto;" />
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    Figure 3. In DA mode, the memory block includes the base address at the top and moves downward through consecutive words.
    </figcaption>
</figure>

<h2 id="db-decrement-before">DB (Decrement Before)</h2>
<ul>
  <li><strong>Decrement</strong>: moves from a higher address to a lower address.</li>
  <li><strong>Before</strong>: starts below the base address (<code class="language-plaintext highlighter-rouge">Rn</code>).</li>
</ul>

<h4 id="address-computation-3">Address Computation</h4>
<ul>
  <li>start addreess = <code class="language-plaintext highlighter-rouge">Rn − (count of registers × 4)</code></li>
  <li>end address = <code class="language-plaintext highlighter-rouge">Rn − 4</code></li>
  <li>After execution, <code class="language-plaintext highlighter-rouge">Rn = Rn − (count of registers × 4)</code></li>
</ul>

<p>For example, if there are 3 registers and <code class="language-plaintext highlighter-rouge">Rn = 0x1000</code>, it works as follows.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0x1000
0x0FFC  : end address
0x0FF8
0x0FF4  : start address

Rn = 0x0FF4
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/img/db-addr-mode-in-memory.png" alt="Memory layout of DB addressing mode" style="display: block; margin: auto;" />
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    Figure 4. In DB mode, the memory block starts below the base address and is often used for descending stack-like layouts.
    </figcaption>
</figure>

<h2 id="how-addressing-modes-are-written-in-actual-arm-syntax">How Addressing Modes Are Written in Actual ARM Syntax</h2>
<p>It’s important to understand addressing modes conceptually, but in actual ARM assembly, addressing modes are applied as suffixes attached to the instruction.</p>

<p>For example, IA, IB, DA, and DB are written like this:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">ldmia</span> <span class="nv">r0</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
  <span class="nb">ldmdb</span> <span class="nv">r0</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
  <span class="nb">stmia</span> <span class="nv">r0</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
  <span class="nb">stmdb</span> <span class="nv">r0</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
</code></pre></div></div>
<ul>
  <li>The two-letter mode (IA/IB/DA/DB) comes immediately after LDM/STM.</li>
</ul>

<p>In other words, there is no space or dot — the instruction literally becomes LDMIA, LDMDB, etc.</p>

<blockquote>
  <p>The <code class="language-plaintext highlighter-rouge">!</code> for write-back is separate from the addressing mode
In this example:</p>
  <div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldmia</span> <span class="nb">r0</span><span class="err">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
</code></pre></div>  </div>
  <ul>
    <li><strong>IA</strong> is the addressing mode (how memory is accessed),</li>
    <li><strong>!</strong> means the base register(<code class="language-plaintext highlighter-rouge">R0</code>) is automatically updated.</li>
  </ul>
</blockquote>

<h2 id="stm-and-ldm-with-the-same-addressing-mode">STM and LDM with the Same Addressing Mode</h2>
<p>Now let’s see how data moves between memory and registers when <code class="language-plaintext highlighter-rouge">STM</code> and <code class="language-plaintext highlighter-rouge">LDM</code> use the same addressing mode.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x2</span>
  <span class="nb">mov</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>

  <span class="nb">stmia</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>

  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">0</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mi">0</span>
  <span class="nb">mov</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">#</span><span class="mi">0</span>

  <span class="nb">ldmia</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
</code></pre></div></div>

<p>In this case, since both <code class="language-plaintext highlighter-rouge">STMIA</code> and <code class="language-plaintext highlighter-rouge">LDMIA</code> use the same addressing mode,
they store data into a memory block and then read back from the exact same addresses.</p>

<figure style="text-align: center;">
  <img src="/assets/img/memory-access-with-same-address-mode.png" alt="STM and LDM memory access with same addressing mode" style="display: block; margin: auto;" />
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    Figure 5. When using the same addressing mode, STM and LDM access the same memory region for both storing and loading.
    </figcaption>
</figure>

<p>However, stack memory does not operate this way.
In a stack, PUSH and POP proceed <strong>in opposite directions</strong>, so you <em>cannot</em> simply use the same addressing mode.</p>

<h2 id="stack-specific-addressing-modes-fd-fa-ed-ea">Stack-Specific Addressing Modes: FD, FA, ED, EA</h2>
<p>ARM introduces stack-specific names—FD, FA, ED, EA—for handling stack operations.
These are not new behaviors, but aliases of IA/IB/DA/DB, renamed from a stack perspective.</p>

<table>
  <thead>
    <tr>
      <th>Stack Mode</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>FD</td>
      <td>Full Descending</td>
    </tr>
    <tr>
      <td>FA</td>
      <td>Full Ascending</td>
    </tr>
    <tr>
      <td>ED</td>
      <td>Empty Descending</td>
    </tr>
    <tr>
      <td>EA</td>
      <td>Empty Ascending</td>
    </tr>
  </tbody>
</table>

<p><strong>Full / Empty</strong> indicate whether the stack pointer (<code class="language-plaintext highlighter-rouge">SP</code>) points to a full location (last stored data) or an empty slot after PUSH/POP. <br />
<strong>Ascending / Descending</strong> describe whether the stack grows toward higher addresses (Ascending) or toward lower addresses (Descending) when pushing.</p>

<h3 id="example-push-on-an-fd-stack">Example: PUSH on an FD Stack</h3>
<p>The most common stack structure in ARM is the Full Descending (FD) stack.
PUSH on an FD stack can be implemented as follows.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">sp</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x2</span>
  <span class="nb">mov</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>

  <span class="nb">stmdb</span> <span class="nv">sp</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
  <span class="err">@</span> <span class="nv">R1</span> <span class="o">-&gt;</span> <span class="o">[</span><span class="mh">0x7FF4</span><span class="o">],</span> <span class="nv">R2</span> <span class="o">-&gt;</span> <span class="o">[</span><span class="mh">0x7FF8</span><span class="o">],</span> <span class="nv">R3</span> <span class="o">-&gt;</span> <span class="o">[</span><span class="mh">0x7FFC</span><span class="o">]</span>
  <span class="err">@</span> <span class="nv">sp</span> <span class="o">=</span> <span class="mh">0x7FF4</span>
</code></pre></div></div>

<p>Here, <code class="language-plaintext highlighter-rouge">stmdb sp!, {r1-r3}</code> behaves the same as <code class="language-plaintext highlighter-rouge">stmfd sp!, {r1-r3}</code> in terms of stack mode naming.
The <code class="language-plaintext highlighter-rouge">SP</code> moves from <code class="language-plaintext highlighter-rouge">0x8000</code> to 0<code class="language-plaintext highlighter-rouge">x7FF4</code>.
The actual memory locations used are as follows.</p>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0x7FF4</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x1</code>(<code class="language-plaintext highlighter-rouge">R1</code>)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0x7FF8</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x2</code>(<code class="language-plaintext highlighter-rouge">R2</code>)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0x7FFC</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x3</code>(<code class="language-plaintext highlighter-rouge">R3</code>)</td>
    </tr>
  </tbody>
</table>

<figure style="text-align: center;">
  <img src="/assets/img/push-address-mode-in-fd-stack.png" alt="PUSH memory layout with STMDB in an FD stack" style="display: block; margin: auto;" />
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    Figure 6. In an FD stack, STMDB (STMFD) moves the SP downward, storing new data at lower addresses.
    </figcaption>
</figure>

<h3 id="example-pop-on-an-fd-stack">Example: POP on an FD Stack</h3>
<p>Now let’s see how to restore values from an FD stack using POP.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">@</span> <span class="nv">Assume</span> <span class="nv">STMDB</span> <span class="nv">sp</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span> <span class="nv">has</span> <span class="nv">already</span> <span class="nv">been</span> <span class="nv">executed</span><span class="mf">.</span>

  <span class="nb">ldmia</span> <span class="nv">sp</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">-</span><span class="nv">r3</span><span class="o">}</span>
</code></pre></div></div>

<p>On an FD stack, POP is implemented as <code class="language-plaintext highlighter-rouge">ldmia sp!</code> in general addressing mode terms, or <code class="language-plaintext highlighter-rouge">ldmfd sp!</code> in stack mode naming.
The load order is as follows.</p>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Register</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0x7FF4</code></td>
      <td><code class="language-plaintext highlighter-rouge">R1</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0x7FF8</code></td>
      <td><code class="language-plaintext highlighter-rouge">R2</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">0x7FFC</code></td>
      <td><code class="language-plaintext highlighter-rouge">R3</code></td>
    </tr>
  </tbody>
</table>

<p>After the POP, the <code class="language-plaintext highlighter-rouge">SP</code> returns to <code class="language-plaintext highlighter-rouge">0x8000</code>.</p>

<figure style="text-align: center;">
  <img src="/assets/img/pop-address-mode-in-fd-stack.png" alt="POP memory layout with LDMIA in an FD stack" style="display: block; margin: auto;" />
    <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    Figure 7. In an FD stack, LDMIA (LDMFD) loads values from the lowest address upward and moves the SP back upward.
    </figcaption>
</figure>

<p>Thus, in an FD stack, using <code class="language-plaintext highlighter-rouge">STMDB</code> for PUSH and <code class="language-plaintext highlighter-rouge">LDMIA</code> for POP ensures that both operations work correctly on the same stack memory block.</p>

<h2 id="comparison-normal-address-modes-vs-stack-address-modes">Comparison: Normal Address Modes vs Stack Address Modes</h2>
<p>As we have seen, stack address modes are essentially aliases attached to IA/IB/DA/DB.</p>

<h3 id="for-stm-push">For STM (PUSH)</h3>

<table>
  <thead>
    <tr>
      <th>Non-stack Address Mode</th>
      <th>Stack Address Mode</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IA</td>
      <td>EA</td>
    </tr>
    <tr>
      <td>IB</td>
      <td>FA</td>
    </tr>
    <tr>
      <td>DA</td>
      <td>ED</td>
    </tr>
    <tr>
      <td>DB</td>
      <td>FD</td>
    </tr>
  </tbody>
</table>

<h3 id="for-ldm-pop">For LDM (POP)</h3>

<table>
  <thead>
    <tr>
      <th>Non-stack Address Mode</th>
      <th>Stack Address Mode</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IA</td>
      <td>FD</td>
    </tr>
    <tr>
      <td>IB</td>
      <td>ED</td>
    </tr>
    <tr>
      <td>DA</td>
      <td>FA</td>
    </tr>
    <tr>
      <td>DB</td>
      <td>EA</td>
    </tr>
  </tbody>
</table>

<p>In real ARM code, you will often see stack modes like <code class="language-plaintext highlighter-rouge">STMFD</code>, <code class="language-plaintext highlighter-rouge">LDMFD</code>,
but it is also common to use general addressing modes directly, such as <code class="language-plaintext highlighter-rouge">STMDB sp!</code> and <code class="language-plaintext highlighter-rouge">LDMIA sp!</code>.</p>

<p>Whichever syntax you choose, the behavior is the same, so at the tutorial stage it is more important to become comfortable with the general addressing modes.</p>

<h2 id="summary">Summary</h2>
<p>In this post, we explored what addressing modes in <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code> mean and how they are related to stack PUSH and POP operations.</p>

<p>Addressing modes are not just syntactic options; they are tools for precisely controlling the direction and range of memory access.
Once you understand them, you can use <code class="language-plaintext highlighter-rouge">LDM</code>/<code class="language-plaintext highlighter-rouge">STM</code> far more flexibly, not only for stacks but also for various memory block operations.</p>

<p>In the next post, we will look at the branch instruction <code class="language-plaintext highlighter-rouge">B</code>, which controls the flow of execution.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="arm ldm" /><category term="arm stm" /><category term="addressing modes" /><category term="stack memory" /><summary type="html"><![CDATA[In this post, we will explore how the various addressing modes of LDM and STM are used for PUSH and POP operations on stack memory. In the previous post, we only covered how to use LDM/STM to read and write multiple words at once. In this post, we will build on that by connecting addressing modes with the stack structure.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #11 - Memory Block Access(LDM, STM)</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/14/arm-memory-block-access.html" rel="alternate" type="text/html" title="ARM Assembly #11 - Memory Block Access(LDM, STM)" /><published>2025-11-14T09:30:00+00:00</published><updated>2025-11-14T09:30:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/14/arm-memory-block-access</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/14/arm-memory-block-access.html"><![CDATA[<p>In this post, we’ll look at the ARM instructions <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code>, which allow reading or writing <strong>multiple words at once</strong> from memory.
In the previous post, we learned that <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code> can only handle <strong>a single word</strong> at a time.<br />
However, when working with <strong>contiguous memory areas</strong> such as arrays or structures, repeatedly using <code class="language-plaintext highlighter-rouge">LDR</code> or <code class="language-plaintext highlighter-rouge">STR</code> becomes inefficient.
To solve this problem, ARM provides <code class="language-plaintext highlighter-rouge">LDM</code> (Load Multiple) and <code class="language-plaintext highlighter-rouge">STM</code> (Store Multiple), which can operate on memory blocks.</p>

<h2 id="memory-block">Memory Block</h2>

<p>In ARM programming, it’s very common to handle <strong>a group of contiguous data</strong> such as arrays or structures at once.<br />
Inside memory, these data are stored consecutively in <strong>word-sized units</strong>.<br />
This continuous region is called a <strong>memory block</strong>.</p>

<figure style="text-align: center;">
  <img src="/assets/img/memory-block.png" alt="Memory block composed of consecutive words" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 1. A memory block consists of consecutive words, each mapped to sequential memory addresses.
  </figcaption>
</figure>

<p>Memory is actually a <strong>linear address space</strong>, but for visualization, it’s often represented as a <strong>2D grid</strong> for clarity.<br />
Each cell represents one word (4 bytes) of storage.</p>

<p>As mentioned in the <a href="/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html">previous post</a>, we can access memory word by word using hexadecimal addresses.</p>

<h3 id="inefficiency-of-using-ldrstr">Inefficiency of Using LDR/STR</h3>
<p>Instructions like <code class="language-plaintext highlighter-rouge">STR</code> and <code class="language-plaintext highlighter-rouge">LDR</code> can only <strong>load or store one word</strong> at a time.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
  <span class="nb">str</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">[</span><span class="nv">r0</span><span class="o">]</span>
</code></pre></div></div>
<figure style="text-align: center;">
  <img src="/assets/img/ldr-str-single-access.png" alt="Single word access structure" style="display: block; margin: auto;" /> 
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 2. LDR/STR transfer data in a one-to-one relation between memory and register.
  </figcaption>
</figure>

<p>In other words, the relationship between Mem[address] and a register is <strong>one-to-one per word</strong>.
Although you can manipulate addresses using offsets, you still need to repeat <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code> to handle multiple words.
Such repetition is inefficient when dealing with arrays or structures that occupy contiguous memory.</p>

<h2 id="ldm-and-stm">LDM and STM</h2>
<p>ARM provides <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code> instructions to handle such contiguous memory blocks efficiently.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">LDM</code>: Loads multiple words from memory into registers.</li>
  <li><code class="language-plaintext highlighter-rouge">STM</code>: Stores the values of multiple registers into memory.</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  ldm Rn, {registers}
  stm Rn, {registers}
</code></pre></div></div>

<p>Here,<br />
<code class="language-plaintext highlighter-rouge">Rn</code> is the base address of the memory block to access.
<code class="language-plaintext highlighter-rouge">{registers}</code> is a list of registers, separated by commas or defined as a range using a dash (-).</p>

<blockquote>
  <p>We call <code class="language-plaintext highlighter-rouge">Rn</code> the base address, <strong>not</strong> the start address,
because the actual start address depends on the addressing mode, which we’ll discuss later.</p>
</blockquote>

<h3 id="working-with-register-lists">Working with Register Lists</h3>
<p>Let’s look at three examples to see how register lists are used.</p>

<blockquote>
  <p>Although only <code class="language-plaintext highlighter-rouge">LDM</code> is used here, <code class="language-plaintext highlighter-rouge">STM</code> works the same way.<br />
The only difference is that <code class="language-plaintext highlighter-rouge">STM</code> stores register values into memory instead of loading them.</p>
</blockquote>

<h4 id="example-1-load-two-words-from-memory-0x8000-into-r1-and-r2">Example 1) Load two words from memory 0x8000 into R1 and R2</h4>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">ldm</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">{</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">}</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">R1</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8000</code>]</li>
  <li><code class="language-plaintext highlighter-rouge">R2</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8004</code>]</li>
</ul>

<figure style="text-align: center;">
  <img src="/assets/img/ldm-comma-seperated-registers.png" alt="Comma-separated register list in LDM" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 3. The instruction <code>ldm r0, {r1, r2}</code> loads two consecutive words into R1 and R2.
  </figcaption>
</figure>

<h4 id="example-2-load-four-words-from-memory-0x8000-into-r1-to-r4">Example 2) Load four words from memory 0x8000 into R1 to R4</h4>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">ldm</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">{</span><span class="nv">r1</span> <span class="o">-</span> <span class="nv">r4</span><span class="o">}</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">R1</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8000</code>]</li>
  <li><code class="language-plaintext highlighter-rouge">R2</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8004</code>]</li>
  <li><code class="language-plaintext highlighter-rouge">R3</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8008</code>]</li>
  <li><code class="language-plaintext highlighter-rouge">R4</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x800C</code>]</li>
</ul>

<figure style="text-align: center;">
  <img src="/assets/img/ldm-range-registers.png" alt="Range register list in LDM" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 4. The instruction <code>ldm r0, {r1-r4}</code> reads four consecutive words and stores them sequentially in R1 to R4.
  </figcaption>
</figure>

<h4 id="example-3-load-from-memory-0x8000-into-r1-r2-and-r7">Example 3) Load from memory 0x8000 into R1, R2, and R7</h4>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">ldm</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">{</span><span class="nv">r1</span> <span class="o">-</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r7</span><span class="o">}</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">R1</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8000</code>]</li>
  <li><code class="language-plaintext highlighter-rouge">R2</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8004</code>]</li>
  <li><code class="language-plaintext highlighter-rouge">R7</code> ← Mem[<code class="language-plaintext highlighter-rouge">0x8008</code>]</li>
</ul>

<figure style="text-align: center;">
  <img src="/assets/img/ldm-mixed-registers.png" alt="Mixed register list in LDM" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 5. The instruction <code>ldm r0, {r1-r2, r7}</code> demonstrates that non-consecutive register lists can be specified.
  </figcaption>
</figure>

<h3 id="auto-updating-the-base-address">Auto-Updating the Base Address</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  ldm Rn!, {registers}
  stm Rn!, {registers}
</code></pre></div></div>
<p>Adding <code class="language-plaintext highlighter-rouge">!</code> after the base register <code class="language-plaintext highlighter-rouge">Rn</code> automatically updates its value to the next address after the operation.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x8000</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x2</span>
  <span class="nb">mov</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>
 
  <span class="nb">stm</span> <span class="nv">r0</span><span class="o">!,</span> <span class="o">{</span><span class="nv">r1</span> <span class="o">-</span> <span class="nv">r3</span><span class="o">}</span>
  <span class="err">@</span> <span class="nv">r0</span> <span class="o">=</span> <span class="nv">r0</span> <span class="o">+</span> <span class="o">(</span><span class="mi">3</span> <span class="o">*</span> <span class="mi">4</span><span class="o">)</span> <span class="o">=</span> <span class="mh">0x8000</span> <span class="o">+</span> <span class="mh">0xC</span> <span class="o">=</span> <span class="mh">0x800C</span>
</code></pre></div></div>
<p>In other words, you can store multiple registers and automatically move to the next memory position in one instruction.</p>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we looked at the basic ideas behind <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code> and learned how to use them. However, <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code> also have a concept called <strong>addressing modes</strong>, which control how memory addresses increase or decrease, and whether the base register is included before or after the access.</p>

<p>Addressing modes make much more sense when explained together with stack memory, so in the next post, we’ll go through them in detail while exploring how the stack works in ARM.</p>

<p>See you in the next post, where we’ll walk through the stack and its addressing modes together.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="memory block" /><category term="arm ldm" /><category term="arm stm" /><summary type="html"><![CDATA[In this post, we’ll look at the ARM instructions LDM and STM, which allow reading or writing multiple words at once from memory. In the previous post, we learned that LDR and STR can only handle a single word at a time. However, when working with contiguous memory areas such as arrays or structures, repeatedly using LDR or STR becomes inefficient. To solve this problem, ARM provides LDM (Load Multiple) and STM (Store Multiple), which can operate on memory blocks.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #10 - Understanding Stack Memory in ARM</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/07/arm-stack-memory.html" rel="alternate" type="text/html" title="ARM Assembly #10 - Understanding Stack Memory in ARM" /><published>2025-11-07T07:00:00+00:00</published><updated>2025-11-07T07:00:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/07/arm-stack-memory</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/07/arm-stack-memory.html"><![CDATA[<p>Today, we’ll learn about <strong>stack memory</strong>, which the CPU uses to handle temporary data.
Building on what we learned about memory access instructions in previous posts, we’ll prepare for the next topic — block memory access using <code class="language-plaintext highlighter-rouge">LDM</code> and <code class="language-plaintext highlighter-rouge">STM</code>.</p>

<p><strong>Previous Posts:</strong></p>
<ul>
  <li><a href="/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html">Accessing memory using LDR and STR</a></li>
  <li><a href="/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html">Applying offset addressing</a></li>
  <li><a href="/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index.html">Pre-index and Post-index addressing</a></li>
</ul>

<h2 id="what-is-temporary-data">What Is Temporary Data?</h2>
<p>Temporary data refers to information that is created and destroyed during program execution.
Typical examples include <strong>local variables</strong>, <strong>function parameters</strong>, and <strong>return addresses</strong> used in function calls.
Because these values exist only temporarily, they don’t need to occupy memory for the entire program runtime.</p>

<h2 id="arm-memory-layout">ARM Memory Layout</h2>
<p>Even temporary data must be stored in memory before the CPU can process it.
Memory is typically divided into several regions based on purpose:</p>
<ul>
  <li><strong>Code Segment:</strong> where executable instructions are stored.</li>
  <li><strong>Data Segment:</strong> where global and static variables are stored.</li>
  <li><strong>Heap Segment:</strong> where dynamically allocated data is stored.</li>
  <li><strong>Stack Segment:</strong> where function calls and local variables are stored.</li>
</ul>

<p>Among these, the <strong>code</strong> and <strong>stack</strong> segments are essential; in this post, we’ll focus on the stack.</p>

<figure style="text-align: center;">
  <img src="/assets/img/aapcs32-memory-categories.png" alt="[AAPCS32] Memory Categories" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Figure 1. Memory regions according to AAPCS32 6.2</figcaption>
</figure>

<h2 id="what-is-a-stack">What Is a Stack?</h2>
<p>A stack is literally a structure that “stacks” data.
It’s a type of data structure widely used across many areas of computer science.</p>

<h3 id="basic-stack-operations-push--pop">Basic Stack Operations: PUSH &amp; POP</h3>
<p>A stack <a href="https://www.w3schools.com/dsa/dsa_data_stacks.php">supports two key operations</a> — <strong>PUSH</strong>, which adds data, and <strong>POP</strong>, which removes it.
PUSH places data onto the top of the stack, and POP retrieves the most recently added item.</p>

<h3 id="stack-vs-queue">Stack vs Queue</h3>
<p>A stack can be imagined as a pile, whereas a queue can be imagined as a waiting line.
Both are linear data structures, but they differ in their input/output order.</p>

<ul>
  <li><strong><a href="https://www.geeksforgeeks.org/dsa/lifo-principle-in-stack/">Stack (LIFO)</a></strong> — The last element added is the first one removed.</li>
  <li><strong><a href="https://www.geeksforgeeks.org/dsa/introduction-to-queue-data-structure-and-algorithm-tutorials/">Queue (FIFO)</a></strong> — The first element added is the first one removed.</li>
</ul>

<p>The key point of a stack is that it processes <strong>the most recent data first</strong>.
This property makes it especially useful for managing function calls and local variables.</p>

<h2 id="arm-stack-memory">ARM Stack Memory</h2>
<p>Stack memory refers to the memory area that behaves like a stack data structure.
In ARM, it’s a continuous region used to store temporary data such as local variables and function parameters.</p>

<h3 id="structure-of-arm-stack-memory">Structure of ARM Stack Memory</h3>
<p>The ARM architecture uses a <strong>Full Descending (FD)</strong> stack model.
This means the stack grows <strong>downward in memory</strong>, and the Stack Pointer (SP) points to the last stored value.</p>

<figure style="text-align: center;">
  <img src="/assets/img/fd-stack-memory.png" alt="Full Descending Stack Memory Model" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Figure 2. Stack growth in FD (Full Descending) model</figcaption>
</figure>

<blockquote>
  <p><strong>Note: Four stack model types</strong></p>
  <ul>
    <li><strong>Full Stack:</strong> SP points to the last stored item.</li>
    <li><strong>Empty Stack:</strong> SP points to the next empty space.</li>
    <li><strong>Descending Stack:</strong> Stack grows toward lower memory addresses.</li>
    <li><strong>Ascending Stack:</strong> Stack grows toward higher memory addresses.</li>
  </ul>

  <p>Therefore, an FD stack grows downward (descending) while SP always points to the topmost (full) item.</p>
</blockquote>

<h3 id="stack-pointer-sp">Stack Pointer (SP)</h3>
<p>Stack memory is still just memory, so it must be accessed through addresses.
In the FD model, we must keep track of the most recently pushed data, so a dedicated register is used for that purpose.
This register is called the <strong>Stack Pointer (SP)</strong>.</p>

<blockquote>
  <p>In <strong>ARMv4</strong>, register <strong>R13</strong> is used as the stack pointer.</p>
</blockquote>

<h3 id="implementing-push--pop-using-ldr-and-str">Implementing PUSH &amp; POP Using LDR and STR</h3>

<h4 id="push-using-str">PUSH using STR</h4>
<p>The PUSH operation adds data to the stack in two steps:</p>

<ol>
  <li>Decrease SP by 4.</li>
  <li>Store the value at the new SP location.</li>
</ol>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">sp</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x80000</span>
  <span class="nb">str</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>
</code></pre></div></div>

<p>Since the stack grows downward, SP must be decreased.
Because the ARM stack uses 4-byte words, the SP is decreased by 4 for each push.</p>

<h4 id="pop-using-ldr">POP using LDR</h4>
<p>POP removes data from the top of the stack in two steps:</p>

<ol>
  <li>Load the value at the address pointed by SP.</li>
  <li>Increase SP by 4.</li>
</ol>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">sp</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x80000</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>
</code></pre></div></div>

<p>Because SP points to the most recent value in the FD model, we first read it, then increment SP to move to the next item.</p>

<h4 id="example-and-debugging">Example and Debugging</h4>
<p><strong>stack-push-pop.s</strong></p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">sp</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x80000</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x2</span>
  <span class="nb">mov</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>
  
  <span class="nb">str</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>
  <span class="nb">str</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>
  <span class="nb">str</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>
  
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>
  
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<blockquote>
  <p>In this example, we manually set the stack base address to <code class="language-plaintext highlighter-rouge">0x80000</code>.
In a real system, the stack pointer is initialized by the <a href="https://wiki.osdev.org/Linker_Scripts">linker script</a> or the kernel during process setup.</p>
</blockquote>

<p><strong>Build and Run in QEMU</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="nt">-nostdlib</span> <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="nt">-o</span> stack-push-pop.elf stack-push-pop.s
<span class="nv">$ </span>qemu-system-arm <span class="nt">-nographic</span> <span class="nt">-machine</span> versatilepb <span class="nt">-S</span> <span class="nt">-s</span> <span class="nt">-kernel</span> stack-push-pop.elf
</code></pre></div></div>

<p><strong>Debugging with GDB</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gdb-multiarch stack-push-pop.elf

<span class="o">(</span>gdb<span class="o">)</span> target remote :1234    <span class="c"># connect to GDB server</span>

<span class="c"># Check pushed values</span>
<span class="o">(</span>gdb<span class="o">)</span> x/3w <span class="nv">$sp</span>
<span class="o">(</span>gdb<span class="o">)</span> i r r1 r2 r3
<span class="o">(</span>gdb<span class="o">)</span> i r sp                 <span class="c"># SP after STR</span>

<span class="c"># Check popped values</span>
<span class="o">(</span>gdb<span class="o">)</span> i r r0
<span class="o">(</span>gdb<span class="o">)</span> x/3w <span class="nv">$sp</span>
<span class="o">(</span>gdb<span class="o">)</span> i r sp                 <span class="c"># SP after LDR</span>
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/img/stack-push-pop-gdb.png" alt="STR and LDR instructions shown as PUSH and POP in GDB disassembly" style="display: block; margin: auto;" /> 
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
  Figure 3. <code>str</code> and <code>ldr</code> instructions automatically shown as <code>push</code> and <code>pop</code> in GDB
  </figcaption>
</figure>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we examined both t<strong>he concept of the stack and how it works in ARM architecture</strong>.
The stack plays a crucial role in managing function calls and local variables.</p>

<p>However, when saving or restoring multiple registers, using only <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code> becomes inefficient, as shown below:</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x2</span>
  <span class="nb">mov</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>
  
  <span class="nb">str</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>
  <span class="nb">str</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>
  <span class="nb">str</span> <span class="nv">r3</span><span class="o">,</span> <span class="o">[</span><span class="nv">sp</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span><span class="o">]!</span>

  <span class="err">@</span> <span class="nv">You</span> <span class="nv">must</span> <span class="nv">repeat</span> <span class="nv">STR</span> <span class="nv">for</span> <span class="nv">each</span> <span class="nv">register</span> <span class="nv">you</span> <span class="nv">want</span> <span class="nv">to</span> <span class="nv">store</span><span class="mf">.</span>
</code></pre></div></div>

<p>As the number of instructions increases, CPU pipeline efficiency decreases.
To solve this problem, ARM provides the <code class="language-plaintext highlighter-rouge">LDM</code> (Load Multiple) and <code class="language-plaintext highlighter-rouge">STM</code> (Store Multiple) instructions.
In the next post, we’ll explore <strong>how to use them for efficient block memory access on the stack</strong>.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="arm stack memory" /><category term="ldm" /><category term="stm" /><summary type="html"><![CDATA[Today, we’ll learn about stack memory, which the CPU uses to handle temporary data. Building on what we learned about memory access instructions in previous posts, we’ll prepare for the next topic — block memory access using LDM and STM.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #9 - Memory Access with Automatic Address Calculation using pre-index and post-index</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index.html" rel="alternate" type="text/html" title="ARM Assembly #9 - Memory Access with Automatic Address Calculation using pre-index and post-index" /><published>2025-11-05T13:35:00+00:00</published><updated>2025-11-05T13:35:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index.html"><![CDATA[<p>When accessing memory in ARM assembly, simply using <code class="language-plaintext highlighter-rouge">[R1]</code> as an address is often not enough. 
When dealing with arrays or buffers stored in consecutive memory, repeatedly updating the base register using <code class="language-plaintext highlighter-rouge">ADD</code> can be inefficient.</p>

<p>To solve this, ARM provides an <strong>automatic address calculation</strong> mechanism through <strong>pre-index</strong> and <strong>post-index</strong> modes. 
In this post, we’ll clearly explain how these modes work, how they differ, 
and explore how the CPU handles address calculation internally with real assembly examples.</p>

<h2 id="why-do-we-need-pre-index-and-post-index">Why do we need pre-index and post-index?</h2>
<p>In <a href="/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html">the previous post</a>, we discussed the basic offset addressing mode using <code class="language-plaintext highlighter-rouge">[Rn, #offset]</code>.
In that form, the CPU simply calculates an address by adding a fixed offset to the base register <code class="language-plaintext highlighter-rouge">Rn</code> and accesses memory at that address.
However, this calculation happens each time the instruction runs,
and the base register <code class="language-plaintext highlighter-rouge">Rn</code> itself does <strong>not</strong> get updated automatically.</p>

<p>In this post, we’ll take it one step further and explore automatic address calculation, <strong>the pre-index and post-index addressing modes</strong>.
These modes allow the CPU to automatically update <code class="language-plaintext highlighter-rouge">Rn</code> during execution,
improving both performance and code compactness when working with repetitive memory accesses.</p>

<p>When accessing memory, the CPU first evaluates the expression inside the brackets to obtain the actual address. 
For example, <code class="language-plaintext highlighter-rouge">LDR R0, [R1]</code> loads the value stored at the address in <code class="language-plaintext highlighter-rouge">R1</code>. 
However, when reading consecutive data such as arrays, an additional <code class="language-plaintext highlighter-rouge">ADD R1, R1, #4</code> instruction is required each time. 
This increases instruction count and reduces performance.</p>

<p>To address this, ARM integrates automatic address calculation into <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code> instructions, 
allowing the CPU to perform the address update itself, improving both performance and pipeline efficiency.</p>

<figure style="text-align: center;">
  <img src="/assets/img/pre-post-offset.png" alt="[Offset vs Pre vs Post] Addressing modes overview" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">offset vs pre-index vs post-index — manual vs automatic address update</figcaption>
</figure>

<h2 id="what-is-automatic-address-calculation">What is automatic address calculation?</h2>
<p>Automatic address calculation is a feature where the CPU automatically increments or decrements the memory address<br />
during the execution of a load or store instruction.<br />
There are two main modes for this: <strong>pre-indexing</strong> and <strong>post-indexing</strong>.</p>

<h3 id="the-pre-index-mode">The pre-index mode</h3>
<p>The <strong>pre-index</strong> mode means that the CPU updates the base register <strong><em>before accessing memory</em></strong>.
In the form <code class="language-plaintext highlighter-rouge">[Rn, #offset]!</code>, the CPU calculates <code class="language-plaintext highlighter-rouge">Rn + offset</code>, writes it back to <code class="language-plaintext highlighter-rouge">Rn</code>, and then uses that address to access memory.</p>

<p>For example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]!</span>   <span class="no">@</span> <span class="nv">add</span> <span class="mi">4</span> <span class="nv">to</span> <span class="nv">R1</span><span class="o">,</span> <span class="nv">then</span> <span class="nv">load</span> <span class="nv">from</span> <span class="nv">the</span> <span class="nv">new</span> <span class="nv">address</span>
</code></pre></div></div>

<p>Execution steps:</p>
<ol>
  <li>R1 ← R1 + 4</li>
  <li>R0 ← Mem[R1]</li>
</ol>

<h3 id="the-post-index-mode">The post-index mode</h3>
<p>The <strong>post-index</strong> mode performs the memory access <strong><em>before updating the base register</em></strong>.
In <code class="language-plaintext highlighter-rouge">[Rn], #offset</code>, the CPU first accesses memory using <code class="language-plaintext highlighter-rouge">Rn</code>, then adds the offset afterward.</p>

<p>For example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>    <span class="no">@</span> <span class="nv">load</span> <span class="nv">from</span> <span class="nv">R1</span><span class="o">,</span> <span class="nv">then</span> <span class="nv">increment</span> <span class="nv">R1</span> <span class="nv">by</span> <span class="mi">4</span>
</code></pre></div></div>

<p>Execution steps:</p>
<ol>
  <li>R0 ← Mem[R1]</li>
  <li>R1 ← R1 + 4</li>
</ol>

<h2 id="comparing-memory-access-and-address-update-timing">Comparing memory access and address update timing</h2>
<p>Although both modes ultimately change Rn by adding the same offset,
their behavior differs depending on when the update occurs.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]!</span>   <span class="no">@</span> <span class="nv">pre</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">update</span> <span class="nv">R1</span> <span class="nv">before</span> <span class="nv">accessing</span> <span class="nv">memory</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>    <span class="no">@</span> <span class="nv">post</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">access</span> <span class="nv">memory</span> <span class="nv">first</span><span class="o">,</span> <span class="nv">then</span> <span class="nv">update</span> <span class="nv">R1</span>
</code></pre></div></div>
<ul>
  <li>In <strong>pre-index</strong>, <code class="language-plaintext highlighter-rouge">R1</code> is updated before memory access.</li>
  <li>In <strong>post-index</strong>, <code class="language-plaintext highlighter-rouge">R1</code> is updated after memory access.</li>
</ul>

<p>Thus, pre-indexing is used when you want to access the <strong>next</strong> element first,
while post-indexing is useful when you need to access the <strong>current</strong> element before moving on.</p>

<figure style="text-align: center;">
  <img src="/assets/img/timing-pre-post.png" alt="Timing: pre-index (update→access) vs post-index (access→update)" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">pre-index updates then accesses; post-index accesses then updates</figcaption>
</figure>

<h2 id="example-test-using-gcc-qemu-and-gdb">Example test using GCC, QEMU, and GDB</h2>
<p>Here’s a minimal example comparing both modes.
You can build and debug it using GCC, QEMU, and GDB as follows.</p>

<p><strong>pre-post.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">=</span><span class="nv">array</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]!</span>   <span class="no">@</span> <span class="nv">pre</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">R1</span> <span class="err">←</span> <span class="nv">R1</span><span class="o">+</span><span class="mi">4</span> <span class="nv">then</span><span class="o">,</span> <span class="nv">load</span>
  <span class="nb">ldr</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>    <span class="no">@</span> <span class="nv">post</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">R2</span> <span class="err">←</span> <span class="nv">Mem</span><span class="o">[</span><span class="nv">R1</span><span class="o">],</span> <span class="nv">then</span> <span class="nv">R1</span> <span class="err">←</span> <span class="nv">R1</span><span class="o">+</span><span class="mi">4</span>
  <span class="nb">b</span> <span class="mf">.</span>
<span class="nl">array</span><span class="err">:</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x11111111</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x22222222</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x33333333</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x44444444</span>
</code></pre></div></div>

<p><strong>Build &amp; Run QEMU</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
    <span class="nt">-nostdlib</span> <span class="se">\</span>
    <span class="nt">-march</span><span class="o">=</span>armv4 <span class="se">\</span>
    <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
    pre-post.s <span class="se">\</span>
    <span class="nt">-o</span> pre-post.elf
<span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
    <span class="nt">-machine</span> versatilepb <span class="se">\</span>
    <span class="nt">-nographic</span> <span class="se">\</span>
    <span class="nt">-S</span> <span class="nt">-s</span> <span class="se">\</span>
    <span class="nt">-kernel</span> pre-post.elf
</code></pre></div></div>
<p><strong>Debug with GDB</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gdb-multiarch pre-post.elf

<span class="o">(</span>gdb<span class="o">)</span> target remote :1234    <span class="c"># Connect to GDB server</span>
<span class="o">(</span>gdb<span class="o">)</span> i r r0 r1 r2           <span class="c"># Show registers, R0, R1, R2</span>
<span class="o">(</span>gdb<span class="o">)</span> x/4w 0x10010           <span class="c"># Print 4 words at 0x10010(address of array)</span>
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/gif/prepost.gif" alt="GDB pre/post step execution demo" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;"><code>R1</code> value changes during GDB step execution — pre-index updates first, post-index after.</figcaption>
</figure>

<p>After running step-by-step in GDB,
you’ll see that <code class="language-plaintext highlighter-rouge">R1</code> changes to <code class="language-plaintext highlighter-rouge">array+4</code> after the pre-index instruction,
and to <code class="language-plaintext highlighter-rouge">array+8</code> after the post-index instruction.</p>

<h2 id="hardware-level-perspective">Hardware-level perspective</h2>

<p>Inside the CPU, each instruction passes through multiple pipeline stages.
Typically, <strong>address calculation happens in the Execute stage</strong> via the ALU,
and <strong>memory access occurs in the Memory stage</strong>.</p>

<ul>
  <li>In <strong>pre-index</strong>, the address is calculated first (Execute) and then used for access (Memory).</li>
  <li>In <strong>post-index</strong>, memory access happens first, then the base register is updated (Write Back).</li>
</ul>

<p>This hardware design allows the CPU to <strong>update addresses automatically</strong> without additional ADD instructions,
reducing instruction count and improving pipeline throughput for repetitive memory operations.</p>

<h2 id="conclusion">Conclusion</h2>
<p>Both pre-index and post-index modes automate address updates,
and their only difference lies in <strong>whether the update occurs before or after memory access</strong>.</p>

<p>Use <strong>pre-index</strong> when you need to move to the next element before accessing it,
and <strong>post-index</strong> when you want to access the current element first.</p>

<p>In the next post, we’ll extend this idea to <strong>stack operations</strong> and explore how <code class="language-plaintext highlighter-rouge">LDM</code>/<code class="language-plaintext highlighter-rouge">STM</code> can load or store multiple registers efficiently.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ldr" /><category term="str" /><category term="pre-index" /><category term="post-index" /><category term="addressing mode" /><summary type="html"><![CDATA[When accessing memory in ARM assembly, simply using [R1] as an address is often not enough. When dealing with arrays or buffers stored in consecutive memory, repeatedly updating the base register using ADD can be inefficient.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #8 - Calculating Memory Addresses with Offsets</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html" rel="alternate" type="text/html" title="ARM Assembly #8 - Calculating Memory Addresses with Offsets" /><published>2025-11-02T04:40:00+00:00</published><updated>2025-11-02T04:40:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/02/arm-offset-addressing</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html"><![CDATA[<p>In this post, we’ll explore how <strong>offset-based addressing</strong> works in ARM assembly.<br />
You’ll learn how to calculate memory addresses efficiently <a href="/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html">using <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code></a> without needing separate <a href="/arm/assembly/tutorial/2025/10/27/arm-arithmetic-operations.html"><code class="language-plaintext highlighter-rouge">ADD</code> instructions</a> each time.</p>

<h2 id="why-we-need-offsets">Why We Need Offsets</h2>
<p>In assembly, a simple instruction like <code class="language-plaintext highlighter-rouge">ldr r0, [r1]</code> always accesses the same memory address.
But when working with arrays or structures <strong>stored in contiguous memory</strong>, we’d have to use extra <code class="language-plaintext highlighter-rouge">add</code> instructions each time to move to the next element.</p>

<p>For example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="nv">Read</span> <span class="nv">first</span> <span class="nv">data</span>
<span class="nl">add</span> <span class="nb">r1</span><span class="err">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span>     <span class="no">@</span> <span class="nv">Move</span> <span class="nv">to</span> <span class="nv">next</span> <span class="nv">element</span>
<span class="nl">ldr</span> <span class="nb">r2</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="nv">Read</span> <span class="nv">second</span> <span class="nv">data</span>
</code></pre></div></div>
<p>That works, but it’s repetitive and inefficient.
Instead, ARM lets us <strong>combine address calculation and memory access</strong>:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]</span>   <span class="no">@</span> <span class="nv">Reads</span> <span class="nv">directly</span> <span class="nv">from</span> <span class="nv">r1</span> <span class="o">+</span> <span class="mi">4</span>
</code></pre></div></div>
<p>This reduces instruction count and CPU cycles, improving performance.</p>

<figure style="text-align: center;">
  <img src="/assets/img/arm-offset-memory.png" alt="Byte-addressed memory with int array and base+offset" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">In byte-addressed memory, an <code>int</code> array advances by 4 bytes per index.</figcaption>
</figure>

<h2 id="how-offset-address-calculation-works">How Offset Address Calculation Works</h2>
<p>An offset represents the distance from a base register address.
ARM calculates the final memory address by adding or subtracting this offset from the base:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Address = Base Register + Offset
</code></pre></div></div>

<p>Offsets can be:</p>
<ul>
  <li>Immediate – a constant value like <code class="language-plaintext highlighter-rouge">#4</code> or <code class="language-plaintext highlighter-rouge">#8</code></li>
  <li>Register – the value of another register</li>
</ul>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">8</span><span class="o">]</span>    <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">+</span> <span class="mi">8</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">]</span>    <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">+</span> <span class="nv">r2</span>
</code></pre></div></div>
<p>Both addition (+) and subtraction (–) are supported,
and immediate offsets are limited to 12-bit unsigned values (<code class="language-plaintext highlighter-rouge">0</code>–<code class="language-plaintext highlighter-rouge">4095</code>).</p>

<h2 id="three-offset-forms-immediate-register-and-shifted-register">Three Offset Forms: Immediate, Register, and Shifted Register</h2>
<p>ARM provides three main ways to express an offset:</p>

<figure style="text-align: center;">
  <img src="/assets/img/arm-offset-forms.png" alt="Three offset forms: immediate, register, shifted register" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Offsets can be expressed as immediate, register, or shifted-register forms.</figcaption>
</figure>

<h3 id="1-immediate-offset--add-a-constant-directly">1. Immediate Offset – Add a constant directly.</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Rn, #imm]
</code></pre></div></div>
<p>Example:</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">12</span><span class="o">]</span>     <span class="no">@</span> <span class="nv">Load</span> <span class="nv">data</span> <span class="nv">from</span> <span class="o">(</span><span class="nv">r1</span> <span class="o">+</span> <span class="mi">12</span><span class="o">)</span>
</code></pre></div></div>

<h3 id="2-register-offset--add-the-value-of-another-register">2. Register Offset – Add the value of another register.</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Rn, Rm]
</code></pre></div></div>
<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">]</span>      <span class="no">@</span> <span class="nv">Load</span> <span class="nv">data</span> <span class="nv">from</span> <span class="o">(</span><span class="nv">r1</span> <span class="o">+</span> <span class="nv">r2</span><span class="o">)</span>
</code></pre></div></div>

<h3 id="3-shifted-register-offset--add-a-shifted-version-of-another-register">3. Shifted Register Offset – Add a shifted version of another register.</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Rn, Rm, Shift #n]
</code></pre></div></div>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">]</span>  <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">+</span> <span class="o">(</span><span class="nv">r2</span> <span class="o">&lt;&lt;</span> <span class="mi">2</span><span class="o">)</span>
</code></pre></div></div>

<p>Here, <code class="language-plaintext highlighter-rouge">lsl #2</code> means “×4”.
Since each int element is 4 bytes, this perfectly aligns with array indexing.</p>

<h2 id="example-code">Example Code</h2>
<p>Here’s how offset-based address calculation works in assembly:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r1</span><span class="err">,</span> <span class="o">=</span><span class="mh">0x1000</span>            <span class="no">@</span> <span class="nv">Base</span> <span class="nv">address</span> <span class="o">(</span><span class="nv">start</span> <span class="nv">of</span> <span class="nv">the</span> <span class="nv">array</span><span class="o">)</span>
<span class="nl">ldr</span> <span class="nb">r2</span><span class="err">,</span> <span class="o">=</span><span class="mi">3</span>                 <span class="no">@</span> <span class="nv">Index</span> <span class="nv">i</span> <span class="o">=</span> <span class="mi">3</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">]</span>   <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="mh">0x1000</span> <span class="o">+</span> <span class="o">(</span><span class="mi">3</span> <span class="o">&lt;&lt;</span> <span class="mi">2</span><span class="o">)</span> <span class="o">=</span> <span class="mh">0x100C</span>
                           <span class="err">@</span> <span class="nv">r0</span> <span class="o">=</span> <span class="nv">value</span> <span class="nv">of</span> <span class="nv">arr</span><span class="o">[</span><span class="mi">3</span><span class="o">]</span>
</code></pre></div></div>

<p>This is equivalent to accessing <code class="language-plaintext highlighter-rouge">arr[i]</code> in C.
The <code class="language-plaintext highlighter-rouge">lsl #2</code> part represents <code class="language-plaintext highlighter-rouge">i * 4</code>, perfectly matching a 4-byte int element.</p>

<figure style="text-align: center;">
  <img src="/assets/img/arm-offset-forms.png" alt="Scaling i by 4 using lsl #2" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;"><code>lsl #2</code> scales the index <code>i</code> by 4 to match the 4-byte size of <code>int</code></figcaption>
</figure>

<h2 id="c-to-assembly-conversion">C to Assembly Conversion</h2>
<p>In C, when accessing an array element, the compiler automatically calculates the memory address.</p>

<p>For example:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">arr</span><span class="p">[</span><span class="mi">4</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">40</span><span class="p">};</span>
<span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="n">arr</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>
</code></pre></div></div>

<p>The compiler might generate:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r1</span><span class="err">,</span> <span class="o">=</span><span class="nv">arr</span>                 <span class="no">@</span> <span class="nv">r1</span> <span class="o">=</span> <span class="mh">&amp;a</span><span class="nv">rr</span><span class="o">[</span><span class="mi">0</span><span class="o">]</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">8</span><span class="o">]</span>             <span class="no">@</span> <span class="nv">arr</span><span class="o">[</span><span class="mi">2</span><span class="o">]</span> <span class="err">→</span> <span class="nv">base</span> <span class="o">+</span> <span class="o">(</span><span class="mi">2</span> <span class="o">*</span> <span class="mi">4</span><span class="o">)</span> <span class="o">=</span> <span class="o">+</span><span class="mi">8</span>
</code></pre></div></div>

<p>If the index is stored in a variable:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r1</span><span class="err">,</span> <span class="o">=</span><span class="nv">arr</span>
<span class="nl">mov</span> <span class="nb">r2</span><span class="err">,</span> <span class="nv">r0</span>                   <span class="no">@</span> <span class="nv">r2</span> <span class="o">=</span> <span class="nv">i</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">]</span>     <span class="no">@</span> <span class="nv">arr</span><span class="o">[</span><span class="nv">i</span><span class="o">]</span> <span class="o">=</span> <span class="o">*(</span><span class="nv">base</span> <span class="o">+</span> <span class="nv">i</span><span class="o">*</span><span class="mi">4</span><span class="o">)</span>
</code></pre></div></div>

<p>In short:</p>
<ul>
  <li>Each int element is 4 bytes.</li>
  <li>Memory is byte-addressed, so the address increases by 4 for each index.</li>
  <li>ARM uses <code class="language-plaintext highlighter-rouge">lsl #2</code> to efficiently represent this <code class="language-plaintext highlighter-rouge">×4</code> scaling.</li>
</ul>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we learned how to use offsets in <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str </code> to calculate memory addresses efficiently.</p>

<p>Using offsets lets you perform both address calculation and memory access in one instruction.
This makes it easier and faster to work with arrays or structures stored in consecutive memory.</p>

<p>In the next post, we’ll explore how to use <strong>pre-index</strong> and <strong>post-index addressing</strong>
to perform automatic address updates after each memory access.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ldr" /><category term="str" /><category term="offset" /><category term="addressing" /><summary type="html"><![CDATA[In this post, we’ll explore how offset-based addressing works in ARM assembly. You’ll learn how to calculate memory addresses efficiently using LDR and STR without needing separate ADD instructions each time.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #7 - The Simplest Way to Access Memory with LDR and STR</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html" rel="alternate" type="text/html" title="ARM Assembly #7 - The Simplest Way to Access Memory with LDR and STR" /><published>2025-10-31T11:00:00+00:00</published><updated>2025-10-31T11:00:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html"><![CDATA[<p>This post is an introductory tutorial on <strong>memory access</strong> for beginners learning ARM assembly.
Using the ARMv4 architecture as a reference, we’ll explore how the CPU uses the <code class="language-plaintext highlighter-rouge">ldr</code> (load) and <code class="language-plaintext highlighter-rouge">str</code> (store) instructions to read and write data to memory.</p>

<p>In this post, you’ll learn:</p>
<ul>
  <li>Why values must be loaded into registers before computation</li>
  <li>How <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code> are structured and what they do</li>
  <li>What the square brackets (<code class="language-plaintext highlighter-rouge">[]</code>) mean in addressing mode</li>
  <li>How to inspect registers and memory states using <strong>QEMU</strong> and <strong>GDB</strong></li>
</ul>

<blockquote>
  <p><strong>Key Takeaway:</strong> <br />
<code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code> are the foundation of all ARM programs.
Through this tutorial, you’ll clearly see how the CPU actually reads and writes data.</p>
</blockquote>

<figure>
<svg role="img" aria-labelledby="title desc" viewBox="0 0 800 240" width="100%" xmlns="http://www.w3.org/2000/svg">

  <title id="title">CPU ↔ Memory data flow with LDR/STR</title>
  <desc id="desc">Shows LDR reading from Memory to Register and STR writing from Register to Memory.</desc>
  <style>
    .fg { stroke:#E6E6E6; fill:#E6E6E6; }
    .box { fill:#0B1220; stroke:#A3B1C6; }
    .wire { stroke:#E6E6E6; }
    .muted { fill:#C8D0DB; }
    .marker path { fill:#E6E6E6; }  
    .label{font:16px sans-serif}
    .note{font:15px sans-serif}
    .mono{font:14px monospace}
  </style>
  <defs>
    <marker class="marker" id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" />
    </marker>
  </defs>

  <!-- CPU Registers -->
  <rect class="box" x="60" y="50" width="280" height="140" rx="8" />
  <text class="label fg" x="200" y="80" text-anchor="middle">CPU Registers</text>
  <!-- little register slots -->
  <g transform="translate(90,100)">
    <rect class="box" x="0" y="0" width="50" height="24" rx="4" />
    <rect class="box" x="60" y="0" width="50" height="24" rx="4" />
    <rect class="box" x="120" y="0" width="50" height="24" rx="4" />
    <rect class="box" x="180" y="0" width="50" height="24" rx="4" />
    <text class="note fg" x="25" y="17" text-anchor="middle">r0</text>
    <text class="note fg" x="85" y="17" text-anchor="middle">r1</text>
    <text class="note fg" x="145" y="17" text-anchor="middle">r2</text>
    <text class="note fg" x="205" y="17" text-anchor="middle">…</text>
  </g>

  <!-- Memory -->
  <rect class="box" x="460" y="30" width="280" height="180" rx="8" />
  <text class="label fg" x="600" y="60" text-anchor="middle">Memory</text>
  <g transform="translate(490,80)">
    <rect class="box" x="0" y="0" width="220" height="28" />
    <rect class="box" x="0" y="36" width="220" height="28" />
    <rect class="box" x="0" y="72" width="220" height="28" />
    <text class="note fg" x="8" y="19">0x1000</text>
    <text class="note fg" x="8" y="55">0x1004</text>
    <text class="note fg" x="8" y="91">0x1008</text>
  </g>

  <!-- Arrows -->
  <line class="wire" x1="340" y1="120" x2="460" y2="120" stroke="#000" stroke-width="2" marker-end="url(#arrow)" />
  <text class="label fg" x="400" y="110" text-anchor="middle">STR</text>
  <line class="wire" x1="460" y1="160" x2="340" y2="160" stroke="#000" stroke-width="2" marker-end="url(#arrow)" />
  <text class="label fg" x="400" y="150" text-anchor="middle">LDR</text>
</svg>
<figcaption>
LDR moves data Memory → Register; STR moves data Register → Memory.
</figcaption>
</figure>

<h2 id="why-we-need-to-access-memory">Why We Need to Access Memory</h2>

<p>The ARM CPU’s data-processing instructions (<code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, etc.) <strong>never access memory directly</strong> — they operate only on values stored in registers.
Therefore, any value stored in memory <strong>must be loaded into a register before performing an operation</strong>.</p>

<p>ARMv4 provides a total of <strong>16</strong> general-purpose registers (<code class="language-plaintext highlighter-rouge">r0</code>–<code class="language-plaintext highlighter-rouge">r15</code>).
It’s impossible to store all program data in these registers alone.
Thus, the CPU uses registers as temporary storage for computation, while the main data resides in memory.</p>

<figure>
<svg role="img" aria-labelledby="title desc" viewBox="0 0 800 260" width="100%" xmlns="http://www.w3.org/2000/svg">

  <title id="title">Registers vs Memory roles</title>
  <desc id="desc">Shows 16 general-purpose registers contrasted with large memory as main storage.</desc>
  <style>
    .fg { stroke:#E6E6E6; fill:#E6E6E6; }
    .box { fill:#0B1220; stroke:#A3B1C6; }
    .wire { stroke:#E6E6E6; }
    .muted { fill:#C8D0DB; }
    .marker path { fill:#E6E6E6; }  
    .label{font:16px sans-serif}
    .note{font:13px sans-serif}
    .mono{font:14px monospace}
  </style>

  <!-- Registers grid -->
  <rect class="box" x="60" y="30" width="300" height="200" rx="8" />
  <text class="label fg" x="210" y="55" text-anchor="middle">Registers (16)</text>
  <g transform="translate(80,75)">
    <!-- 4 x 4 grid -->
    <g class="row" transform="translate(0,0)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r0</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r1</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r2</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r3</text>
    </g>
    <g class="row" transform="translate(0,40)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r4</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r5</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r6</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r7</text>
    </g>
    <g class="row" transform="translate(0,80)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r8</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r9</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r10</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r11</text>
    </g>
    <g class="row" transform="translate(0,120)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r12</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r13</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r14</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r15</text>
    </g>
  </g>

  <!-- Memory -->
  <rect class="box" x="440" y="30" width="300" height="200" rx="8" />
  <text class="label fg" x="590" y="55" text-anchor="middle">Main Memory (Data store)</text>
  <text class="note fg" x="590" y="90" text-anchor="middle">Large, holds program data</text>
  <text class="note fg" x="590" y="110" text-anchor="middle">Registers used for compute</text>
</svg>
<figcaption>
Registers are temporary for compute; memory is the main data store.
</figcaption>
</figure>

<h2 id="assembly-instructions-for-memory-access">Assembly Instructions for Memory Access</h2>

<p>There are two basic instructions for accessing memory:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">ldr</code>: loads data <strong>from memory into a register</strong>.</li>
  <li><code class="language-plaintext highlighter-rouge">str</code>: stores data <strong>from a register into memory</strong>.</li>
</ul>

<p>Their basic syntax is:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ldr Rd, [Rn]
str Rd, [Rn]
</code></pre></div></div>
<p>Where:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">Rd</code>: <strong><em>destination register</em></strong> to store or retrieve data.</li>
  <li><code class="language-plaintext highlighter-rouge">Rn</code>: <strong><em>base register</em></strong> containing the memory address.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">ldr</code> reads the value stored at memory address <code class="language-plaintext highlighter-rouge">Rn</code> into register <code class="language-plaintext highlighter-rouge">Rd</code>,
and <code class="language-plaintext highlighter-rouge">str</code> writes the value in <code class="language-plaintext highlighter-rouge">Rd</code> to the memory location pointed to by <code class="language-plaintext highlighter-rouge">Rn</code>.</p>

<h3 id="meaning-of-brackets-and-base-register">Meaning of Brackets and Base Register</h3>

<p>The value inside square brackets (<code class="language-plaintext highlighter-rouge">[]</code>) represents a memory address,
and the expression <code class="language-plaintext highlighter-rouge">[address]</code> refers to the value stored at that address.</p>

<p>For example:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ldr r0, [r1]
</code></pre></div></div>

<p>This means <em>“read the value stored at the <strong>memory address pointed to by</strong> <code class="language-plaintext highlighter-rouge">r1</code>, and put it into <code class="language-plaintext highlighter-rouge">r0</code>.”</em></p>

<figure>
<svg role="img" aria-labelledby="title desc" viewBox="0 0 800 220" width="100%" xmlns="http://www.w3.org/2000/svg">

  <title id="title">Base register addressing</title>

  <desc id="desc">Shows r1 holding an address 0x1000 and [r1] meaning value at memory[0x1000].</desc>

  <style>
    .fg { stroke:#E6E6E6; fill:#E6E6E6; }
    .box { fill:#0B1220; stroke:#A3B1C6; }
    .wire { stroke:#E6E6E6; }
    .muted { fill:#C8D0DB; }
    .marker path { fill:#E6E6E6; }  
    .label{font:16px sans-serif}
    .note{font:15px sans-serif}
    .mono{font:14px monospace}
  </style>

  <defs>
    <marker class="marker" id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" />
    </marker>
  </defs>

  <!-- r1 register -->
  <rect class="box" x="60" y="60" width="240" height="100" rx="8" />
  <text class="label fg" x="180" y="88" text-anchor="middle">r1</text>
  <text class="label fg" x="180" y="120" text-anchor="middle">0x00001000</text>

  <!-- memory cell -->
  <rect class="box" x="480" y="40" width="240" height="140" rx="8" />
  <text class="label fg" x="600" y="70" text-anchor="middle">Memory[0x1000]</text>
  <rect class="box" x="510" y="90" width="180" height="40" />
  <text class="mono fg" x="600" y="115" text-anchor="middle">0x000004D2</text>

  <!-- arrow and labels -->
  <line x1="300" y1="110" x2="480" y2="110" stroke="#6f6c6cff" stroke-width="2" marker-end="url(#arrow)" />
  <text class="note fg" x="390" y="100" text-anchor="middle">[r1] ≡ *(uint32_t*)0x1000</text>
</svg>
<figcaption>
If <code>r1</code> holds 0x1000, then <code>[r1]</code> means the value stored at that memory address.
</figcaption>
</figure>

<h2 id="how-the-cpu-reads-and-writes-data-via-an-address">How the CPU Reads and Writes Data via an Address</h2>

<p>The CPU interprets the value stored in the base register (<code class="language-plaintext highlighter-rouge">Rn</code>) as a memory address.
It then locates the memory cell corresponding to that address and either reads or writes the value stored there.</p>

<p>In other words:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">ldr</code> = read data from memory using the address.</li>
  <li><code class="language-plaintext highlighter-rouge">str</code> = write data to memory using the address.</li>
</ul>

<h2 id="practice-example">Practice Example</h2>

<p>The following examples demonstrate the basic behavior of <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code>.
You can verify the results using <strong>QEMU (versatilepb)</strong> with <code class="language-plaintext highlighter-rouge">gdb-multiarch</code>.</p>

<blockquote>
  <p>For compiling, running on QEMU, and setting up GDB, refer to <a href="/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html">this post</a>.</p>
</blockquote>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>       <span class="no">@</span> <span class="nv">store</span> <span class="nv">decimal</span> <span class="mi">3</span> <span class="nv">into</span> <span class="nv">r0</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">=</span><span class="mh">0x1000</span>    <span class="no">@</span> <span class="nv">store</span> <span class="nv">address</span> <span class="mh">0x1000</span> <span class="nv">into</span> <span class="nv">r1</span>
  <span class="nb">str</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="o">[</span><span class="mh">0x1000</span><span class="o">]</span> <span class="o">=</span> <span class="mi">3</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>Here, the CPU interprets the value in <code class="language-plaintext highlighter-rouge">r1</code> (<code class="language-plaintext highlighter-rouge">0x1000</code>) as a memory address
and stores the value of <code class="language-plaintext highlighter-rouge">r0</code> (<code class="language-plaintext highlighter-rouge">3</code>) into that memory location.</p>

<p>Next, we can load the value back from memory:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">=</span><span class="mh">0x1000</span>    <span class="no">@</span> <span class="nv">store</span> <span class="nv">address</span> <span class="mh">0x1000</span> <span class="nv">into</span> <span class="nv">r1</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="nv">load</span> <span class="nv">value</span> <span class="nv">from</span> <span class="o">[</span><span class="mh">0x1000</span><span class="o">]</span> <span class="nv">into</span> <span class="nv">r0</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>If memory address <code class="language-plaintext highlighter-rouge">0x1000</code> previously held <code class="language-plaintext highlighter-rouge">3</code>,
then after execution, <code class="language-plaintext highlighter-rouge">r0</code> will contain <code class="language-plaintext highlighter-rouge">3</code>.</p>

<hr />
<p>In this post, we explored <strong>the most fundamental way the CPU interacts with memory using <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code></strong>.
In the next post, we’ll cover more flexible addressing modes using offsets.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ldr" /><category term="str" /><category term="memory-access" /><category term="armv4" /><summary type="html"><![CDATA[This post is an introductory tutorial on memory access for beginners learning ARM assembly. Using the ARMv4 architecture as a reference, we’ll explore how the CPU uses the ldr (load) and str (store) instructions to read and write data to memory.]]></summary></entry></feed>
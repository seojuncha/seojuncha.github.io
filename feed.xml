<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://seojuncha.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://seojuncha.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2025-09-08T13:46:30+00:00</updated><id>https://seojuncha.github.io/feed.xml</id><title type="html">Hacking the System, One Bit at a Time</title><subtitle>Cha Seojun&apos;s journey into the heart of computers—from transistor to terminal. Writing compilers, emulating hardware, and uncovering how machines truly work. A blog for hackers, builders, and curious minds.</subtitle><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><entry xml:lang="en"><title type="html">ARM Assembly (1) – mov; Storing Values in Registers</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html" rel="alternate" type="text/html" title="ARM Assembly (1) – mov; Storing Values in Registers" /><published>2025-09-08T13:24:00+00:00</published><updated>2025-09-08T13:24:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/08/arm-mov-instruction</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html"><![CDATA[<p>Computers are machines designed to perform <em>calculations</em>.<br />
These calculations are ultimately performed by the <strong>CPU</strong>, and a typical operation requires two things:
<strong>operands</strong> and an <strong>operator</strong>.<br />
Operands are usually stored in <strong>registers</strong>, which are small, fast storage locations inside the CPU.</p>

<p>Assembly language gives us direct access to CPU registers. In this post, we’ll explore how to store values in these registers using the fundamental <code class="language-plaintext highlighter-rouge">mov</code> instruction.</p>

<blockquote>
  <p>You can find the source code used in this tutorial on <a href="https://github.com/seojuncha/arm-assembly-tutorial/tree/main/02_moving-data-around">GitHub</a>!</p>
</blockquote>

<h2 id="using-mov-to-store-a-value-in-a-register">Using mov to Store a Value in a Register</h2>

<h3 id="storing-immediate-values">Storing Immediate Values</h3>

<p><strong>store-imm-to-reg.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm"><table class="rouge-table"><tbody><tr><td class="gutter gl"><pre class="lineno">1
2
3
4
5
</pre></td><td class="code"><pre>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>
</span>  <span class="nb">b</span> <span class="mf">.</span>

</pre></td></tr></tbody></table></code></pre></figure>

<p><code class="language-plaintext highlighter-rouge">mov r0, #2</code>: stores the number 3 in register R0.</p>

<p>The <code class="language-plaintext highlighter-rouge">mov</code> instruction follows a simple syntax:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mov destination_register, value
</code></pre></div></div>
<p>When a number is prefixed with <code class="language-plaintext highlighter-rouge">#</code> (like <code class="language-plaintext highlighter-rouge">#2</code>), it is called an <strong><em>immediate value</em></strong>.
You can also use hexadecimal notation: e.g., <code class="language-plaintext highlighter-rouge">#0x2</code>.</p>

<blockquote>
  <p>For compiling, running on QEMU, and setting up GDB, refer to <a href="hello-arm-assembly">this post</a>.</p>
</blockquote>

<h3 id="compile">Compile</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
  <span class="nt">-nostdlib</span> <span class="se">\</span>
  <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
  store-imm-to-reg.s <span class="se">\</span>
  <span class="nt">-o</span> store-imm-to-reg.elf
</code></pre></div></div>

<h3 id="run-on-qemu--debug-with-gdb">Run on QEMU &amp; Debug with GDB</h3>
<h4 id="start-qemu">Start QEMU</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
  <span class="nt">-machine</span> versatilepb <span class="se">\</span>
  <span class="nt">-nographic</span> <span class="se">\</span>
  <span class="nt">-S</span> <span class="nt">-s</span> <span class="se">\</span>
  <span class="nt">-kernel</span> store-imm-to-reg.elf
</code></pre></div></div>
<p>Here’s a quick explanation of each option:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Option</th>
      <th style="text-align: left">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-machine versatilepb</code></td>
      <td style="text-align: left">Use the VersatilePB ARM development board</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-nographic</code></td>
      <td style="text-align: left">Disable GUI; run in console</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-S</code></td>
      <td style="text-align: left">Halt the CPU until GDB connects</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-s</code></td>
      <td style="text-align: left">Start GDB server on port 1234</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">-kernel</code></td>
      <td style="text-align: left">Load the specified ELF file</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>QEMU may seem unresponsive at first — this is expected. It’s waiting for GDB to connect.</p>
</blockquote>

<h4 id="connect-with-gdb">Connect with GDB</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gdb-multiarch store-imm-to-reg.elf
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/img/run-gdb.png" alt="Launching GDB with the ELF binary" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Launching GDB with the compiled ELF binary</figcaption>
</figure>

<p>Inside GDB, connect to QEMU’s GDB server:</p>
<pre><code class="language-gdb">(gdb) target retmote localhost:1234
</code></pre>

<figure style="text-align: center;">
  <img src="/assets/img/connect-gdb-server.png" alt="Connecting GDB to QEMU's GDB server on port 1234" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Connecting GDB to QEMU’s GDB server at port 1234</figcaption>
</figure>

<p>Once connected, we can inspect whether our program is properly loaded into memory.
To check the contents at a specific memory address, GDB uses the following command format:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x/{N}{format} {address}
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">{N}</code>: number of units to display</li>
  <li><code class="language-plaintext highlighter-rouge">{format}</code>: output type (e.g., <code class="language-plaintext highlighter-rouge">i</code> for instructions, <code class="language-plaintext highlighter-rouge">x</code> for hexadecimal, <code class="language-plaintext highlighter-rouge">s</code> for string, <code class="language-plaintext highlighter-rouge">u</code> for unsigned decimal)</li>
  <li><code class="language-plaintext highlighter-rouge">{address}</code>: memory address to inspect</li>
</ul>

<p>For example, to disassemble 10 instructions starting at address <code class="language-plaintext highlighter-rouge">0x10000</code>, use:</p>
<pre><code class="language-gdb">(gdb) x/10i 0x10000
</code></pre>
<p>This helps you verify that the machine code was loaded properly and matches what you wrote in your assembly source file.</p>

<figure style="text-align: center;">
  <img src="/assets/img/show-10-instructions.png" alt="Disassembling 10 instructions starting at address 0x10000" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Disassembling 10 instructions from address 0x10000</figcaption>
</figure>

<p>To check the current register values:</p>

<pre><code class="language-gdb">(gdb) info registers   # or shorthand: i r 
(gdb) i r pc r0        # check just PC and R0
</code></pre>
<figure style="text-align: center;">
  <img src="/assets/img/print-pc-and-r0.png" alt="Printing the values of PC and R0 registers in GDB" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Checking the values of PC and R0 in GDB</figcaption>
</figure>

<p>At this point, PC (program counter) should be pointing at <code class="language-plaintext highlighter-rouge">0x10000</code>, which is our <code class="language-plaintext highlighter-rouge">mov r0, #3</code> instruction.</p>

<h4 id="execute-step-by-step">Execute Step-by-Step</h4>
<p>To execute a single instruction:</p>
<pre><code class="language-gdb">(gdb) stepi   # or shorthand: si
</code></pre>
<p>Then check the register values again:</p>
<pre><code class="language-gdb">(gdb) i r pc r0
</code></pre>
<figure style="text-align: center;">
  <img src="/assets/img/next-step-pc-and-r0.png" alt="Stepping through one instruction and inspecting updated PC and R0" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">PC moves forward, and R0 is now updated after executing `mov`</figcaption>
</figure>

<p>You’ll see:</p>
<ul>
  <li>PC has moved from <code class="language-plaintext highlighter-rouge">0x10000</code> to <code class="language-plaintext highlighter-rouge">0x10004</code></li>
  <li>R0 now contains <code class="language-plaintext highlighter-rouge">3</code></li>
</ul>

<p>Run <code class="language-plaintext highlighter-rouge">stepi</code> one more time to execute <code class="language-plaintext highlighter-rouge">b .</code>, which creates an infinite loop by jumping to the current PC.</p>

<figure style="text-align: center;">
  <img src="/assets/img/next-step-to-branch.png" alt="PC value remains the same after executing a self-branching instruction" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">PC value stays the same after `b .` (infinite loop)</figcaption>
</figure>

<h3 id="copying-between-registers">Copying Between Registers</h3>
<p>Let’s now copy a value from one register to another.</p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm"><table class="rouge-table"><tbody><tr><td class="gutter gl"><pre class="lineno">1
2
3
4
5
6
</pre></td><td class="code"><pre>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span>
</span>  <span class="nb">b</span> <span class="mf">.</span>

</pre></td></tr></tbody></table></code></pre></figure>

<p>Here, <code class="language-plaintext highlighter-rouge">mov r1, r0</code> copies the value of R0 into R1.</p>

<blockquote>
  <p>You can repeat the GDB steps from above to verify the register contents after each instruction.</p>
</blockquote>

<h2 id="whats-the-difference-between-mov-and-mvn">What’s the Difference Between mov and mvn?</h2>
<p>The <code class="language-plaintext highlighter-rouge">mvn</code> instruction is similar to <code class="language-plaintext highlighter-rouge">mov</code>, but it stores the bitwise NOT (<a href="https://en.wikipedia.org/wiki/Ones%27_complement">1’s complement</a>) of the value.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">mvn</th>
      <th style="text-align: left">mov</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">mvn r0, #0</td>
      <td style="text-align: left">mov r0, #-1</td>
    </tr>
    <tr>
      <td style="text-align: left">mvn r0, #-1</td>
      <td style="text-align: left">mov r0, #0</td>
    </tr>
    <tr>
      <td style="text-align: left">mvn r0, #0xF</td>
      <td style="text-align: left">mov r0, #-16</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Pro tip: Use Python REPL to test bitwise negation:</p>
  <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="o">~</span><span class="mh">0xf</span>
<span class="o">-</span><span class="mi">16</span>
</code></pre></div>  </div>
</blockquote>

<h2 id="coming-up-next-shift-operands">Coming Up Next: Shift Operands</h2>
<p>Both <code class="language-plaintext highlighter-rouge">mov</code> and <code class="language-plaintext highlighter-rouge">mvn</code> belong to the <strong>Data Processing Instructions</strong> category in the ARM architecture.</p>

<p>These instructions can use a <strong>shifted operand</strong> as input, which allows you to shift bits while copying or computing values.</p>

<p>This concept applies to many arithmetic operations (like <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code>, etc.), and we’ll cover it in the next post.</p>

<figure style="text-align: center;">
  <img src="/assets/img/shifter-operand-in-arm-manual.png" alt="Excerpt from ARM manual showing shifted operand encoding" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">ARM manual: encoding format for shifted operand</figcaption>
</figure>

<h2 id="summary">Summary</h2>
<p>In this post, we explored how to:</p>
<ul>
  <li>Use <code class="language-plaintext highlighter-rouge">mov</code> to assign immediate values to registers</li>
  <li>Copy values between registers</li>
  <li>Understand the difference between <code class="language-plaintext highlighter-rouge">mov</code> and <code class="language-plaintext highlighter-rouge">mvn</code></li>
  <li>Use QEMU and GDB to debug and inspect registers</li>
</ul>

<p>We confirmed our code works in a simulated ARM environment using QEMU and GDB — a key part of your low-level dev workflow.</p>

<p>Don’t worry if GDB feels unfamiliar right now. We’ll continue using this setup in future posts, so it will become second nature with practice.</p>

<p>Up next: we’ll dive into shift operands and how they enhance the expressiveness of ARM instructions.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="ARM" /><category term="Assembly" /><category term="Tutorial" /><category term="assembly" /><category term="embedded" /><category term="low-level" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[Computers are machines designed to perform calculations. These calculations are ultimately performed by the CPU, and a typical operation requires two things: operands and an operator. Operands are usually stored in registers, which are small, fast storage locations inside the CPU.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly - Build Environment and First Code</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/03/hello-arm-assembly.html" rel="alternate" type="text/html" title="ARM Assembly - Build Environment and First Code" /><published>2025-09-03T14:24:00+00:00</published><updated>2025-09-03T14:24:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/03/hello-arm-assembly</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/03/hello-arm-assembly.html"><![CDATA[<p>In this post, I will show you how to write and run your first ARM assembly code. 
This is the foundation for the rest of this tutorial series. 
We will write a very simple assembly program, compile it, run it in QEMU, and prepare for debugging with GDB.</p>

<h2 id="why-learn-assembly">Why Learn Assembly?</h2>

<p>I started learning assembly while building a small <a href="https://github.com/seojuncha/ftt-c-compiler">C compiler</a>. I had trouble implementing the <strong>code generation</strong> phase.<br />
The main problem was:</p>
<blockquote>
  <p><em>“How do I convert C code into proper assembly?”</em></p>
</blockquote>

<figure style="text-align: center;">
  <img src="/assets/img/ast-output-of-my-compiler.png" alt="AST output of my compiler" width="70%" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">I made the AST… but how do I turn this into assembly?</figcaption>
</figure>

<p>Later, I realized that knowing assembly helps with:</p>
<ul>
  <li>writing more optimized code</li>
  <li>debugging low-level issues</li>
  <li>reverse engineering</li>
</ul>

<blockquote>
  <p>People say: “If you know assembly, all software is open-source.”<br />
(But of course, you also need to understand the <strong>ISA</strong> — Instruction Set Architecture.)</p>
</blockquote>

<h2 id="a-minimal-assembly-program">A Minimal Assembly Program</h2>
<p>Let’s start with the simplest possible ARM assembly code.</p>

<p><strong>only-main.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<p>This code does nothing useful — but it’s a great starting point.</p>

<p>It creates <strong>an infinite loop with just one instruction</strong>.
We’ll use this as a template for later exercises.</p>

<blockquote>
  <p>In C or Python, we start with “Hello, World!”<br />
But in assembly, even that is too complex for day one.</p>
</blockquote>

<h2 id="development-environment">Development Environment</h2>
<p>I use <strong>Debian Linux</strong> for this tutorial.
It will also work on <strong>Ubuntu</strong> and other Linux systems.</p>

<p>We need <strong>3 tools</strong>:</p>
<ul>
  <li><strong>arm-none-eabi-gcc</strong> (cross-compiler for ARM)</li>
  <li><strong>qemu-system-arm</strong> (virtual ARM board)</li>
  <li><strong>gdb-multiarch</strong> (debugger that supports ARM)</li>
</ul>

<h3 id="1-install-arm-gcc">1. Install ARM GCC</h3>
<p>Most desktop PCs are x86, not ARM.
So we can’t use the default GCC to build ARM binaries.</p>

<p>We need a cross-compiler for ARM.
Download it from the <a href="https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads">official ARM toolchain page</a>.</p>

<p>Choose:</p>
<ul>
  <li>OS: Linux (x86_64)</li>
  <li>Target: bare-metal</li>
  <li>Format: arm-none-eabi-gcc.tar.gz</li>
</ul>

<figure style="text-align: center;">
  <img src="/assets/img/arm-toolchain-download-page.png" alt="ARM toolchain download page" style="display: block; margin: auto;" />
</figure>

<p>If you use terminal only:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>wget https://developer.arm.com/path/to/your/toolchain.tar.gz
</code></pre></div></div>
<p>Extract the archive and add the bin folder to your PATH.</p>

<p>Check the version:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="nt">--version</span>
</code></pre></div></div>

<h3 id="2-install-qemu">2. Install QEMU</h3>
<p><strong><em>QEMU</em></strong> is a powerful virtual machine for many architectures.
We’ll use it to emulate ARM hardware.</p>

<p>Install it using <strong><em>apt</em></strong>:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>qemu-system qemu-user-static
</code></pre></div></div>
<p>You can also find it on the <a href="https://www.qemu.org/download/">official QEMU website</a>.</p>

<h3 id="3-install-gdb-multiarch">3. Install GDB (Multiarch)</h3>
<p>We also need a debugger for ARM.
Install <strong><em>gdb-multiarch</em></strong>:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>gdb-multiarch
</code></pre></div></div>
<p>You’ll use this later to connect to QEMU and inspect memory or registers.</p>

<h2 id="compile-the-assembly-code">Compile the Assembly Code</h2>
<p>Now, let’s compile our <code class="language-plaintext highlighter-rouge">.s</code> file to a binary (ELF format):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
	<span class="nt">-O0</span> <span class="se">\</span>
	<span class="nt">-nostdlib</span> <span class="se">\</span>
	<span class="nt">-march</span><span class="o">=</span>armv4 <span class="se">\</span>
	<span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
	only-main.s <span class="se">\</span>
	<span class="nt">-o</span> only-main.elf
</code></pre></div></div>

<p>Options explained:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">-O0</code>: no optimization</li>
  <li><code class="language-plaintext highlighter-rouge">-nostdlib</code>: no standard C library</li>
  <li><code class="language-plaintext highlighter-rouge">-march=armv4</code>: target architecture</li>
  <li><code class="language-plaintext highlighter-rouge">-Ttext=0x10000</code>: start .text at address 0x10000</li>
</ul>

<p>Check the output:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>file only-main.elf
only-main.elf: ELF 32-bit LSB executable, ARM, EABI5 version 1 <span class="o">(</span>SYSV<span class="o">)</span>, statically linked, not stripped
</code></pre></div></div>

<h2 id="code-structure-explained">Code Structure Explained</h2>
<p>Let’s understand the 4 lines of our code.</p>

<h3 id="text--code-section">.text → Code section</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">.</span><span class="nl">text</span>
</code></pre></div></div>
<p>This means <strong><em>“put the following instructions in the code section.”</em></strong>
This is where the CPU fetches instructions.</p>

<blockquote>
  <p>We’ll learn more about memory layout (<code class="language-plaintext highlighter-rouge">.text</code>, <code class="language-plaintext highlighter-rouge">.data</code>, <code class="language-plaintext highlighter-rouge">.bss</code>) later.</p>
</blockquote>

<figure style="text-align: center;">
  <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/5/50/Program_memory_layout.pdf/page1-234px-Program_memory_layout.pdf.jpg" alt="Program memory layout" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Source: <a href="https://en.wikipedia.org/wiki/Data_segment#Program_memory">Wikipedia</a></figcaption>
</figure>

<h3 id="global-_start--make-the-label-visible">.global _start → Make the label visible</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">.</span><span class="nl">global</span> <span class="nf">_start</span>
</code></pre></div></div>
<p>This tells the linker:
<strong><em>”_start is the entry point of the program.”</em></strong></p>

<p>In C, we use <code class="language-plaintext highlighter-rouge">main()</code> — in assembly, we use <code class="language-plaintext highlighter-rouge">_start</code>.</p>

<h3 id="_start--label-definition">_start: → Label definition</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">_start</span><span class="err">:</span>
</code></pre></div></div>
<p>This defines a <strong>label</strong>.
It marks a location in the code so other instructions can jump to it.</p>

<h3 id="b---infinite-loop">b . → Infinite loop</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">b</span> <span class="err">.</span>
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">b</code> means <strong><em>branch</em></strong> (jump).<br />
.(dot) means <strong><em>“this current line”</em></strong> — so the program jumps to itself forever.</p>

<p>This creates a simple infinite loop.</p>

<blockquote>
  <p>This is useful. It keeps the program alive so we can connect GDB and inspect the system.</p>
</blockquote>

<h2 id="summary">Summary</h2>
<p>We wrote the smallest possible ARM assembly program.
We installed the tools, compiled the code, and prepared to debug it.</p>

<p>This basic template will be reused in many future posts.</p>

<p>In the next post, we will:</p>
<ul>
  <li>Add register access</li>
  <li>Connect QEMU and GDB</li>
  <li>Start real debugging</li>
</ul>

<p>Thanks for reading!</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="ARM" /><category term="Assembly" /><category term="Tutorial" /><category term="Assembly" /><category term="Embedded" /><category term="low-level programming" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[In this post, I will show you how to write and run your first ARM assembly code. This is the foundation for the rest of this tutorial series. We will write a very simple assembly program, compile it, run it in QEMU, and prepare for debugging with GDB.]]></summary></entry><entry xml:lang="en"><title type="html">What is Low-Level Programming?</title><link href="https://seojuncha.github.io/programming/low-level/2025/09/01/what-is-low-level-programming.html" rel="alternate" type="text/html" title="What is Low-Level Programming?" /><published>2025-09-01T12:49:00+00:00</published><updated>2025-09-01T12:49:00+00:00</updated><id>https://seojuncha.github.io/programming/low-level/2025/09/01/what-is-low-level-programming</id><content type="html" xml:base="https://seojuncha.github.io/programming/low-level/2025/09/01/what-is-low-level-programming.html"><![CDATA[<p>All the electronic devices we use every day—smartphones, laptops, and servers—are built on a complex combination of software and hardware.
Most people only interact with the visible parts, like websites or apps, but beneath the surface lies an unseen world: <strong>low-level programming</strong>.</p>

<p>Throughout my 10+ years of programming experience, I’ve worked with many languages and environments. But I’ve always been curious about how things actually work inside. Over time, I grew uncomfortable with code that I couldn’t clearly predict or fully understand. Even C++, the language I used most in industry, felt unsatisfying—despite being relatively close to the hardware, it still left me wondering what was truly happening underneath.</p>

<blockquote>
  <p>Of course, I completely agree that scripting languages like Python are extremely useful—and I still use them all the time!</p>
</blockquote>

<p>So even though my professional career hasn’t centered on low-level programming, my personal curiosity and research have led me down this path.
In this article, I’ll explore what low-level programming is, why it matters, and why we should care about it.</p>

<h2 id="what-is-low-level-programming">What Is Low-Level Programming?</h2>

<p>Low-level programming means working <strong>as close to the hardware as possible</strong>:</p>

<ul>
  <li>Directly manipulating CPU registers</li>
  <li>Accessing specific memory addresses</li>
  <li>Designing software based on hardware principles</li>
</ul>

<p>High-level languages like Python, Java, and JavaScript abstract away these details so developers don’t need to think about hardware.
By contrast, languages like C and Assembly map almost directly to hardware operations, which is why they’re considered <strong>low-level languages</strong>.</p>

<p>Personally, I see C more as a high-level language than a low-level one. With C, you usually don’t have to rewrite code just because the hardware changes—thanks to its portability. Assembly, on the other hand, ties you directly to the CPU and memory architecture. That’s also where the compiler plays its role: bridging the gap between high-level abstractions and low-level execution.</p>

<h2 id="key-characteristics-of-low-level-programming">Key Characteristics of Low-Level Programming</h2>

<ol>
  <li>Hardware Control
    <ul>
      <li>You can control I/O devices, memory, and CPU instructions directly.</li>
      <li>Example: writing a value to a specific memory address or executing an instruction that manipulates a register.</li>
    </ul>
  </li>
  <li>Fine-Grained Optimization
    <ul>
      <li>Because code is closely tied to hardware, it can be optimized for speed and memory usage.</li>
      <li>Essential for embedded systems, operating systems, and real-time control systems.</li>
    </ul>
  </li>
  <li>Complexity and Difficulty
    <ul>
      <li>With less abstraction, the learning curve is steeper.</li>
      <li>But it also provides a deep understanding of how computers fundamentally work.</li>
    </ul>
  </li>
</ol>

<h2 id="why-does-it-matter">Why Does It Matter?</h2>

<p>Low-level programming isn’t just an “old-school” technique—it still plays a crucial role in many fields today:</p>

<ul>
  <li><strong>Operating Systems &amp; Kernels:</strong> The core of Linux, Windows, and macOS is written in C and Assembly.</li>
  <li><strong>Drivers &amp; Firmware:</strong> Keyboards, mice, graphics cards, network cards—none of them work without low-level code.</li>
  <li><strong>Security &amp; Hacking:</strong> Understanding vulnerabilities requires knowledge of memory structures and Assembly.</li>
  <li><strong>Embedded Systems:</strong> Cars, IoT devices, and appliances all rely on low-level programming.</li>
</ul>

<p>In short: everything we use daily is powered by low-level code beneath the surface.</p>

<p><img src="/assets/img/github-linux-kernel-lanauge.png" alt="github linux kernel language" /></p>

<h2 id="simple-examples">Simple Examples</h2>

<ul>
  <li>In ARM Assembly, the instruction <code class="language-plaintext highlighter-rouge">add r0, r1, r2</code> isn’t just “addition.” 
It represents the CPU’s Arithmetic Logic Unit (ALU) receiving register values, performing the operation, and storing the result.</li>
  <li>In Verilog, the line <code class="language-plaintext highlighter-rouge">assign y = a &amp; b;</code> corresponds to actual electrical signals flowing through a semiconductor circuit.</li>
  <li>In C, the line <code class="language-plaintext highlighter-rouge">printf("Hello");</code> eventually invokes a system call, where the kernel manages the hardware I/O operation.</li>
</ul>

<p>Every single line of code hides countless low-level operations.
Low-level programming isn’t just about learning difficult skills—it’s about developing a mindset of <strong>understanding computers at their core</strong>.</p>

<h2 id="closing-thoughts">Closing Thoughts</h2>

<p>Low-level programming may not always be visible, but it forms the foundation of all modern technology.
My guiding framework will be George Hotz’s <a href="https://github.com/seojuncha/fromthetransistor">From the Transistor project</a>, 
which demonstrates how to directly build and observe low-level computer systems.</p>

<p>In future posts, I’ll dive into topics like:</p>
<ul>
  <li><strong>Binary Representation</strong></li>
  <li><strong>ARM Assembly</strong></li>
  <li><strong>Verilog</strong></li>
  <li><strong>Compiler and OS Development</strong></li>
</ul>

<p>Low-level programming is not just about writing code—it’s about exploring the essence of how computers work.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="programming" /><category term="low-level" /><category term="low-level" /><category term="assembly" /><category term="C" /><category term="ARM" /><category term="Verilog" /><category term="compiler" /><category term="operating-system" /><summary type="html"><![CDATA[All the electronic devices we use every day—smartphones, laptops, and servers—are built on a complex combination of software and hardware. Most people only interact with the visible parts, like websites or apps, but beneath the surface lies an unseen world: low-level programming.]]></summary></entry></feed>
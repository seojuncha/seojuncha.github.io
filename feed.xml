<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://seojuncha.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://seojuncha.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2025-11-06T10:36:38+00:00</updated><id>https://seojuncha.github.io/feed.xml</id><title type="html">Hacking the System, One Bit at a Time</title><subtitle>Cha Seojun&apos;s journey into the heart of computers—from transistor to terminal. Writing compilers, emulating hardware, and uncovering how machines truly work. A blog for hackers, builders, and curious minds.</subtitle><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><entry xml:lang="en"><title type="html">ARM Assembly #9 - Memory Access with Automatic Address Calculation using pre-index and post-index</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index.html" rel="alternate" type="text/html" title="ARM Assembly #9 - Memory Access with Automatic Address Calculation using pre-index and post-index" /><published>2025-11-05T13:35:00+00:00</published><updated>2025-11-05T13:35:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/05/arm-assembly-pre-post-index.html"><![CDATA[<p>When accessing memory in ARM assembly, simply using <code class="language-plaintext highlighter-rouge">[R1]</code> as an address is often not enough. 
When dealing with arrays or buffers stored in consecutive memory, repeatedly updating the base register using <code class="language-plaintext highlighter-rouge">ADD</code> can be inefficient.</p>

<p>To solve this, ARM provides an <strong>automatic address calculation</strong> mechanism through <strong>pre-index</strong> and <strong>post-index</strong> modes. 
In this post, we’ll clearly explain how these modes work, how they differ, 
and explore how the CPU handles address calculation internally with real assembly examples.</p>

<h2 id="why-do-we-need-pre-index-and-post-index">Why do we need pre-index and post-index?</h2>
<p>In <a href="/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html">the previous post</a>, we discussed the basic offset addressing mode using <code class="language-plaintext highlighter-rouge">[Rn, #offset]</code>.
In that form, the CPU simply calculates an address by adding a fixed offset to the base register <code class="language-plaintext highlighter-rouge">Rn</code> and accesses memory at that address.
However, this calculation happens each time the instruction runs,
and the base register <code class="language-plaintext highlighter-rouge">Rn</code> itself does <strong>not</strong> get updated automatically.</p>

<p>In this post, we’ll take it one step further and explore automatic address calculation, <strong>the pre-index and post-index addressing modes</strong>.
These modes allow the CPU to automatically update <code class="language-plaintext highlighter-rouge">Rn</code> during execution,
improving both performance and code compactness when working with repetitive memory accesses.</p>

<p>When accessing memory, the CPU first evaluates the expression inside the brackets to obtain the actual address. 
For example, <code class="language-plaintext highlighter-rouge">LDR R0, [R1]</code> loads the value stored at the address in <code class="language-plaintext highlighter-rouge">R1</code>. 
However, when reading consecutive data such as arrays, an additional <code class="language-plaintext highlighter-rouge">ADD R1, R1, #4</code> instruction is required each time. 
This increases instruction count and reduces performance.</p>

<p>To address this, ARM integrates automatic address calculation into <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code> instructions, 
allowing the CPU to perform the address update itself, improving both performance and pipeline efficiency.</p>

<figure style="text-align: center;">
  <img src="/assets/img/pre-post-offset.png" alt="[Offset vs Pre vs Post] Addressing modes overview" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">offset vs pre-index vs post-index — manual vs automatic address update</figcaption>
</figure>

<h2 id="what-is-automatic-address-calculation">What is automatic address calculation?</h2>
<p>Automatic address calculation is a feature where the CPU automatically increments or decrements the memory address<br />
during the execution of a load or store instruction.<br />
There are two main modes for this: <strong>pre-indexing</strong> and <strong>post-indexing</strong>.</p>

<h3 id="the-pre-index-mode">The pre-index mode</h3>
<p>The <strong>pre-index</strong> mode means that the CPU updates the base register <strong><em>before accessing memory</em></strong>.
In the form <code class="language-plaintext highlighter-rouge">[Rn, #offset]!</code>, the CPU calculates <code class="language-plaintext highlighter-rouge">Rn + offset</code>, writes it back to <code class="language-plaintext highlighter-rouge">Rn</code>, and then uses that address to access memory.</p>

<p>For example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]!</span>   <span class="no">@</span> <span class="nv">add</span> <span class="mi">4</span> <span class="nv">to</span> <span class="nv">R1</span><span class="o">,</span> <span class="nv">then</span> <span class="nv">load</span> <span class="nv">from</span> <span class="nv">the</span> <span class="nv">new</span> <span class="nv">address</span>
</code></pre></div></div>

<p>Execution steps:</p>
<ol>
  <li>R1 ← R1 + 4</li>
  <li>R0 ← Mem[R1]</li>
</ol>

<h3 id="the-post-index-mode">The post-index mode</h3>
<p>The <strong>post-index</strong> mode performs the memory access <strong><em>before updating the base register</em></strong>.
In <code class="language-plaintext highlighter-rouge">[Rn], #offset</code>, the CPU first accesses memory using <code class="language-plaintext highlighter-rouge">Rn</code>, then adds the offset afterward.</p>

<p>For example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>    <span class="no">@</span> <span class="nv">load</span> <span class="nv">from</span> <span class="nv">R1</span><span class="o">,</span> <span class="nv">then</span> <span class="nv">increment</span> <span class="nv">R1</span> <span class="nv">by</span> <span class="mi">4</span>
</code></pre></div></div>

<p>Execution steps:</p>
<ol>
  <li>R0 ← Mem[R1]</li>
  <li>R1 ← R1 + 4</li>
</ol>

<h2 id="comparing-memory-access-and-address-update-timing">Comparing memory access and address update timing</h2>
<p>Although both modes ultimately change Rn by adding the same offset,
their behavior differs depending on when the update occurs.</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]!</span>   <span class="no">@</span> <span class="nv">pre</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">update</span> <span class="nv">R1</span> <span class="nv">before</span> <span class="nv">accessing</span> <span class="nv">memory</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>    <span class="no">@</span> <span class="nv">post</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">access</span> <span class="nv">memory</span> <span class="nv">first</span><span class="o">,</span> <span class="nv">then</span> <span class="nv">update</span> <span class="nv">R1</span>
</code></pre></div></div>
<ul>
  <li>In <strong>pre-index</strong>, <code class="language-plaintext highlighter-rouge">R1</code> is updated before memory access.</li>
  <li>In <strong>post-index</strong>, <code class="language-plaintext highlighter-rouge">R1</code> is updated after memory access.</li>
</ul>

<p>Thus, pre-indexing is used when you want to access the <strong>next</strong> element first,
while post-indexing is useful when you need to access the <strong>current</strong> element before moving on.</p>

<figure style="text-align: center;">
  <img src="/assets/img/timing-pre-post.png" alt="Timing: pre-index (update→access) vs post-index (access→update)" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">pre-index updates then accesses; post-index accesses then updates</figcaption>
</figure>

<h2 id="example-test-using-gcc-qemu-and-gdb">Example test using GCC, QEMU, and GDB</h2>
<p>Here’s a minimal example comparing both modes.
You can build and debug it using GCC, QEMU, and GDB as follows.</p>

<p><strong>pre-post.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">=</span><span class="nv">array</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]!</span>   <span class="no">@</span> <span class="nv">pre</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">R1</span> <span class="err">←</span> <span class="nv">R1</span><span class="o">+</span><span class="mi">4</span> <span class="nv">then</span><span class="o">,</span> <span class="nv">load</span>
  <span class="nb">ldr</span> <span class="nv">r2</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">],</span> <span class="o">#</span><span class="mi">4</span>    <span class="no">@</span> <span class="nv">post</span><span class="o">-</span><span class="nv">index</span><span class="err">:</span> <span class="nv">R2</span> <span class="err">←</span> <span class="nv">Mem</span><span class="o">[</span><span class="nv">R1</span><span class="o">],</span> <span class="nv">then</span> <span class="nv">R1</span> <span class="err">←</span> <span class="nv">R1</span><span class="o">+</span><span class="mi">4</span>
  <span class="nb">b</span> <span class="mf">.</span>
<span class="nl">array</span><span class="err">:</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x11111111</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x22222222</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x33333333</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mh">0x44444444</span>
</code></pre></div></div>

<p><strong>Build &amp; Run QEMU</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
    <span class="nt">-nostdlib</span> <span class="se">\</span>
    <span class="nt">-march</span><span class="o">=</span>armv4 <span class="se">\</span>
    <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
    pre-post.s <span class="se">\</span>
    <span class="nt">-o</span> pre-post.elf
<span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
    <span class="nt">-machine</span> versatilepb <span class="se">\</span>
    <span class="nt">-nographic</span> <span class="se">\</span>
    <span class="nt">-S</span> <span class="nt">-s</span> <span class="se">\</span>
    <span class="nt">-kernel</span> pre-post.elf
</code></pre></div></div>
<p><strong>Debug with GDB</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gdb-multiarch pre-post.elf

<span class="o">(</span>gdb<span class="o">)</span> target remote :1234    <span class="c"># Connect to GDB server</span>
<span class="o">(</span>gdb<span class="o">)</span> i r r0 r1 r2           <span class="c"># Show registers, R0, R1, R2</span>
<span class="o">(</span>gdb<span class="o">)</span> x/4w 0x10010           <span class="c"># Print 4 words at 0x10010(address of array)</span>
</code></pre></div></div>

<figure style="text-align: center;">
  <img src="/assets/gif/prepost.gif" alt="GDB pre/post step execution demo" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;"><code>R1</code> value changes during GDB step execution — pre-index updates first, post-index after.</figcaption>
</figure>

<p>After running step-by-step in GDB,
you’ll see that <code class="language-plaintext highlighter-rouge">R1</code> changes to <code class="language-plaintext highlighter-rouge">array+4</code> after the pre-index instruction,
and to <code class="language-plaintext highlighter-rouge">array+8</code> after the post-index instruction.</p>

<h2 id="hardware-level-perspective">Hardware-level perspective</h2>

<p>Inside the CPU, each instruction passes through multiple pipeline stages.
Typically, <strong>address calculation happens in the Execute stage</strong> via the ALU,
and <strong>memory access occurs in the Memory stage</strong>.</p>

<ul>
  <li>In <strong>pre-index</strong>, the address is calculated first (Execute) and then used for access (Memory).</li>
  <li>In <strong>post-index</strong>, memory access happens first, then the base register is updated (Write Back).</li>
</ul>

<p>This hardware design allows the CPU to <strong>update addresses automatically</strong> without additional ADD instructions,
reducing instruction count and improving pipeline throughput for repetitive memory operations.</p>

<h2 id="conclusion">Conclusion</h2>
<p>Both pre-index and post-index modes automate address updates,
and their only difference lies in <strong>whether the update occurs before or after memory access</strong>.</p>

<p>Use <strong>pre-index</strong> when you need to move to the next element before accessing it,
and <strong>post-index</strong> when you want to access the current element first.</p>

<p>In the next post, we’ll extend this idea to <strong>stack operations</strong> and explore how <code class="language-plaintext highlighter-rouge">LDM</code>/<code class="language-plaintext highlighter-rouge">STM</code> can load or store multiple registers efficiently.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ldr" /><category term="str" /><category term="pre-index" /><category term="post-index" /><category term="addressing mode" /><summary type="html"><![CDATA[When accessing memory in ARM assembly, simply using [R1] as an address is often not enough. When dealing with arrays or buffers stored in consecutive memory, repeatedly updating the base register using ADD can be inefficient.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #8 - Calculating Memory Addresses with Offsets</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html" rel="alternate" type="text/html" title="ARM Assembly #8 - Calculating Memory Addresses with Offsets" /><published>2025-11-02T04:40:00+00:00</published><updated>2025-11-02T04:40:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/11/02/arm-offset-addressing</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/11/02/arm-offset-addressing.html"><![CDATA[<p>In this post, we’ll explore how <strong>offset-based addressing</strong> works in ARM assembly.<br />
You’ll learn how to calculate memory addresses efficiently <a href="/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html">using <code class="language-plaintext highlighter-rouge">LDR</code> and <code class="language-plaintext highlighter-rouge">STR</code></a> without needing separate <a href="/arm/assembly/tutorial/2025/10/27/arm-arithmetic-operations.html"><code class="language-plaintext highlighter-rouge">ADD</code> instructions</a> each time.</p>

<h2 id="why-we-need-offsets">Why We Need Offsets</h2>
<p>In assembly, a simple instruction like <code class="language-plaintext highlighter-rouge">ldr r0, [r1]</code> always accesses the same memory address.
But when working with arrays or structures <strong>stored in contiguous memory</strong>, we’d have to use extra <code class="language-plaintext highlighter-rouge">add</code> instructions each time to move to the next element.</p>

<p>For example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="nv">Read</span> <span class="nv">first</span> <span class="nv">data</span>
<span class="nl">add</span> <span class="nb">r1</span><span class="err">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span>     <span class="no">@</span> <span class="nv">Move</span> <span class="nv">to</span> <span class="nv">next</span> <span class="nv">element</span>
<span class="nl">ldr</span> <span class="nb">r2</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="nv">Read</span> <span class="nv">second</span> <span class="nv">data</span>
</code></pre></div></div>
<p>That works, but it’s repetitive and inefficient.
Instead, ARM lets us <strong>combine address calculation and memory access</strong>:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span><span class="o">]</span>   <span class="no">@</span> <span class="nv">Reads</span> <span class="nv">directly</span> <span class="nv">from</span> <span class="nv">r1</span> <span class="o">+</span> <span class="mi">4</span>
</code></pre></div></div>
<p>This reduces instruction count and CPU cycles, improving performance.</p>

<figure style="text-align: center;">
  <img src="/assets/img/arm-offset-memory.png" alt="Byte-addressed memory with int array and base+offset" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">In byte-addressed memory, an <code>int</code> array advances by 4 bytes per index.</figcaption>
</figure>

<h2 id="how-offset-address-calculation-works">How Offset Address Calculation Works</h2>
<p>An offset represents the distance from a base register address.
ARM calculates the final memory address by adding or subtracting this offset from the base:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Address = Base Register + Offset
</code></pre></div></div>

<p>Offsets can be:</p>
<ul>
  <li>Immediate – a constant value like <code class="language-plaintext highlighter-rouge">#4</code> or <code class="language-plaintext highlighter-rouge">#8</code></li>
  <li>Register – the value of another register</li>
</ul>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">8</span><span class="o">]</span>    <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">+</span> <span class="mi">8</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">]</span>    <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">+</span> <span class="nv">r2</span>
</code></pre></div></div>
<p>Both addition (+) and subtraction (–) are supported,
and immediate offsets are limited to 12-bit unsigned values (<code class="language-plaintext highlighter-rouge">0</code>–<code class="language-plaintext highlighter-rouge">4095</code>).</p>

<h2 id="three-offset-forms-immediate-register-and-shifted-register">Three Offset Forms: Immediate, Register, and Shifted Register</h2>
<p>ARM provides three main ways to express an offset:</p>

<figure style="text-align: center;">
  <img src="/assets/img/arm-offset-forms.png" alt="Three offset forms: immediate, register, shifted register" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">Offsets can be expressed as immediate, register, or shifted-register forms.</figcaption>
</figure>

<h3 id="1-immediate-offset--add-a-constant-directly">1. Immediate Offset – Add a constant directly.</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Rn, #imm]
</code></pre></div></div>
<p>Example:</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">12</span><span class="o">]</span>     <span class="no">@</span> <span class="nv">Load</span> <span class="nv">data</span> <span class="nv">from</span> <span class="o">(</span><span class="nv">r1</span> <span class="o">+</span> <span class="mi">12</span><span class="o">)</span>
</code></pre></div></div>

<h3 id="2-register-offset--add-the-value-of-another-register">2. Register Offset – Add the value of another register.</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Rn, Rm]
</code></pre></div></div>
<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">]</span>      <span class="no">@</span> <span class="nv">Load</span> <span class="nv">data</span> <span class="nv">from</span> <span class="o">(</span><span class="nv">r1</span> <span class="o">+</span> <span class="nv">r2</span><span class="o">)</span>
</code></pre></div></div>

<h3 id="3-shifted-register-offset--add-a-shifted-version-of-another-register">3. Shifted Register Offset – Add a shifted version of another register.</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Rn, Rm, Shift #n]
</code></pre></div></div>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">]</span>  <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">+</span> <span class="o">(</span><span class="nv">r2</span> <span class="o">&lt;&lt;</span> <span class="mi">2</span><span class="o">)</span>
</code></pre></div></div>

<p>Here, <code class="language-plaintext highlighter-rouge">lsl #2</code> means “×4”.
Since each int element is 4 bytes, this perfectly aligns with array indexing.</p>

<h2 id="example-code">Example Code</h2>
<p>Here’s how offset-based address calculation works in assembly:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r1</span><span class="err">,</span> <span class="o">=</span><span class="mh">0x1000</span>            <span class="no">@</span> <span class="nv">Base</span> <span class="nv">address</span> <span class="o">(</span><span class="nv">start</span> <span class="nv">of</span> <span class="nv">the</span> <span class="nv">array</span><span class="o">)</span>
<span class="nl">ldr</span> <span class="nb">r2</span><span class="err">,</span> <span class="o">=</span><span class="mi">3</span>                 <span class="no">@</span> <span class="nv">Index</span> <span class="nv">i</span> <span class="o">=</span> <span class="mi">3</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">]</span>   <span class="no">@</span> <span class="nv">address</span> <span class="o">=</span> <span class="mh">0x1000</span> <span class="o">+</span> <span class="o">(</span><span class="mi">3</span> <span class="o">&lt;&lt;</span> <span class="mi">2</span><span class="o">)</span> <span class="o">=</span> <span class="mh">0x100C</span>
                           <span class="err">@</span> <span class="nv">r0</span> <span class="o">=</span> <span class="nv">value</span> <span class="nv">of</span> <span class="nv">arr</span><span class="o">[</span><span class="mi">3</span><span class="o">]</span>
</code></pre></div></div>

<p>This is equivalent to accessing <code class="language-plaintext highlighter-rouge">arr[i]</code> in C.
The <code class="language-plaintext highlighter-rouge">lsl #2</code> part represents <code class="language-plaintext highlighter-rouge">i * 4</code>, perfectly matching a 4-byte int element.</p>

<figure style="text-align: center;">
  <img src="/assets/img/arm-offset-forms.png" alt="Scaling i by 4 using lsl #2" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;"><code>lsl #2</code> scales the index <code>i</code> by 4 to match the 4-byte size of <code>int</code></figcaption>
</figure>

<h2 id="c-to-assembly-conversion">C to Assembly Conversion</h2>
<p>In C, when accessing an array element, the compiler automatically calculates the memory address.</p>

<p>For example:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">arr</span><span class="p">[</span><span class="mi">4</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">10</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">40</span><span class="p">};</span>
<span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="n">arr</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>
</code></pre></div></div>

<p>The compiler might generate:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r1</span><span class="err">,</span> <span class="o">=</span><span class="nv">arr</span>                 <span class="no">@</span> <span class="nv">r1</span> <span class="o">=</span> <span class="mh">&amp;a</span><span class="nv">rr</span><span class="o">[</span><span class="mi">0</span><span class="o">]</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">8</span><span class="o">]</span>             <span class="no">@</span> <span class="nv">arr</span><span class="o">[</span><span class="mi">2</span><span class="o">]</span> <span class="err">→</span> <span class="nv">base</span> <span class="o">+</span> <span class="o">(</span><span class="mi">2</span> <span class="o">*</span> <span class="mi">4</span><span class="o">)</span> <span class="o">=</span> <span class="o">+</span><span class="mi">8</span>
</code></pre></div></div>

<p>If the index is stored in a variable:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">ldr</span> <span class="nb">r1</span><span class="err">,</span> <span class="o">=</span><span class="nv">arr</span>
<span class="nl">mov</span> <span class="nb">r2</span><span class="err">,</span> <span class="nv">r0</span>                   <span class="no">@</span> <span class="nv">r2</span> <span class="o">=</span> <span class="nv">i</span>
<span class="nl">ldr</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">]</span>     <span class="no">@</span> <span class="nv">arr</span><span class="o">[</span><span class="nv">i</span><span class="o">]</span> <span class="o">=</span> <span class="o">*(</span><span class="nv">base</span> <span class="o">+</span> <span class="nv">i</span><span class="o">*</span><span class="mi">4</span><span class="o">)</span>
</code></pre></div></div>

<p>In short:</p>
<ul>
  <li>Each int element is 4 bytes.</li>
  <li>Memory is byte-addressed, so the address increases by 4 for each index.</li>
  <li>ARM uses <code class="language-plaintext highlighter-rouge">lsl #2</code> to efficiently represent this <code class="language-plaintext highlighter-rouge">×4</code> scaling.</li>
</ul>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we learned how to use offsets in <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str </code> to calculate memory addresses efficiently.</p>

<p>Using offsets lets you perform both address calculation and memory access in one instruction.
This makes it easier and faster to work with arrays or structures stored in consecutive memory.</p>

<p>In the next post, we’ll explore how to use <strong>pre-index</strong> and <strong>post-index addressing</strong>
to perform automatic address updates after each memory access.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ldr" /><category term="str" /><category term="offset" /><category term="addressing" /><summary type="html"><![CDATA[In this post, we’ll explore how offset-based addressing works in ARM assembly. You’ll learn how to calculate memory addresses efficiently using LDR and STR without needing separate ADD instructions each time.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #7 - The Simplest Way to Access Memory with LDR and STR</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html" rel="alternate" type="text/html" title="ARM Assembly #7 - The Simplest Way to Access Memory with LDR and STR" /><published>2025-10-31T11:00:00+00:00</published><updated>2025-10-31T11:00:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/10/31/arm-ldr-str-basic.html"><![CDATA[<p>This post is an introductory tutorial on <strong>memory access</strong> for beginners learning ARM assembly.
Using the ARMv4 architecture as a reference, we’ll explore how the CPU uses the <code class="language-plaintext highlighter-rouge">ldr</code> (load) and <code class="language-plaintext highlighter-rouge">str</code> (store) instructions to read and write data to memory.</p>

<p>In this post, you’ll learn:</p>
<ul>
  <li>Why values must be loaded into registers before computation</li>
  <li>How <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code> are structured and what they do</li>
  <li>What the square brackets (<code class="language-plaintext highlighter-rouge">[]</code>) mean in addressing mode</li>
  <li>How to inspect registers and memory states using <strong>QEMU</strong> and <strong>GDB</strong></li>
</ul>

<blockquote>
  <p><strong>Key Takeaway:</strong> <br />
<code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code> are the foundation of all ARM programs.
Through this tutorial, you’ll clearly see how the CPU actually reads and writes data.</p>
</blockquote>

<figure>
<svg role="img" aria-labelledby="title desc" viewBox="0 0 800 240" width="100%" xmlns="http://www.w3.org/2000/svg">

  <title id="title">CPU ↔ Memory data flow with LDR/STR</title>
  <desc id="desc">Shows LDR reading from Memory to Register and STR writing from Register to Memory.</desc>
  <style>
    .fg { stroke:#E6E6E6; fill:#E6E6E6; }
    .box { fill:#0B1220; stroke:#A3B1C6; }
    .wire { stroke:#E6E6E6; }
    .muted { fill:#C8D0DB; }
    .marker path { fill:#E6E6E6; }  
    .label{font:16px sans-serif}
    .note{font:15px sans-serif}
    .mono{font:14px monospace}
  </style>
  <defs>
    <marker class="marker" id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" />
    </marker>
  </defs>

  <!-- CPU Registers -->
  <rect class="box" x="60" y="50" width="280" height="140" rx="8" />
  <text class="label fg" x="200" y="80" text-anchor="middle">CPU Registers</text>
  <!-- little register slots -->
  <g transform="translate(90,100)">
    <rect class="box" x="0" y="0" width="50" height="24" rx="4" />
    <rect class="box" x="60" y="0" width="50" height="24" rx="4" />
    <rect class="box" x="120" y="0" width="50" height="24" rx="4" />
    <rect class="box" x="180" y="0" width="50" height="24" rx="4" />
    <text class="note fg" x="25" y="17" text-anchor="middle">r0</text>
    <text class="note fg" x="85" y="17" text-anchor="middle">r1</text>
    <text class="note fg" x="145" y="17" text-anchor="middle">r2</text>
    <text class="note fg" x="205" y="17" text-anchor="middle">…</text>
  </g>

  <!-- Memory -->
  <rect class="box" x="460" y="30" width="280" height="180" rx="8" />
  <text class="label fg" x="600" y="60" text-anchor="middle">Memory</text>
  <g transform="translate(490,80)">
    <rect class="box" x="0" y="0" width="220" height="28" />
    <rect class="box" x="0" y="36" width="220" height="28" />
    <rect class="box" x="0" y="72" width="220" height="28" />
    <text class="note fg" x="8" y="19">0x1000</text>
    <text class="note fg" x="8" y="55">0x1004</text>
    <text class="note fg" x="8" y="91">0x1008</text>
  </g>

  <!-- Arrows -->
  <line class="wire" x1="340" y1="120" x2="460" y2="120" stroke="#000" stroke-width="2" marker-end="url(#arrow)" />
  <text class="label fg" x="400" y="110" text-anchor="middle">STR</text>
  <line class="wire" x1="460" y1="160" x2="340" y2="160" stroke="#000" stroke-width="2" marker-end="url(#arrow)" />
  <text class="label fg" x="400" y="150" text-anchor="middle">LDR</text>
</svg>
<figcaption>
LDR moves data Memory → Register; STR moves data Register → Memory.
</figcaption>
</figure>

<h2 id="why-we-need-to-access-memory">Why We Need to Access Memory</h2>

<p>The ARM CPU’s data-processing instructions (<code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, etc.) <strong>never access memory directly</strong> — they operate only on values stored in registers.
Therefore, any value stored in memory <strong>must be loaded into a register before performing an operation</strong>.</p>

<p>ARMv4 provides a total of <strong>16</strong> general-purpose registers (<code class="language-plaintext highlighter-rouge">r0</code>–<code class="language-plaintext highlighter-rouge">r15</code>).
It’s impossible to store all program data in these registers alone.
Thus, the CPU uses registers as temporary storage for computation, while the main data resides in memory.</p>

<figure>
<svg role="img" aria-labelledby="title desc" viewBox="0 0 800 260" width="100%" xmlns="http://www.w3.org/2000/svg">

  <title id="title">Registers vs Memory roles</title>
  <desc id="desc">Shows 16 general-purpose registers contrasted with large memory as main storage.</desc>
  <style>
    .fg { stroke:#E6E6E6; fill:#E6E6E6; }
    .box { fill:#0B1220; stroke:#A3B1C6; }
    .wire { stroke:#E6E6E6; }
    .muted { fill:#C8D0DB; }
    .marker path { fill:#E6E6E6; }  
    .label{font:16px sans-serif}
    .note{font:13px sans-serif}
    .mono{font:14px monospace}
  </style>

  <!-- Registers grid -->
  <rect class="box" x="60" y="30" width="300" height="200" rx="8" />
  <text class="label fg" x="210" y="55" text-anchor="middle">Registers (16)</text>
  <g transform="translate(80,75)">
    <!-- 4 x 4 grid -->
    <g class="row" transform="translate(0,0)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r0</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r1</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r2</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r3</text>
    </g>
    <g class="row" transform="translate(0,40)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r4</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r5</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r6</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r7</text>
    </g>
    <g class="row" transform="translate(0,80)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r8</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r9</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r10</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r11</text>
    </g>
    <g class="row" transform="translate(0,120)">
      <rect class="box" x="0" y="0" width="60" height="30" rx="4" /><text class="note fg" x="30" y="20" text-anchor="middle">r12</text>
      <rect class="box" x="70" y="0" width="60" height="30" rx="4" /><text class="note fg" x="100" y="20" text-anchor="middle">r13</text>
      <rect class="box" x="140" y="0" width="60" height="30" rx="4" /><text class="note fg" x="170" y="20" text-anchor="middle">r14</text>
      <rect class="box" x="210" y="0" width="60" height="30" rx="4" /><text class="note fg" x="240" y="20" text-anchor="middle">r15</text>
    </g>
  </g>

  <!-- Memory -->
  <rect class="box" x="440" y="30" width="300" height="200" rx="8" />
  <text class="label fg" x="590" y="55" text-anchor="middle">Main Memory (Data store)</text>
  <text class="note fg" x="590" y="90" text-anchor="middle">Large, holds program data</text>
  <text class="note fg" x="590" y="110" text-anchor="middle">Registers used for compute</text>
</svg>
<figcaption>
Registers are temporary for compute; memory is the main data store.
</figcaption>
</figure>

<h2 id="assembly-instructions-for-memory-access">Assembly Instructions for Memory Access</h2>

<p>There are two basic instructions for accessing memory:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">ldr</code>: loads data <strong>from memory into a register</strong>.</li>
  <li><code class="language-plaintext highlighter-rouge">str</code>: stores data <strong>from a register into memory</strong>.</li>
</ul>

<p>Their basic syntax is:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ldr Rd, [Rn]
str Rd, [Rn]
</code></pre></div></div>
<p>Where:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">Rd</code>: <strong><em>destination register</em></strong> to store or retrieve data.</li>
  <li><code class="language-plaintext highlighter-rouge">Rn</code>: <strong><em>base register</em></strong> containing the memory address.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">ldr</code> reads the value stored at memory address <code class="language-plaintext highlighter-rouge">Rn</code> into register <code class="language-plaintext highlighter-rouge">Rd</code>,
and <code class="language-plaintext highlighter-rouge">str</code> writes the value in <code class="language-plaintext highlighter-rouge">Rd</code> to the memory location pointed to by <code class="language-plaintext highlighter-rouge">Rn</code>.</p>

<h3 id="meaning-of-brackets-and-base-register">Meaning of Brackets and Base Register</h3>

<p>The value inside square brackets (<code class="language-plaintext highlighter-rouge">[]</code>) represents a memory address,
and the expression <code class="language-plaintext highlighter-rouge">[address]</code> refers to the value stored at that address.</p>

<p>For example:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ldr r0, [r1]
</code></pre></div></div>

<p>This means <em>“read the value stored at the <strong>memory address pointed to by</strong> <code class="language-plaintext highlighter-rouge">r1</code>, and put it into <code class="language-plaintext highlighter-rouge">r0</code>.”</em></p>

<figure>
<svg role="img" aria-labelledby="title desc" viewBox="0 0 800 220" width="100%" xmlns="http://www.w3.org/2000/svg">

  <title id="title">Base register addressing</title>

  <desc id="desc">Shows r1 holding an address 0x1000 and [r1] meaning value at memory[0x1000].</desc>

  <style>
    .fg { stroke:#E6E6E6; fill:#E6E6E6; }
    .box { fill:#0B1220; stroke:#A3B1C6; }
    .wire { stroke:#E6E6E6; }
    .muted { fill:#C8D0DB; }
    .marker path { fill:#E6E6E6; }  
    .label{font:16px sans-serif}
    .note{font:15px sans-serif}
    .mono{font:14px monospace}
  </style>

  <defs>
    <marker class="marker" id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" />
    </marker>
  </defs>

  <!-- r1 register -->
  <rect class="box" x="60" y="60" width="240" height="100" rx="8" />
  <text class="label fg" x="180" y="88" text-anchor="middle">r1</text>
  <text class="label fg" x="180" y="120" text-anchor="middle">0x00001000</text>

  <!-- memory cell -->
  <rect class="box" x="480" y="40" width="240" height="140" rx="8" />
  <text class="label fg" x="600" y="70" text-anchor="middle">Memory[0x1000]</text>
  <rect class="box" x="510" y="90" width="180" height="40" />
  <text class="mono fg" x="600" y="115" text-anchor="middle">0x000004D2</text>

  <!-- arrow and labels -->
  <line x1="300" y1="110" x2="480" y2="110" stroke="#6f6c6cff" stroke-width="2" marker-end="url(#arrow)" />
  <text class="note fg" x="390" y="100" text-anchor="middle">[r1] ≡ *(uint32_t*)0x1000</text>
</svg>
<figcaption>
If <code>r1</code> holds 0x1000, then <code>[r1]</code> means the value stored at that memory address.
</figcaption>
</figure>

<h2 id="how-the-cpu-reads-and-writes-data-via-an-address">How the CPU Reads and Writes Data via an Address</h2>

<p>The CPU interprets the value stored in the base register (<code class="language-plaintext highlighter-rouge">Rn</code>) as a memory address.
It then locates the memory cell corresponding to that address and either reads or writes the value stored there.</p>

<p>In other words:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">ldr</code> = read data from memory using the address.</li>
  <li><code class="language-plaintext highlighter-rouge">str</code> = write data to memory using the address.</li>
</ul>

<h2 id="practice-example">Practice Example</h2>

<p>The following examples demonstrate the basic behavior of <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code>.
You can verify the results using <strong>QEMU (versatilepb)</strong> with <code class="language-plaintext highlighter-rouge">gdb-multiarch</code>.</p>

<blockquote>
  <p>For compiling, running on QEMU, and setting up GDB, refer to <a href="/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html">this post</a>.</p>
</blockquote>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>       <span class="no">@</span> <span class="nv">store</span> <span class="nv">decimal</span> <span class="mi">3</span> <span class="nv">into</span> <span class="nv">r0</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">=</span><span class="mh">0x1000</span>    <span class="no">@</span> <span class="nv">store</span> <span class="nv">address</span> <span class="mh">0x1000</span> <span class="nv">into</span> <span class="nv">r1</span>
  <span class="nb">str</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="o">[</span><span class="mh">0x1000</span><span class="o">]</span> <span class="o">=</span> <span class="mi">3</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>Here, the CPU interprets the value in <code class="language-plaintext highlighter-rouge">r1</code> (<code class="language-plaintext highlighter-rouge">0x1000</code>) as a memory address
and stores the value of <code class="language-plaintext highlighter-rouge">r0</code> (<code class="language-plaintext highlighter-rouge">3</code>) into that memory location.</p>

<p>Next, we can load the value back from memory:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">ldr</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">=</span><span class="mh">0x1000</span>    <span class="no">@</span> <span class="nv">store</span> <span class="nv">address</span> <span class="mh">0x1000</span> <span class="nv">into</span> <span class="nv">r1</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">[</span><span class="nv">r1</span><span class="o">]</span>       <span class="no">@</span> <span class="nv">load</span> <span class="nv">value</span> <span class="nv">from</span> <span class="o">[</span><span class="mh">0x1000</span><span class="o">]</span> <span class="nv">into</span> <span class="nv">r0</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>If memory address <code class="language-plaintext highlighter-rouge">0x1000</code> previously held <code class="language-plaintext highlighter-rouge">3</code>,
then after execution, <code class="language-plaintext highlighter-rouge">r0</code> will contain <code class="language-plaintext highlighter-rouge">3</code>.</p>

<hr />
<p>In this post, we explored <strong>the most fundamental way the CPU interacts with memory using <code class="language-plaintext highlighter-rouge">ldr</code> and <code class="language-plaintext highlighter-rouge">str</code></strong>.
In the next post, we’ll cover more flexible addressing modes using offsets.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ldr" /><category term="str" /><category term="memory-access" /><category term="armv4" /><summary type="html"><![CDATA[This post is an introductory tutorial on memory access for beginners learning ARM assembly. Using the ARMv4 architecture as a reference, we’ll explore how the CPU uses the ldr (load) and str (store) instructions to read and write data to memory.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #6 - Arithmetic Operations</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/10/27/arm-arithmetic-operations.html" rel="alternate" type="text/html" title="ARM Assembly #6 - Arithmetic Operations" /><published>2025-10-27T11:20:00+00:00</published><updated>2025-10-27T11:20:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/10/27/arm-arithmetic-operations</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/10/27/arm-arithmetic-operations.html"><![CDATA[<p>In this post, we’ll explore the <strong>basic arithmetic operations</strong> in ARM Assembly —<br />
addition, subtraction, and their variants that include <strong>carry (C flag)</strong>.</p>

<p>So far, we’ve learned how to move values into registers and how to shift them.<br />
Now it’s time to perform actual <strong>arithmetic operations</strong> with those values.</p>

<h2 id="addition-and-subtraction-in-armv4">Addition and Subtraction in ARMv4</h2>
<p>ARM provides the following arithmetic instructions:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Instructions</th>
      <th style="text-align: center">Descriptions</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">add</code></td>
      <td style="text-align: center">Add</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">adc</code></td>
      <td style="text-align: center">Add with carry</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sub</code></td>
      <td style="text-align: center">Subtract</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sbc</code></td>
      <td style="text-align: center">Subtract with carry</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">rsb</code></td>
      <td style="text-align: center">Reverse subtract</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">rsc</code></td>
      <td style="text-align: center">Reverse subtract with carry</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>From now on, we’ll refer specifically to <strong>ARMv4</strong>, as this series is based on that architecture.</p>
</blockquote>

<p>All these instructions are <strong>binary operations</strong> that take two operands:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>result = first-operand op second-operand
&lt;opcode&gt; = &lt;Rd&gt;, &lt;Rn&gt;, &lt;shifter-operand&gt;
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Field</th>
      <th style="text-align: center">Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">&lt;opcode&gt;</code></td>
      <td style="text-align: center">Operations (e.g., add, sub)</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">&lt;Rd&gt;</code></td>
      <td style="text-align: center">Destination register (result)</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">&lt;Rn&gt;</code></td>
      <td style="text-align: center">First operand</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">&lt;shifter-operand&gt;</code></td>
      <td style="text-align: center">Second operand (immediate or shifted register)</td>
    </tr>
  </tbody>
</table>

<p>Just like other <strong>data processing instructions</strong>, these also use<br />
the <a href="/arm/assembly/tutorial/2025/09/25/arm-sfhiter-operand.html">shifter operand</a> mechanism introduced earlier.</p>

<h2 id="1-add--addition">1. ADD : Addition</h2>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Assembly Syntax: add &lt;Rd&gt;, &lt;Rn&gt;, &lt;Shifter-Operand&gt;

Methemetical form: C = A + B
  C: &lt;Rd&gt;
  A: &lt;Rn&gt;
  B: &lt;Shifter-Operand&gt;
</code></pre></div></div>

<p>Unlike mathematics, where <code class="language-plaintext highlighter-rouge">C = A + B</code> and <code class="language-plaintext highlighter-rouge">C = B + A</code> are equivalent,<br />
ARM assembly requires the <strong>first operand (<code class="language-plaintext highlighter-rouge">&lt;Rn&gt;</code>) to be a register</strong>,<br />
so the order <strong>cannot be swapped</strong>.</p>

<p><strong>Valid</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">add</span> <span class="nb">r0</span><span class="err">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>
</code></pre></div></div>
<p><strong>Invalid</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">add</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">#</span><span class="mi">2</span><span class="o">,</span> <span class="nv">r1</span>   <span class="no">@</span> <span class="nv">The</span> <span class="nv">first</span> <span class="nv">operand</span> <span class="nv">must</span> <span class="nv">be</span> <span class="nv">a</span> <span class="nv">register</span><span class="mf">.</span>
</code></pre></div></div>

<h3 id="example-add">Example: ADD</h3>
<p><strong>add.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span><span class="err">:</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
<span class="hll">  <span class="nb">add</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
</span>  <span class="nb">add</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span>
  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p>Explanation:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #3</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R0 = 3</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">add r1, r0, #3</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R1 = R0 + 3</code></li>
      <li><code class="language-plaintext highlighter-rouge">R1= 3 + 3</code></li>
      <li><code class="language-plaintext highlighter-rouge">R1 = 6</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">add r2, r0, r0 lsl #2</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R2 = R0 + (3 &lt;&lt; 2)</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 3 + (3 &lt;&lt; 2)</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 3 + 12</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 15</code></li>
    </ul>
  </li>
</ul>

<p>Here, the second operand <code class="language-plaintext highlighter-rouge">r0 lsl #2</code> is a <strong>shifter operand</strong>,
so ARM shifts <code class="language-plaintext highlighter-rouge">r0</code> left by 2 bits before performing the addition.</p>

<h2 id="2-sub--subtraction">2. SUB : Subtraction</h2>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Assembly Syntax: sub &lt;Rd&gt;, &lt;Rn&gt;, &lt;Shifter-Operand&gt;

Methemetical form: C = A - B
  C: &lt;Rd&gt;
  A: &lt;Rn&gt;
  B: &lt;Shifter-Operand&gt;
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">sub</code> subtracts the second operand from the first operand.</p>

<h3 id="example-sub">Example: SUB</h3>
<p><strong>sub.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span><span class="err">:</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">4</span>
<span class="hll">  <span class="nb">sub</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x1</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p>Explanation:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #4</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R0 = 4</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">sub r1, r0, #0x1</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R1 = R0 - 0x1</code></li>
      <li><code class="language-plaintext highlighter-rouge">R1 = 4 - 1</code></li>
      <li><code class="language-plaintext highlighter-rouge">R1 = 3</code></li>
    </ul>
  </li>
</ul>

<h2 id="3-rsb--reverse-subtract">3. RSB : Reverse Subtract</h2>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Assembly Syntax: rsb &lt;Rd&gt;, &lt;Rn&gt;, &lt;Shifter-Operand&gt;

Mathmetical form: C = B - A
  C: &lt;Rd&gt;
  A: &lt;Rn&gt;
  B: &lt;Shifter-Operand&gt;
</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">rsb</code> performs subtraction in the <strong>reverse direction</strong>:
it subtracts the first operand from the second one.
RSB stands for <em>Reverse Subtract</em>.</p>

<h3 id="example-rsb">Example: RSB</h3>
<p><strong>rsb.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span><span class="err">:</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">5</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>
<span class="hll">  <span class="nb">rsb</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p>Explanation:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #5</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R0 = 5</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">mov r1, #2</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R1 = 2</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">rsb r2, r1, r0</code>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R2 = R0 - R1</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 5 - 2</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 3</code></li>
    </ul>
  </li>
</ul>

<p>If <code class="language-plaintext highlighter-rouge">sub r2, r1, r0</code> were used instead, the result would be <code class="language-plaintext highlighter-rouge">-3</code>.</p>

<h2 id="4-adcsbcrbc--operations-with-carry">4. ADC/SBC/RBC : Operations with Carry</h2>

<p>So far, we’ve handled simple addition and subtraction.
However, computers must also handle carry (C flag) —
the overflow or borrow that occurs when results exceed bit limits.</p>

<p>ARM provides separate instructions for these cases:
| Instruction | Desciption |
| :-: | :-: |
| <code class="language-plaintext highlighter-rouge">adc</code> | Add with carry (includes carry flag) |
| <code class="language-plaintext highlighter-rouge">sbc</code> | Subtract with carry (includes borrow) |
| <code class="language-plaintext highlighter-rouge">rsc</code> | Reverse subtract with carry |</p>

<h3 id="example-adc-add-with-carry">Example: ADC (Add with Carry)</h3>
<p><strong>adc.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x3</span>
  <span class="nb">movs</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="o">#</span><span class="mi">2</span>
  <span class="nb">adc</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">r1</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>Explanation:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #0x3</code>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R0 = 3</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">movs r0, r0, lsr #1</code>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R0 = R0 &gt;&gt; 1</code></li>
      <li><code class="language-plaintext highlighter-rouge">R0 = 3 &gt;&gt; 1</code></li>
      <li><code class="language-plaintext highlighter-rouge">R0 = 1</code></li>
      <li>because of the <code class="language-plaintext highlighter-rouge">S</code> suffix, the <strong>bit shifted out</strong> (1) sets the <strong>Carry flag</strong>.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">mov r1, #2</code>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R1 = 2</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">adc r2, r0, r1</code>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">R2 = R0 + R1 + Carry</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 1 + 2 + 1</code></li>
      <li><code class="language-plaintext highlighter-rouge">R2 = 4</code></li>
    </ul>
  </li>
</ul>

<h4 id="gdb-debugging-output">GDB Debugging Output</h4>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(gdb) x/5i 0x10000
=&gt; 0x10000 &lt;_start&gt;:    mov     r0, #3
   0x10004 &lt;_start+4&gt;:  lsrs    r0, r0, #1
   0x10008 &lt;_start+8&gt;:  mov     r1, #2
   0x1000c &lt;_start+12&gt;: adc     r2, r0, r1
   0x10010 &lt;_start+16&gt;: b       0x10010 &lt;_start+16&gt;
(gdb) p ($cpsr &gt;&gt; 29) &amp; 1
$1 = 0
(gdb) i r r0 r1 r2
r0             0x0      0
r1             0x0      0
r2             0x0      0
(gdb) si
0x00010004 in _start ()
(gdb) i r r0 r1 r2
r0             0x3      3
r1             0x0      0
r2             0x0      0
(gdb) p ($cpsr &gt;&gt; 29) &amp; 1
$2 = 0       @ Carry flag = 0
(gdb) si
0x00010008 in _start ()
(gdb) p ($cpsr &gt;&gt; 29) &amp; 1
$3 = 1
(gdb) i r r0 r1 r2
r0             0x1      1
r1             0x0      0
r2             0x0      0
(gdb) si
0x0001000c in _start ()
(gdb) i r r0 r1 r2
r0             0x1      1
r1             0x2      2
r2             0x0      0
(gdb) si
0x00010010 in _start ()
(gdb) p ($cpsr &gt;&gt; 29) &amp; 1
$4 = 1        @ Carry flag = 1
(gdb) i r r0 r1 r2
r0             0x1      1
r1             0x2      2
r2             0x4      4
(gdb)
</code></pre></div></div>

<p>As shown, the Carry flag was set during the shift,
and <code class="language-plaintext highlighter-rouge">adc</code> correctly included it in the addition.</p>

<h2 id="summary">Summary</h2>
<p>Here’s a quick summary of what we covered:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Type</th>
      <th style="text-align: center">Instuctions</th>
      <th style="text-align: center">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">Addition</td>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">adc</code></td>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">adc</code> adds with the carry flag</td>
    </tr>
    <tr>
      <td style="text-align: center">Subtraction</td>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sub</code>, <code class="language-plaintext highlighter-rouge">sbc</code></td>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sbc</code> subtracts with borrow</td>
    </tr>
    <tr>
      <td style="text-align: center">Reverse Subtraction</td>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">rsb</code>, <code class="language-plaintext highlighter-rouge">rsc</code></td>
      <td style="text-align: center">Performs subtraction in reverse order</td>
    </tr>
  </tbody>
</table>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="add" /><category term="sub" /><category term="adc" /><category term="sbc" /><category term="rsb" /><category term="rsc" /><summary type="html"><![CDATA[In this post, we’ll explore the basic arithmetic operations in ARM Assembly — addition, subtraction, and their variants that include carry (C flag).]]></summary></entry><entry xml:lang="en"><title type="html">Binary Representation: Signed vs Unsigned</title><link href="https://seojuncha.github.io/2025/09/27/signed-and-unsigned-binary.html" rel="alternate" type="text/html" title="Binary Representation: Signed vs Unsigned" /><published>2025-09-27T13:00:00+00:00</published><updated>2025-09-27T13:00:00+00:00</updated><id>https://seojuncha.github.io/2025/09/27/signed-and-unsigned-binary</id><content type="html" xml:base="https://seojuncha.github.io/2025/09/27/signed-and-unsigned-binary.html"><![CDATA[<p>In this article, we will explore the <strong>differences between Signed Binary and Unsigned Binary</strong>, <strong>their respective representation methods</strong>, and <strong>considerations when programming in C</strong>.</p>

<p>From the user’s perspective, data types handled by computers can be broadly categorized as either <strong>characters</strong> or <strong>numbers</strong>. However, from the computer’s perspective, even characters are ultimately processed as numbers. Therefore, today, we will focus on the numeric data types, specifically integers, and how they are handled by computers.</p>

<figure>
  <img src="https://upload.wikimedia.org/wikipedia/commons/b/b3/ASCII-Table.png" style="display: block; margin: auto;" alt="wiki-ascii-table" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
An ASCII table is used to represent characters as numbers.
  </figcaption>
</figure>

<h2 id="concept-of-integers">Concept of Integers</h2>
<p>Numerical data in computers can be broadly divided into integers and real numbers. In mathematics, numbers are classified based on their characteristics as shown below.</p>

<h3 id="integers-and-real-numbers">Integers and Real Numbers</h3>
<figure>
  <img src="https://upload.wikimedia.org/wikipedia/commons/4/43/Classification_of_Numbers.jpg" style="display: block; margin: auto;" alt="classification-of-real-number" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
Definition of integers within the range of real numbers, which include positive numbers, negative numbers, and zero.
  </figcaption>
</figure>

<h3 id="meaning-of-integers-in-computers">Meaning of Integers in Computers</h3>
<p>All data is stored in a computer’s memory as binary. This means that <strong>storing an integer in a computer’s memory occupies a specific memory space</strong>. In daily life, we use decimal representation, where each digit (e.g., tens, hundreds) can range from 0 to 9, allowing for 10 different values per place (hence the term “decimal”).</p>

<blockquote>
  <p><strong>Memory Space?</strong><br />
The term “memory space” is used because data is not always stored in main memory (RAM). For instance, data can also be stored in CPU registers. For convenience, we will use “memory” interchangeably in this article.</p>
</blockquote>

<p>Computers process all data as binary (0s and 1s). Thus, to represent any data, including numbers, we need a binary representation. However, this is merely a difference in representation; the meaning remains the same. For instance, the number ‘5’ is represented as ‘101’ in binary but remains the same numerical value.</p>

<p>As previously defined, integers consist of positive numbers, negative numbers, and zero. In computers, integers are represented using <strong>Signed Integers</strong> and <strong>Unsigned Integers</strong>.</p>

<ul>
  <li><strong>Signed Integers</strong> include both positive (+) and negative (-) signs in memory.</li>
  <li><strong>Unsigned Integers</strong> assume a positive sign by default and only store the magnitude of the number in memory.</li>
</ul>

<p>This distinction is significant because <strong>the same binary data can represent different values depending on whether it is treated as signed or unsigned</strong>.</p>

<h2 id="unsigned-binary-concept-and-conversion">Unsigned Binary: Concept and Conversion</h2>
<p>Unsigned Binary represents unsigned integers in binary. Since there is no sign bit, straightforward mathematical conversion between binary and decimal applies.</p>

<blockquote>
  <p><strong>Natural Numbers</strong><br />
Unsigned integers are equivalent to natural numbers.</p>
</blockquote>

<h3 id="concept">Concept</h3>

<p>For example, in a 4-bit space, the binary, decimal, and hexadecimal representations of unsigned integers are as follows:</p>

<p><strong>4-bit Unsigned Binary</strong></p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Binary (bin)</th>
      <th style="text-align: center">Decimal (dec)</th>
      <th style="text-align: center">Hexadecimal (hex)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0000</td>
      <td style="text-align: center">0</td>
      <td style="text-align: center">0x0</td>
    </tr>
    <tr>
      <td style="text-align: center">0001</td>
      <td style="text-align: center">1</td>
      <td style="text-align: center">0x1</td>
    </tr>
    <tr>
      <td style="text-align: center">0010</td>
      <td style="text-align: center">2</td>
      <td style="text-align: center">0x2</td>
    </tr>
    <tr>
      <td style="text-align: center">0011</td>
      <td style="text-align: center">3</td>
      <td style="text-align: center">0x3</td>
    </tr>
    <tr>
      <td style="text-align: center">0100</td>
      <td style="text-align: center">4</td>
      <td style="text-align: center">0x4</td>
    </tr>
    <tr>
      <td style="text-align: center">0101</td>
      <td style="text-align: center">5</td>
      <td style="text-align: center">0x5</td>
    </tr>
    <tr>
      <td style="text-align: center">0110</td>
      <td style="text-align: center">6</td>
      <td style="text-align: center">0x6</td>
    </tr>
    <tr>
      <td style="text-align: center">0111</td>
      <td style="text-align: center">7</td>
      <td style="text-align: center">0x7</td>
    </tr>
    <tr>
      <td style="text-align: center">1000</td>
      <td style="text-align: center">8</td>
      <td style="text-align: center">0x8</td>
    </tr>
    <tr>
      <td style="text-align: center">1001</td>
      <td style="text-align: center">9</td>
      <td style="text-align: center">0x9</td>
    </tr>
    <tr>
      <td style="text-align: center">1010</td>
      <td style="text-align: center">10</td>
      <td style="text-align: center">0xA</td>
    </tr>
    <tr>
      <td style="text-align: center">1011</td>
      <td style="text-align: center">11</td>
      <td style="text-align: center">0xB</td>
    </tr>
    <tr>
      <td style="text-align: center">1100</td>
      <td style="text-align: center">12</td>
      <td style="text-align: center">0xC</td>
    </tr>
    <tr>
      <td style="text-align: center">1101</td>
      <td style="text-align: center">13</td>
      <td style="text-align: center">0xD</td>
    </tr>
    <tr>
      <td style="text-align: center">1110</td>
      <td style="text-align: center">14</td>
      <td style="text-align: center">0xE</td>
    </tr>
    <tr>
      <td style="text-align: center">1111</td>
      <td style="text-align: center">15</td>
      <td style="text-align: center">0xF</td>
    </tr>
  </tbody>
</table>

<p>Thus, the decimal range of a 4-bit unsigned integer is $0 \leq \Z \leq 2^4-1$, and the hexadecimal range is from <code class="language-plaintext highlighter-rouge">0x0</code> to <code class="language-plaintext highlighter-rouge">0xF</code>.</p>

<blockquote>
  <p><strong>Hexadecimal Representation by Bit Size</strong><br />
Hexadecimal is widely used to represent numbers in computer systems. Here are the maximum hexadecimal values based on bit size:<br />
4 bits  = 0xF<br />
8 bits  = 0xFF<br />
16 bits = 0xFFFF<br />
32 bits = 0xFFFF_FFFF</p>
</blockquote>

<h3 id="conversion">Conversion</h3>
<h4 id="converting-decimal-to-binary">Converting Decimal to Binary</h4>
<p>To convert a decimal number to binary, use division (/) and modulus (%) operations as follows:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Division (/)</th>
      <th style="text-align: center">Modulus (%)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">45 / 2 = 22</td>
      <td style="text-align: center">45 % 2 = 1</td>
    </tr>
    <tr>
      <td style="text-align: center">22 / 2 = 11</td>
      <td style="text-align: center">22 % 2 = 0</td>
    </tr>
    <tr>
      <td style="text-align: center">11 / 2 = 5</td>
      <td style="text-align: center">11 % 2 = 1</td>
    </tr>
    <tr>
      <td style="text-align: center">5 / 2 = 2</td>
      <td style="text-align: center">5 % 2 = 1</td>
    </tr>
    <tr>
      <td style="text-align: center">2 / 2 = 1</td>
      <td style="text-align: center">2 % 2 = 0</td>
    </tr>
    <tr>
      <td style="text-align: center">1 / 2 = 0</td>
      <td style="text-align: center">1 % 2 = 1</td>
    </tr>
  </tbody>
</table>

\[\begin{aligned}
45_{10} &amp;= 22 \times 2 + 1 \newline
&amp;= (11 \times 2 + 0) \times 2 + 1 \newline
&amp;= ((5 \times 2 + 1) \times 2 + 0) \times 2 + 1 \newline
&amp;= (((2 \times 2 + 1) \times 2 + 1) \times 2 + 0) \times 2 + 1 \newline
&amp;= ((((1 \times 2 + 0) \times 2 + 1) \times 2 + 1) \times 2 + 0) \times 2 + 1 \newline
&amp;= (((((0 \times 2 + 1) \times 2 + 0) \times 2 + 1) \times 2 + 1) \times 2 + 0) \times 2 + 1 \newline
&amp;= 1 \times 2^5 + 0 \times 2^4 + 1 \times 2^3 + 1 \times 2^2 + 0 \times 2^1 + 1 \newline
&amp;= 101101_2
\end{aligned}\]

<h4 id="converting-binary-to-decimal">Converting Binary to Decimal</h4>
<p>Binary numbers can be converted to decimal using positional notation:</p>

\[\begin{aligned}
101_2 &amp;= 1 \times 2^2 + 0 \times 2^1 + 1 \times 2^0  \newline
&amp;= 1 \times 4 + 0 \times 2 + 1 \times 1 \newline
&amp;= 5_{10}
\end{aligned}\]

<h4 id="using-python-for-conversion">Using Python for Conversion</h4>
<p>Using Python’s REPL is often the simplest way to convert between binary, decimal, and hexadecimal. Note that the <code class="language-plaintext highlighter-rouge">bin</code> and <code class="language-plaintext highlighter-rouge">hex</code> functions return strings, not integers.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="nf">bin</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
<span class="sh">'</span><span class="s">0b1</span><span class="sh">'</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">bin</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>
<span class="sh">'</span><span class="s">0b10</span><span class="sh">'</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">int</span><span class="p">(</span><span class="mb">0b11</span><span class="p">)</span>
<span class="mi">3</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">int</span><span class="p">(</span><span class="mb">0b110</span><span class="p">)</span>
<span class="mi">6</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">hex</span><span class="p">(</span><span class="mb">0b1</span><span class="p">)</span>
<span class="sh">'</span><span class="s">0x1</span><span class="sh">'</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">hex</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
<span class="sh">'</span><span class="s">0x1</span><span class="sh">'</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">int</span><span class="p">(</span><span class="mh">0xf</span><span class="p">)</span>
<span class="mi">15</span>
</code></pre></div></div>

<h2 id="signed-binary-representation-and-python-usage">Signed Binary: Representation and Python Usage</h2>
<p>Signed Binary represents signed integers in binary. Unlike unsigned binary, signed binary requires bit patterns to represent the sign. Although there are three methods to represent signed binary, modern computers primarily use the two’s complement method.</p>

<h3 id="representation-methods">Representation Methods</h3>
<p>The basic principle for representing negative numbers is to <strong>allocate one bit (0 or 1) for the sign</strong>:</p>
<ul>
  <li>0: Positive</li>
  <li>1: Negative</li>
</ul>

<h4 id="method-1-sign-magnitude">Method 1: Sign-Magnitude</h4>
<p>In the sign-magnitude method, the most significant bit (MSB) is used to indicate the sign.
For example, in an 8-bit space, the binary representation of +5 and −5 is:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+5 = 0000 0101
-5 = 1000 0101
</code></pre></div></div>
<p>Here, <strong><em>0101</em></strong> represents the number <strong><em>5</em></strong>, and the MSB is set to 0 (positive) or 1 (negative).
In an 8-bit space, the range of representable values using this method is summarized as follows:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Binary</th>
      <th style="text-align: center">Decimal</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0000 0000</td>
      <td style="text-align: center">+0</td>
    </tr>
    <tr>
      <td style="text-align: center">0000 0001</td>
      <td style="text-align: center">+1</td>
    </tr>
    <tr>
      <td style="text-align: center">0000 0010</td>
      <td style="text-align: center">+2</td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
      <td style="text-align: center">…</td>
    </tr>
    <tr>
      <td style="text-align: center">0111 1111</td>
      <td style="text-align: center">+127</td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0000</td>
      <td style="text-align: center">-0</td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0001</td>
      <td style="text-align: center">-1</td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
      <td style="text-align: center">…</td>
    </tr>
    <tr>
      <td style="text-align: center">1111 1111</td>
      <td style="text-align: center">-127</td>
    </tr>
  </tbody>
</table>

<p>Thus, this method allows the representation of 254 unique values: <code class="language-plaintext highlighter-rouge">+0</code> to <code class="language-plaintext highlighter-rouge">+127</code> and <code class="language-plaintext highlighter-rouge">-0</code> to <code class="language-plaintext highlighter-rouge">-127</code>.</p>

<h4 id="method-2-ones-complement">Method 2: One’s Complement</h4>
<p>In one’s complement, the binary representation of an unsigned integer is inverted (0 becomes 1 and 1 becomes 0).</p>

<p><strong>Bit Inversion</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0 -&gt; 1
1 -&gt; 0
</code></pre></div></div>
<p><strong>Representation of <code class="language-plaintext highlighter-rouge">±5</code> in 8-bit One’s Complement</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+5 = 0000 0101
-5 = 1111 1010
</code></pre></div></div>
<p>The range of representable values in 8-bit one’s complement is summarized as:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Binary</th>
      <th style="text-align: center">Decimal</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0000 0000</td>
      <td style="text-align: center">+0</td>
    </tr>
    <tr>
      <td style="text-align: center">0000 0001</td>
      <td style="text-align: center">+1</td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
      <td style="text-align: center">…</td>
    </tr>
    <tr>
      <td style="text-align: center">0111 1110</td>
      <td style="text-align: center">+126</td>
    </tr>
    <tr>
      <td style="text-align: center">0111 1111</td>
      <td style="text-align: center">+127</td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0000</td>
      <td style="text-align: center">-127</td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0001</td>
      <td style="text-align: center">-126</td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
      <td style="text-align: center">…</td>
    </tr>
    <tr>
      <td style="text-align: center">1111 1110</td>
      <td style="text-align: center">-1</td>
    </tr>
    <tr>
      <td style="text-align: center">1111 1111</td>
      <td style="text-align: center">-0</td>
    </tr>
  </tbody>
</table>

<h4 id="method-3-twos-complement">Method 3: Two’s Complement</h4>
<p>In two’s complement, the binary representation is obtained by inverting all bits and adding 1 to the least significant bit (LSB). This method is widely used in modern computers as it addresses the shortcomings of the previous methods.</p>

<p>To represent a negative number in two’s complement:</p>
<ol>
  <li>Compute the one’s complement of the unsigned integer.</li>
  <li>Add 1 to the LSB.</li>
</ol>

<p><strong>Representation of ±5 in 8-bit Two’s Complement</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+5 = 0000 0101
-5 = 1111 1010 (One's Complement)
   +         1
   -----------
   = 1111 1011
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Binary</th>
      <th style="text-align: center">Decimal</th>
      <th style="text-align: center">Transformation (Positive → One’s Complement → +1)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0000 0000</td>
      <td style="text-align: center">+0</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">0000 0001</td>
      <td style="text-align: center">+1</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">0000 0010</td>
      <td style="text-align: center">+2</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
      <td style="text-align: center">…</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">0111 1110</td>
      <td style="text-align: center">+126</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">0111 1111</td>
      <td style="text-align: center">+127</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0000</td>
      <td style="text-align: center">-128</td>
      <td style="text-align: center">1000 0000 → 0111 1111 → 1000 0000</td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0001</td>
      <td style="text-align: center">-127</td>
      <td style="text-align: center">0111 1111 → 1000 0000 → 1000 0001</td>
    </tr>
    <tr>
      <td style="text-align: center">1000 0010</td>
      <td style="text-align: center">-126</td>
      <td style="text-align: center">0111 1110 → 1000 0001 → 1000 0010</td>
    </tr>
    <tr>
      <td style="text-align: center">…</td>
      <td style="text-align: center">…</td>
      <td style="text-align: center"> </td>
    </tr>
    <tr>
      <td style="text-align: center">1111 1101</td>
      <td style="text-align: center">-3</td>
      <td style="text-align: center">0000 0100 → 1111 1011 → 1111 1101</td>
    </tr>
    <tr>
      <td style="text-align: center">1111 1110</td>
      <td style="text-align: center">-2</td>
      <td style="text-align: center">0000 0010 → 1111 1101 → 1111 1110</td>
    </tr>
    <tr>
      <td style="text-align: center">1111 1111</td>
      <td style="text-align: center">-1</td>
      <td style="text-align: center">0000 0001 → 1111 1110 → 1111 1111</td>
    </tr>
  </tbody>
</table>

<h4 id="using-python-for-twos-complement-conversion">Using Python for Two’s Complement Conversion</h4>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="nf">int</span><span class="p">(</span><span class="sh">"</span><span class="s">-128</span><span class="sh">"</span><span class="p">,</span> <span class="mi">10</span><span class="p">).</span><span class="nf">to_bytes</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">byteorder</span><span class="o">=</span><span class="sh">"</span><span class="s">little</span><span class="sh">"</span><span class="p">,</span> <span class="n">signed</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="sa">b</span><span class="sh">'</span><span class="se">\x80</span><span class="sh">'</span>

<span class="o">&gt;&gt;&gt;</span> <span class="nf">int</span><span class="p">(</span><span class="sh">"</span><span class="s">-3</span><span class="sh">"</span><span class="p">,</span> <span class="mi">10</span><span class="p">).</span><span class="nf">to_bytes</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">byteorder</span><span class="o">=</span><span class="sh">"</span><span class="s">little</span><span class="sh">"</span><span class="p">,</span> <span class="n">signed</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="sa">b</span><span class="sh">'</span><span class="se">\xfd</span><span class="sh">'</span>
<span class="o">&gt;&gt;&gt;</span> <span class="nf">bin</span><span class="p">(</span><span class="mh">0xfd</span><span class="p">)</span>
<span class="sh">'</span><span class="s">0b11111101</span><span class="sh">'</span>
</code></pre></div></div>

<h2 id="signed-vs-unsigned-comparison">Signed vs Unsigned Comparison</h2>
<p>A summary of the key differences between signed and unsigned integers is as follows:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Category</th>
      <th style="text-align: left">Signed Integer</th>
      <th style="text-align: left">Unsigned Integer</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">Value Range</td>
      <td style="text-align: left">Negative, 0, Positive</td>
      <td style="text-align: left">0, Positive</td>
    </tr>
    <tr>
      <td style="text-align: left">Range (8-bit)</td>
      <td style="text-align: left">-128 to 127</td>
      <td style="text-align: left">0 to 255</td>
    </tr>
    <tr>
      <td style="text-align: left">Use Cases</td>
      <td style="text-align: left">Sensors, Offsets, etc.</td>
      <td style="text-align: left">Memory Addresses, Counters/Timers</td>
    </tr>
    <tr>
      <td style="text-align: left">Sign Bit</td>
      <td style="text-align: left">Present (MSB)</td>
      <td style="text-align: left">Absent</td>
    </tr>
  </tbody>
</table>

<p>The representable range based on two’s complement for signed integers and unsigned integers can be summarized mathematically:</p>

<p><strong>Unsigned Integer</strong>
\(0 \leq Z \leq 2^{\text{bit size}} - 1\)</p>

<p><strong>Signed Integer</strong>
\(-2^{\text{bit size} - 1} \leq Z \leq 2^{\text{bit size} - 1} - 1\)</p>

<h2 id="programming-considerations">Programming Considerations</h2>
<p>As seen, the interpretation of binary data changes depending on whether it is signed or unsigned. Understanding these differences is crucial when writing programs. Let’s look at two common pitfalls in C programming.</p>

<h3 id="example-1-comparing-signed-and-unsigned-integers">Example 1: Comparing Signed and Unsigned Integers</h3>
<p>A common mistake is comparing integers of different signedness. In the example below, the signed integer <code class="language-plaintext highlighter-rouge">-1</code> is treated as an unsigned integer during comparison, resulting in unexpected behavior.</p>

<p><strong>comp.c</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
  <span class="kt">unsigned</span> <span class="n">b</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>

  <span class="k">if</span> <span class="p">(</span><span class="n">a</span> <span class="o">&lt;</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"a is less than b</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
  <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"a is greater than or equals to b</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>
<p><strong>Output</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ gcc comp.c &amp;&amp; ./a.out
a is greater than or equals to b
</code></pre></div></div>
<p>Although intuitively <code class="language-plaintext highlighter-rouge">-1</code> is less than <code class="language-plaintext highlighter-rouge">1</code>, the signed integer <code class="language-plaintext highlighter-rouge">a</code> is converted to an unsigned integer, resulting in a value greater than <code class="language-plaintext highlighter-rouge">b</code>.</p>

<blockquote>
  <p><strong>Bit Pattern of <code class="language-plaintext highlighter-rouge">int a = -1</code></strong><br />
a = -1 = 0xFFFFFFFF (Two’s complement representation of -1) = 4,294,967,295</p>
</blockquote>

<p>Use the <a href="https://gcc.gnu.org/onlinedocs/gcc/Warning-Options.html#index-Wsign-compare">-W compiler option</a> to prevent such issues:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gcc <span class="nt">-W</span> comp.c
comp.c: In <span class="k">function</span> ‘main’:
comp.c:8:9: warning: comparison of integer expressions of different signedness: ‘int’ and ‘unsigned int’ <span class="o">[</span><span class="nt">-Wsign-compare</span><span class="o">]</span>
    8 |   <span class="k">if</span> <span class="o">(</span>a &lt; b<span class="o">)</span> <span class="o">{</span>
      |         ^
</code></pre></div></div>

<h3 id="example-2-type-casting">Example 2: Type Casting</h3>
<p>Another common mistake involves type casting between signed and unsigned integers. When casting, the value’s bit pattern remains unchanged, but its interpretation changes.</p>

<p><strong>main.c</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdint.h&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
  <span class="kt">int8_t</span> <span class="n">i8</span> <span class="o">=</span> <span class="o">-</span><span class="mi">5</span><span class="p">;</span>  <span class="c1">// 1111 1011</span>
  <span class="kt">uint8_t</span> <span class="n">u8</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>  <span class="c1">// 0000 0101</span>
  <span class="kt">uint8_t</span> <span class="n">ui8</span> <span class="o">=</span> <span class="p">(</span><span class="kt">uint8_t</span><span class="p">)</span><span class="n">i8</span><span class="p">;</span>  <span class="c1">// 1111 1011</span>

  <span class="n">printf</span><span class="p">(</span><span class="s">"UINT8_MAX: %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">UINT8_MAX</span><span class="p">);</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"INT8_MAX: %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">INT8_MAX</span><span class="p">);</span>

  <span class="n">printf</span><span class="p">(</span><span class="s">"0x%02x [%d]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">i8</span><span class="p">,</span> <span class="n">i8</span><span class="p">);</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"0x%02x [%d]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">u8</span><span class="p">,</span> <span class="n">u8</span><span class="p">);</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"0x%02x [%d]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui8</span><span class="p">,</span> <span class="n">ui8</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>
<p><strong>Output</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>gcc main.c <span class="o">&amp;&amp;</span> ./a.out
UINT8_MAX: 255
INT8_MAX: 127
0xfffffffb <span class="o">[</span><span class="nt">-5</span><span class="o">]</span>     <span class="c"># (1)</span>
0x05 <span class="o">[</span>5]            <span class="c"># (2)</span>
0xfb <span class="o">[</span>251]          <span class="c"># (3)</span>
</code></pre></div></div>

<ul>
  <li><strong>Sign Extension</strong>: When printing <code class="language-plaintext highlighter-rouge">i8</code>, the signed integer is promoted to <code class="language-plaintext highlighter-rouge">int</code>, causing sign extension (filling higher bits with the MSB of <code class="language-plaintext highlighter-rouge">i8</code>).</li>
  <li><strong>Bit Pattern Interpretation</strong>: The value of <code class="language-plaintext highlighter-rouge">ui8</code> is interpreted as an unsigned integer, resulting in <code class="language-plaintext highlighter-rouge">251</code> instead of <code class="language-plaintext highlighter-rouge">-5</code>.</li>
</ul>

<h2 id="conclusion">Conclusion</h2>
<p>This article explored the differences between <strong>Signed</strong> and <strong>Unsigned Binary</strong>, their representation methods, and programming considerations.</p>

<ul>
  <li><strong>Signed Binary</strong>: Represents both positive and negative values (e.g., sensor data, offsets).</li>
  <li><strong>Unsigned Binary</strong>: Represents only positive values (e.g., memory addresses, timers).</li>
</ul>

<p><strong>Did this article help you? Leave your questions or feedback in the comments below!</strong></p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="signed binary" /><category term="unsigned binary" /><category term="binary representation" /><summary type="html"><![CDATA[In this article, we will explore the differences between Signed Binary and Unsigned Binary, their respective representation methods, and considerations when programming in C.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #5 - A Complete Guide to Shifter Operands</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/25/arm-sfhiter-operand.html" rel="alternate" type="text/html" title="ARM Assembly #5 - A Complete Guide to Shifter Operands" /><published>2025-09-25T14:10:00+00:00</published><updated>2025-09-25T14:10:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/25/arm-sfhiter-operand</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/25/arm-sfhiter-operand.html"><![CDATA[<p>In this post, we’ll take a complete look at <strong>Shifter Operands</strong> used in ARM’s <strong>Data Processing Instructions</strong>. 
Shifter operands are not optional—they are <strong>fundamental components</strong> of all data processing instructions.</p>

<p>We’ll focus on three key aspects:</p>

<ol>
  <li>Types and syntax of shifter operands</li>
  <li>How immediate values are encoded and what limitations they have</li>
  <li>Why ARM supports shifter operands at all</li>
</ol>

<h2 id="how-shifter-operands-are-used">How Shifter Operands Are Used</h2>

<p>Instructions like <code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, and <code class="language-plaintext highlighter-rouge">sub</code> always use at least one <strong>shifter operand</strong>.<br />
A typical data processing instruction includes:</p>

<ul>
  <li>Destination register (<code class="language-plaintext highlighter-rouge">Rd</code>)</li>
  <li>Input register (<code class="language-plaintext highlighter-rouge">Rn</code>)</li>
  <li>Shifter operand (<code class="language-plaintext highlighter-rouge">Shifter Operand</code>)</li>
</ul>

<figure>
  <img src="/assets/img/arm-data-processing-table.png" style="display: block; margin: auto;" alt="ARM Data Processing Instruction Table" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
    A summary of ARM's <b>Data Processing Instructions</b>.  
    All of them use a <code>shifter_operand</code> and may combine it with <code>Rn</code> to produce a result in <code>Rd</code>, or simply update condition flags.
  </figcaption>
</figure>

<h3 id="instruction-format">Instruction Format</h3>

<ul>
  <li>Unary instructions (<code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">mvn</code>):</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;instruction&gt; &lt;Rd&gt;, &lt;Shifter Operand&gt;
</code></pre></div></div>

<ul>
  <li>Binary instructions (<code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code>, <code class="language-plaintext highlighter-rouge">and</code>, etc.):</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;instruction&gt; &lt;Rd&gt;, &lt;Rn&gt;, &lt;Shifter Operand&gt;
</code></pre></div></div>

<p>In other words:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">mov</code> and <code class="language-plaintext highlighter-rouge">mvn</code> store the shifter operand’s value (or its bitwise negation) into <code class="language-plaintext highlighter-rouge">Rd</code></li>
  <li>Other instructions compute <code class="language-plaintext highlighter-rouge">Rn op ShifterOperand</code>, storing the result in <code class="language-plaintext highlighter-rouge">Rd</code></li>
</ul>

<h2 id="types-of-shifter-operands">Types of Shifter Operands</h2>

<p>Shifter operands can take one of the following forms:</p>

<ul>
  <li><strong>Immediate constant</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">#&lt;immediate&gt;</code></li>
    </ul>
  </li>
  <li><strong>Register value</strong>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">&lt;Rm&gt;</code></li>
    </ul>
  </li>
  <li><strong>Shifted register</strong>
    <ul>
      <li>Logical shift left: <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, lsl #&lt;shift_imm&gt;</code> or <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, lsl &lt;Rs&gt;</code></li>
      <li>Logical shift right: <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, lsr #&lt;shift_imm&gt;</code> or <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, lsr &lt;Rs&gt;</code></li>
      <li>Arithmetic shift right: <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, asr #&lt;shift_imm&gt;</code> or <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, asr &lt;Rs&gt;</code></li>
      <li>Rotate: <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, ror #&lt;shift_imm&gt;</code> / <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, ror &lt;Rs&gt;</code> / <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;, rrx</code></li>
    </ul>
  </li>
</ul>

<p>Here, <code class="language-plaintext highlighter-rouge">&lt;Rm&gt;</code> is the operand register used in the operation.</p>

<p>You can plug these into the <code class="language-plaintext highlighter-rouge">&lt;Shifter Operand&gt;</code> position in any data-processing instruction.</p>

<p>Example:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># mov r0, r1
  &lt;instruction&gt; &lt;Rd&gt;, &lt;Shifter Operand&gt;
  &lt;instruction&gt; &lt;Rd&gt;, &lt;Rm&gt;
       mov       r0    r1
 
# add r0, r1, r0, lsl #2
  &lt;instruction&gt; &lt;Rd&gt;, &lt;Rn&gt;, &lt;Shifter Operand&gt;
  &lt;instruction&gt; &lt;Rd&gt;, &lt;Rn&gt;, &lt;Rm&gt; lsl #&lt;shift_imm&gt;
       add       r0    r1    r0         #2
</code></pre></div></div>

<h3 id="valid-and-invalid-syntax-examples">Valid and Invalid Syntax Examples</h3>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">mov</span> <span class="nb">r0</span><span class="err">,</span> <span class="nv">r1</span>           <span class="no">@</span> <span class="nv">Valid</span> <span class="o">(&lt;</span><span class="nv">Rd</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">&lt;</span><span class="nv">Rm</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nv">r1</span><span class="o">)</span>
<span class="nl">mov</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">#</span><span class="mh">0xff</span>        <span class="no">@</span> <span class="nv">Valid</span> <span class="o">(&lt;</span><span class="nv">Rd</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">&lt;</span><span class="nv">Shifter</span> <span class="nv">Operand</span><span class="o">&gt;</span> <span class="o">=</span> <span class="mh">0xff</span><span class="o">)</span>
<span class="nl">mov</span> <span class="nb">r0</span><span class="err">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span>   <span class="no">@</span> <span class="nv">Valid</span> <span class="o">(&lt;</span><span class="nv">Rd</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">&lt;</span><span class="nv">Sfhiter</span> <span class="nv">Operand</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span><span class="o">)</span>
<span class="nl">add</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">#</span><span class="mi">1</span>           <span class="no">@</span> <span class="nv">Invalid</span> <span class="o">(&lt;</span><span class="nv">Rd</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nv">r0</span><span class="o">,</span> <span class="nv">missing</span> <span class="o">&lt;</span><span class="nv">Rn</span><span class="o">&gt;)</span>
</code></pre></div></div>

<h2 id="immediate-value-limitations">Immediate Value Limitations</h2>
<p>When using an immediate (#<immediate>) as a shifter operand, there’s an important limitation.
You can’t represent all 32-bit values—even though ARM is a 32-bit architecture.</immediate></p>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x101</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<p>This causes an error:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>imm.s: Assembler messages:
imm.s:4: Error: invalid constant (101) after fixup
</code></pre></div></div>

<h3 id="immediate-value-encoding">Immediate Value Encoding</h3>
<p>Immediate values in shifter operands are encoded in <strong>12 bits</strong> total:</p>
<ul>
  <li>8-bit constant (imm8)</li>
  <li>4-bit rotation (rotate_imm)</li>
</ul>

<p>Actual rotation is <code class="language-plaintext highlighter-rouge">rotate_imm * 2</code> bits (even numbers only)</p>

<p>The final value is computed like this:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>#&lt;immediate&gt; = #&lt;imm8&gt; ROR (2 * #&lt;rotate_imm&gt;)
</code></pre></div></div>

<p>That means <strong>you can only represent values that can be formed by rotating an 8-bit number.</strong></p>
<blockquote>
  <p>Constraints:</p>
  <ul>
    <li>0x00 &lt;= imm8 &lt;= 0xff</li>
    <li>0 &lt;= rotate_imm &lt;= 15</li>
    <li>Effective rotation = 2 * rotate_imm</li>
  </ul>
</blockquote>

<h3 id="example-valid-immediate">Example: Valid Immediate</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">mov</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">#</span><span class="mh">0x104</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">0x104</code> = <code class="language-plaintext highlighter-rouge">0b0000_0000_0000_0000_0000_0001_0000_0100</code></li>
  <li>Can’t be stored directly in 8 bits, but:</li>
  <li>If you rotate it <strong>left by 2</strong> (== right by 30), the resulting 8-bit pattern(<code class="language-plaintext highlighter-rouge">0100_0001</code>) is valid</li>
  <li>Therefore, <code class="language-plaintext highlighter-rouge">imm8=0x41</code>, <code class="language-plaintext highlighter-rouge">rotate_imm=15</code> → valid encoding</li>
</ul>

<h3 id="example-invalid-immediate">Example: Invalid Immediate</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">mov</span> <span class="nb">r0</span><span class="err">,</span> <span class="o">#</span><span class="mh">0x101</span>
</code></pre></div></div>
<ul>
  <li><code class="language-plaintext highlighter-rouge">0x101</code>=<code class="language-plaintext highlighter-rouge">0b0000_0000_0000_0000_0000_0001_0000_0001</code></li>
  <li>No rotation of 8-bit values can produce this → invalid</li>
</ul>

<h3 id="what-to-do-with-unrepresentable-constants">What to Do with Unrepresentable Constants?</h3>
<p>Use a <strong>literal pool</strong>.</p>

<p><strong>literal-pool.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">ldr</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">=</span><span class="mh">0x101</span>   <span class="no">@</span> <span class="nv">The</span> <span class="nv">assembler</span> <span class="nv">places</span> <span class="mh">0x101</span> <span class="nv">nearby</span> <span class="nv">and</span> <span class="nv">loads</span> <span class="nv">it</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>
<p>Compile:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
    <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
    <span class="nt">-nostdlib</span> <span class="se">\</span>
    literal-pool.s <span class="se">\</span>
    <span class="nt">-o</span> literal-pool.elf
</code></pre></div></div>
<p>Disassembled:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-objdump <span class="nt">-D</span> literal-pool.elf
00010000 &lt;_start&gt;:
   10000:       e51f0000        ldr     r0, <span class="o">[</span>pc, <span class="c">#-0]   @ 10008 &lt;_start+0x8&gt;</span>
</code></pre></div></div>
<blockquote>
  <p>We’ll explore literal pools in more detail in a future post.</p>
</blockquote>

<h4 id="example-in-c-when-constants-cant-be-encoded">Example in C: When Constants Can’t Be Encoded</h4>

<p><strong>invalid-imm.c</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="mh">0x101</span><span class="p">;</span>
  <span class="k">return</span> <span class="n">x</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Compile:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="nt">-fomit-frame-pointer</span> <span class="nt">-nostdlib</span> <span class="nt">-S</span> invalid-imm.c
</code></pre></div></div>

<p>Assembly output:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">main</span><span class="err">:</span>
  <span class="nb">sub</span> <span class="nv">sp</span><span class="o">,</span> <span class="nv">sp</span><span class="o">,</span> <span class="o">#</span><span class="mi">8</span>
  <span class="nb">ldr</span> <span class="nv">r3</span><span class="o">,</span> <span class="mf">.</span><span class="nv">L3</span>
  <span class="err">@...</span>
<span class="err">.</span><span class="nl">L3</span><span class="err">:</span>
  <span class="err">.</span><span class="nb">word</span> <span class="mi">257</span>
</code></pre></div></div>
<p>The constant is stored in a literal pool and loaded with <code class="language-plaintext highlighter-rouge">ldr</code>.</p>

<h2 id="why-use-this-encoding">Why Use This Encoding?</h2>
<p>If ARM allowed 32-bit immediate constants in every instruction, the instruction size would need to increase to 64 bits.
That would violate the compact RISC design of ARM.</p>

<p>Most common constants in real code are:</p>

<ul>
  <li>Small values (0, 1, 255)</li>
  <li>Bitmasks (0xff00, 0x8000)</li>
  <li>Address offsets</li>
</ul>

<p>These are often representable via rotate encoding.
The result is <strong>compact, expressive, and efficient</strong>.</p>

<h2 id="why-do-shifter-operands-exist">Why Do Shifter Operands Exist?</h2>
<p>ARM instructions pass the second operand through a <strong>Barrel Shifter</strong> before going to the ALU.</p>

<figure>
  <img src="/assets/img/arm-barrel-shifter.png" style="display: block; margin: auto;" alt="ARM Barrel Shifter Diagram" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">
A partial block diagram of the ARM7TDMI. The second operand is always passed through a <b>Barrel Shifter</b> before entering the ALU. This allows shift and arithmetic/logic operations to happen in a single instruction.
  </figcaption>
</figure>

<p>This hardware design allows:</p>

<ul>
  <li>Fewer instructions</li>
  <li>Higher code density</li>
  <li>Better performance</li>
</ul>

<hr />
<p>This concludes our deep dive into shifter operands in ARM assembly.
Let me know in the comments if you want a breakdown of shift operation opcodes or internal instruction encoding!</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="shifter-operand" /><summary type="html"><![CDATA[In this post, we’ll take a complete look at Shifter Operands used in ARM’s Data Processing Instructions. Shifter operands are not optional—they are fundamental components of all data processing instructions.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #4 - How to Rotate Bits with ROR and RRX</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/17/arm-rotate-shift.html" rel="alternate" type="text/html" title="ARM Assembly #4 - How to Rotate Bits with ROR and RRX" /><published>2025-09-17T13:40:00+00:00</published><updated>2025-09-17T13:40:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/17/arm-rotate-shift</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/17/arm-rotate-shift.html"><![CDATA[<p>In this post, we’ll explore the final topic in ARM’s shift operations — <strong>Rotate Shift</strong>.<br />
The ARMv4 architecture provides two rotate instructions: <code class="language-plaintext highlighter-rouge">ror</code> (Rotate Right) and <code class="language-plaintext highlighter-rouge">rrx</code> (Rotate Right with eXtend).</p>

<p>Unlike logical or arithmetic shifts, rotate instructions <strong>wrap the shifted-out bits around and insert them back</strong> on the other side.</p>

<h2 id="rotate-shift-instructions-in-arm">Rotate Shift Instructions in ARM</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Instruction</th>
      <th style="text-align: center">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ror</code></td>
      <td style="text-align: center">Rotate Right</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">rrx</code></td>
      <td style="text-align: center">Rotate Right with eXtend</td>
    </tr>
  </tbody>
</table>

<p>Just like other shift operators, <code class="language-plaintext highlighter-rouge">ror</code> accepts either an immediate or a register for the shift amount:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">ror #&lt;imm&gt;</code>: rotates right by an immediate value</li>
  <li><code class="language-plaintext highlighter-rouge">ror &lt;Rs&gt;</code>: rotates right by the value in a register</li>
</ul>

<p>On the other hand, <code class="language-plaintext highlighter-rouge">rrx</code> does <strong>not</strong> take any operand and <strong>always rotates by 1 bit</strong>, using the <strong>Carry flag in CPSR</strong> to extend the operation to 33 bits.</p>

<h2 id="ror-rotate-right">ROR: Rotate Right</h2>
<p>The <code class="language-plaintext highlighter-rouge">ror</code> instruction shifts all bits to the right, and the bits shifted out from the LSB side are <strong>re-inserted into the MSB side</strong>.<br />
In other words, it’s a <strong>bit-preserving, circular move</strong>.</p>

<p><strong>ror.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x96</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">ror</span> <span class="o">#</span><span class="mi">4</span>
</code></pre></div></div>

<p>Here, <code class="language-plaintext highlighter-rouge">R0 = 0x96</code> → <code class="language-plaintext highlighter-rouge">0b0000...1001_0110</code>
After <code class="language-plaintext highlighter-rouge">ror #4</code>, the lower 4 bits <code class="language-plaintext highlighter-rouge">0110</code> are wrapped around and placed at the MSB:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before:   0b0000_0000_0000_0000_0000_0000_1001_0110 = 0x96
After:    0b0110_0000_0000_0000_0000_0000_0000_1001 = 0x60000009
</code></pre></div></div>

<h2 id="rrx-rotate-right-with-extend">RRX: Rotate Right with Extend</h2>
<p><code class="language-plaintext highlighter-rouge">rrx</code> behaves similarly to <code class="language-plaintext highlighter-rouge">ror #1</code>, but with one major difference — it includes the <strong>CPSR’s Carry flag</strong> and performs a <strong>33-bit rotation</strong>:</p>

<ul>
  <li>The LSB is shifted into the <strong>Carry flag</strong></li>
  <li>The current Carry flag value is shifted into the <strong>MSB</strong></li>
</ul>

<p><strong>rrx.s</strong></p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x69</span>
  <span class="nb">movs</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">rrx</span>
  <span class="nb">movs</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="nb">rrx</span>
  <span class="nb">b</span> <span class="mf">.</span>
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Initial:   0b0000_0000_0000_0000_0000_0000_0110_1001 = 0x69

[rrx/C=0]: 0b0000_0000_0000_0000_0000_0000_0011_0100 -&gt; R1=0x34, C=1
[rrx/C=1]: 0b1000_0000_0000_0000_0000_0000_0001_1010 -&gt; R2=0x8000001a, C=0
</code></pre></div></div>

<h3 id="checking-carry-flag-in-gdb">Checking Carry Flag in GDB</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="nt">-nostdlib</span> rrx.s <span class="nt">-o</span> rrx.elf
<span class="nv">$ </span>qemu-system-arm <span class="nt">-machine</span> versatilepb <span class="nt">-nographic</span> <span class="nt">-S</span> <span class="nt">-s</span> <span class="nt">-kernel</span> rrx.elf
<span class="nv">$ </span>gdb-multiarch rrx.elf
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="o">(</span>gdb<span class="o">)</span> target remote :1234
<span class="o">(</span>gdb<span class="o">)</span> si                          <span class="c"># Executes: mov r0, #0x69</span>
0x00010004 <span class="k">in </span>_start <span class="o">()</span>
<span class="o">(</span>gdb<span class="o">)</span> i r r0
r0             0x69     105
<span class="o">(</span>gdb<span class="o">)</span> p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 29<span class="o">)</span> &amp; 1         <span class="c"># Check initial Carry Flag</span>
<span class="nv">$3</span> <span class="o">=</span> 0
<span class="o">(</span>gdb<span class="o">)</span> si                          <span class="c"># Executes: movs r1, r0, rrx</span>
0x00010008 <span class="k">in </span>_start <span class="o">()</span>
<span class="o">(</span>gdb<span class="o">)</span> p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 29<span class="o">)</span> &amp; 1
<span class="hll"><span class="nv">$4</span> <span class="o">=</span> 1
</span><span class="o">(</span>gdb<span class="o">)</span> i r r1
r1             0x34     52
<span class="o">(</span>gdb<span class="o">)</span> si                          <span class="c"># Executes: movs r2, r1, rrx</span>
0x0001000c <span class="k">in </span>_start <span class="o">()</span>
<span class="o">(</span>gdb<span class="o">)</span> p <span class="o">(</span><span class="nv">$cpsr</span> <span class="o">&gt;&gt;</span> 29<span class="o">)</span> &amp; 1
<span class="hll"><span class="nv">$5</span> <span class="o">=</span> 0
</span><span class="o">(</span>gdb<span class="o">)</span> i r r0 r1 r2
r0             0x69     105
r1             0x34     52
r2             0x8000001a       <span class="nt">-2147483622</span></code></pre></figure>

<h2 id="why-is-there-no-rotate-left-rol">Why is there no Rotate Left (ROL)?</h2>
<p>ARM does not include a ROL instruction. Why?
Because you can easily <strong>simulate rotate-left</strong> using a combination of <code class="language-plaintext highlighter-rouge">lsl</code> and <code class="language-plaintext highlighter-rouge">ror</code>.</p>

<h3 id="simulated-rol-using-lsl-and-ror">Simulated ROL using LSL and ROR</h3>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="err">@</span> <span class="nv">ROL</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="nv">n</span>
  <span class="nb">lsl</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="nv">n</span>           <span class="no">@</span> <span class="nv">Logical</span> <span class="nv">sfhit</span> <span class="nv">left</span>
  <span class="nb">ror</span> <span class="nv">r3</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#(</span><span class="mi">32</span> <span class="o">-</span> <span class="nv">n</span><span class="o">)</span>    <span class="no">@</span> <span class="nv">Rotate</span> <span class="nv">right</span> <span class="nv">the</span> <span class="nv">remaining</span> <span class="nv">bits</span>
  <span class="nb">orr</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r3</span>           <span class="no">@</span> <span class="nv">Combine</span> <span class="nv">both</span> <span class="nv">halves</span>
</code></pre></div></div>

<p>For example, if <code class="language-plaintext highlighter-rouge">r0 = 0x80000001</code> and <code class="language-plaintext highlighter-rouge">n=1</code>:</p>
<ul>
  <li>LSL → <code class="language-plaintext highlighter-rouge">0x00000002</code></li>
  <li>ROR(31) → <code class="language-plaintext highlighter-rouge">0x40000000</code></li>
  <li>ORR → <code class="language-plaintext highlighter-rouge">0x40000002</code> (Valid rotate-left result)</li>
</ul>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">orr</code> performs bitwise OR between the two shifted values.</p>
</blockquote>

<h2 id="summary">Summary</h2>
<p>In this post, we’ve covered ARM’s rotate shift instructions:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ror</code>: circular right shift that preserves all bits</li>
  <li><code class="language-plaintext highlighter-rouge">rrx</code>: 1-bit circular shift with carry extension</li>
  <li><code class="language-plaintext highlighter-rouge">rol</code> is not an instruction but can be emulated using <code class="language-plaintext highlighter-rouge">lsl</code> + <code class="language-plaintext highlighter-rouge">ror</code></li>
</ul>

<p>In the next post, we’ll explore how these shift operations integrate with real arithmetic instructions (<code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">sub</code>) and how they affect <strong>Carry and Overflow</strong> flags.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="ror" /><category term="rrx" /><category term="arm" /><category term="assembly" /><category term="rotate shift" /><summary type="html"><![CDATA[In this post, we’ll explore the final topic in ARM’s shift operations — Rotate Shift. The ARMv4 architecture provides two rotate instructions: ror (Rotate Right) and rrx (Rotate Right with eXtend).]]></summary></entry><entry xml:lang="en"><title type="html">Understanding Carry in Arithmetic</title><link href="https://seojuncha.github.io/computer/arithmetic/2025/09/16/what-is-carry.html" rel="alternate" type="text/html" title="Understanding Carry in Arithmetic" /><published>2025-09-16T02:45:00+00:00</published><updated>2025-09-16T02:45:00+00:00</updated><id>https://seojuncha.github.io/computer/arithmetic/2025/09/16/what-is-carry</id><content type="html" xml:base="https://seojuncha.github.io/computer/arithmetic/2025/09/16/what-is-carry.html"><![CDATA[<p>Carry is a fundamental concept in computer arithmetic. It ensures the accuracy of operations and plays a critical role in hardware and software design, debugging, and advanced algorithm optimization. Especially in systems with limited bit space, understanding how Carry works is essential. In this article, we will explore <strong>the definition and concept of Carry</strong>, <strong>how it works in binary and decimal arithmetic</strong>, and <strong>how programming languages handle Carry</strong>.</p>

<p>Example: When unsigned int exceeds its maximum range:</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="mi">4294967295</span><span class="p">;</span>   <span class="cm">/* maximum value (32-bit unsigned integer) */</span>
<span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">b</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
<span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">c</span> <span class="o">=</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="p">;</span>        <span class="cm">/* Overflow: c has 0 */</span>
</code></pre></div></div>

<h2 id="what-is-carry">What is Carry?</h2>
<p>Let’s start with the concept of carrying in decimal addition:</p>

\[5 + 4 = 9\]

<p>No carry is generated when <code class="language-plaintext highlighter-rouge">5 + 4 = 9</code>, as the result fits within a single digit. Carry occurs when the sum exceeds the value that a single digit can represent. For example:</p>

\[9 + 5 = 14 \; (Carry)\]

<p>Here, <code class="language-plaintext highlighter-rouge">9 + 5</code> results in <code class="language-plaintext highlighter-rouge">14</code>, where the ones place overflows into the tens place.</p>

<p>The same principle applies to binary addition. In binary, the maximum value a single digit can represent is 1. When adding <code class="language-plaintext highlighter-rouge">1 + 1</code>, carry is generated:</p>

\[\begin{aligned}
&amp;0 + 0 = 0  \; (No Carry) \newline
&amp;1 + 0 = 1  \; (No Carry) \newline
&amp;1 + 1 = 10 \; (Carry)
\end{aligned}\]

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1 + 1 = 10 (Carry generated)
=&gt; In binary addition, carry is handled as a 1 passed to the next higher bit.
</code></pre></div></div>

<p>Carry is important because it <strong>occurs when the sum exceeds the capacity of a fixed-width space</strong>. For example, in a 4-bit memory, the maximum binary value <code class="language-plaintext highlighter-rouge">1111</code> overflows to <code class="language-plaintext highlighter-rouge">10000</code>.</p>

<h2 id="carry-in-and-carry-out">Carry in and Carry out</h2>
<p><strong><em>Carry in</em></strong> is the carry value passed from a lower bit operation, and <strong><em>Carry out</em></strong> is the carry value resulting from the current bit operation. This process is typically handled by a hardware <a href="https://en.wikipedia.org/wiki/Adder_(electronics)#Full_adder">Full Adder</a> circuit.</p>

<p><em>Carry in</em> ensures continuity and accuracy in multi-bit operations.</p>

<p><strong>Example</strong>: Adding <code class="language-plaintext highlighter-rouge">13</code> and <code class="language-plaintext highlighter-rouge">7</code> in a <code class="language-plaintext highlighter-rouge">4-bit</code> space</p>

<figure class="highlight"><pre><code class="language-text" data-lang="text"><span class="hll">carry in   :  1   1   1   1   0
</span>13 (1101)  :      1   1   0   1
7  (0111)  :      0   1   1   1
--------------------------------
sum        :  1   0   1   0   0
<span class="hll">carry out  :  0   1   1   1   1
</span></code></pre></figure>

<p>Carry occurs when the sum exceeds the capacity of a bit. The result here is <code class="language-plaintext highlighter-rouge">13 + 7 = 20 (0b10100)</code>.</p>

<h2 id="carry-in-arithmetic-operations">Carry in Arithmetic Operations</h2>
<p>Let’s analyze carry in basic arithmetic operations like addition and subtraction.</p>

<h3 id="binary-addition">Binary Addition</h3>
<p>Addition in binary operates similarly regardless of whether the numbers are signed or unsigned.</p>

<p><strong>Example 1:</strong> Unsigned <code class="language-plaintext highlighter-rouge">3 + 9</code> in 4-bit space</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  0011
+ 1001
------
  1100  = 12 in Decimal
</code></pre></div></div>

<p><strong>Example 2:</strong> Signed <code class="language-plaintext highlighter-rouge">3 + 2</code> in 4-bit space</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  0011
+ 0010
------
  0101  = 5 in Decimal
</code></pre></div></div>
<blockquote>
  <p>For signed integers in 4-bit space, the maximum positive value is 7. Results exceeding this will be discussed in the later post.</p>
</blockquote>

<h3 id="binary-subtraction">Binary Subtraction</h3>
<p>In computers, subtraction is performed by adding the two’s complement of the subtrahend.</p>

\[7 - 3 = 7 + (-3)\]

<p>For example, in 4-bit space:<br />
Represent <code class="language-plaintext highlighter-rouge">-3</code> as <code class="language-plaintext highlighter-rouge">two's complement of 3</code>:</p>
<ul>
  <li>3 = 0011</li>
  <li>-3 = 1101</li>
</ul>

<p><strong>Example 1:</strong> <code class="language-plaintext highlighter-rouge">7 - 3</code></p>
<ul>
  <li>7 = 0111</li>
  <li>3 = 0011
    <ul>
      <li>-3 = 1101</li>
    </ul>
  </li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  0111   (7 in binary)
+ 1101   (-3 in binary)
-------
 10100  = 4 (ignore carry)
</code></pre></div></div>
<p><strong>Example 2:</strong> <code class="language-plaintext highlighter-rouge">3 - 7</code></p>
<ul>
  <li>3 = 0011</li>
  <li>7 = 0111
    <ul>
      <li>-7 = 1001</li>
    </ul>
  </li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  0011   (3 in binary)
+ 1001   (-7 in binary)
-------
  1100  = (interpret based on signed/unsigned)
</code></pre></div></div>
<p>For unsigned integers:</p>
<ul>
  <li>1100 = 12</li>
</ul>

<p>For signed integers:</p>
<ul>
  <li>1100 = -4</li>
</ul>

<h2 id="carry-in-shift-operations">Carry in Shift Operations</h2>
<p>Carry is also relevant in shift operations, which move bits left or right. The behavior depends on the direction of the shift:</p>
<ul>
  <li><strong>Left Shift</strong>: Carry occurs if the MSB is exceeded.</li>
  <li><strong>Right Shift</strong>: Carry occurs if the LSB is exceeded.</li>
</ul>

<p>Example of Left Shift:<br />
Left shifts are used to double the value of a binary number, while right shifts are used to halve it. However, without considering the carry, data loss may occur.</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">value</span> <span class="o">=</span> <span class="mb">0b0111</span>           <span class="c1"># 7 in binary
</span><span class="n">left_shift</span> <span class="o">=</span> <span class="n">value</span> <span class="o">&lt;&lt;</span> <span class="mi">2</span>  <span class="c1"># Shift left by 2 bits
</span><span class="n">carry</span> <span class="o">=</span> <span class="n">left_shift</span> <span class="o">&gt;&gt;</span> <span class="mi">4</span>  <span class="c1"># Extract carry from MSB
</span><span class="nf">print</span><span class="p">(</span><span class="nf">bin</span><span class="p">(</span><span class="n">carry</span><span class="p">))</span>        <span class="c1"># Outputs: 0b1
</span></code></pre></div></div>

<p>Left Shift Example (4-bit):</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0111 &lt;&lt; 1 = (0)1110 (No Carry)
0111 &lt;&lt; 2 = (1)1100 (Carry)
</code></pre></div></div>

<p>Right Shift Example (4-bit):</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0110 &gt;&gt; 1 = 0011(0)  (No Carry)
0110 &gt;&gt; 2 = 0001(1)  (Carry)
</code></pre></div></div>

<h3 id="utilizing-carry-in-shift-operations">Utilizing Carry in Shift Operations</h3>
<p>The carry in shift operations is often used in tasks such as circular data shifts (Circular Shift) or <a href="https://en.wikipedia.org/wiki/Cyclic_redundancy_check">CRC (Cyclic Redundancy Check)</a> calculations.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">circular_left_shift</span><span class="p">(</span><span class="n">value</span><span class="p">,</span> <span class="n">bits</span><span class="p">,</span> <span class="n">size</span><span class="p">):</span>
    <span class="nf">return </span><span class="p">((</span><span class="n">value</span> <span class="o">&lt;&lt;</span> <span class="n">bits</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">value</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">size</span> <span class="o">-</span> <span class="n">bits</span><span class="p">)))</span> <span class="o">&amp;</span> <span class="p">((</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="n">size</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span>
</code></pre></div></div>

<h2 id="programming-examples">Programming Examples</h2>

<h3 id="example-1-detecting-carry-in-python">Example 1: Detecting Carry in Python</h3>
<p>Here’s a Python function to detect carry:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">has_carry</span><span class="p">(</span><span class="n">value</span><span class="p">,</span> <span class="n">bits</span><span class="p">):</span>
  <span class="sh">"""</span><span class="s">
  Detects if carry occurs beyond a given bit width.
  </span><span class="sh">"""</span>
  <span class="k">return</span> <span class="nf">bool</span><span class="p">(</span><span class="n">value</span> <span class="o">&amp;</span> <span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="n">bits</span><span class="p">))</span>
</code></pre></div></div>

<p>Example:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a</span><span class="p">,</span> <span class="n">b</span> <span class="o">=</span> <span class="mi">13</span><span class="p">,</span> <span class="mi">5</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">a</span><span class="si">:</span><span class="mi">05</span><span class="n">b</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">b</span><span class="si">:</span><span class="mi">05</span><span class="n">b</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="si">:</span><span class="mi">05</span><span class="n">b</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="nf">has_carry</span><span class="p">(</span><span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="p">,</span> <span class="mi">4</span><span class="p">))</span>
<span class="nf">print</span><span class="p">(</span><span class="nf">has_carry</span><span class="p">(</span><span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="p">,</span> <span class="mi">5</span><span class="p">))</span>
</code></pre></div></div>

<p>Output:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>01101
00101
10010
True
False
</code></pre></div></div>

<h3 id="example-2-unsigned-integer-carry-in-c">Example 2: Unsigned Integer Carry in C</h3>
<p>This C example demonstrates how carry affects unsigned integers:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdint.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">uint8_t</span> <span class="n">ui8</span> <span class="o">=</span> <span class="n">UINT8_MAX</span><span class="p">;</span> <span class="c1">// 8-bit max value = 255</span>
  <span class="kt">uint8_t</span> <span class="n">ui8_plus_one</span> <span class="o">=</span> <span class="n">UINT8_MAX</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>  <span class="c1">// Carry wraps to 0</span>
  <span class="kt">uint8_t</span> <span class="n">ui8_plus_two</span> <span class="o">=</span> <span class="n">UINT8_MAX</span> <span class="o">+</span> <span class="mi">2</span><span class="p">;</span>  <span class="c1">// Carry wraps to 1</span>

  <span class="kt">uint16_t</span> <span class="n">ui16</span> <span class="o">=</span> <span class="n">UINT8_MAX</span><span class="p">;</span>  <span class="c1">// Expand to 16-bit</span>
  <span class="kt">uint16_t</span> <span class="n">ui16_plus_one</span> <span class="o">=</span> <span class="n">UINT8_MAX</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>  <span class="c1">// 256</span>
  <span class="kt">uint16_t</span> <span class="n">ui16_plus_two</span> <span class="o">=</span> <span class="n">UINT8_MAX</span> <span class="o">+</span> <span class="mi">2</span><span class="p">;</span>  <span class="c1">// 257</span>

  <span class="n">printf</span><span class="p">(</span><span class="s">"8-bit Memory</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"  %u [0x%02X]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui8</span><span class="p">,</span> <span class="n">ui8</span><span class="p">);</span>  <span class="c1">// 255</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"  %u [0x%02X]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui8_plus_one</span><span class="p">,</span> <span class="n">ui8_plus_one</span><span class="p">);</span>  <span class="c1">// 0</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"  %u [0x%02X]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui8_plus_two</span><span class="p">,</span> <span class="n">ui8_plus_two</span><span class="p">);</span>  <span class="c1">// 1</span>

  <span class="n">printf</span><span class="p">(</span><span class="s">"16-bit Memory</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"  %u [0x%04X]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui16</span><span class="p">,</span> <span class="n">ui16</span><span class="p">);</span>  <span class="c1">// 255</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"  %u [0x%04X]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui16_plus_one</span><span class="p">,</span> <span class="n">ui16_plus_one</span><span class="p">);</span>  <span class="c1">// 256</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"  %u [0x%04X]</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ui16_plus_two</span><span class="p">,</span> <span class="n">ui16_plus_two</span><span class="p">);</span>  <span class="c1">// 257</span>

  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Output:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>8-bit Memory
  255 [0xFF]
  0 [0x00]
  1 [0x01]
16-bit Memory
  255 [0x00FF]
  256 [0x0100]
  257 [0x0101]
</code></pre></div></div>
<p>In this case, the compiler makes the warning message.</p>

<p>Compiler Warning:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>carry.c: In function ‘main’:
carry.c:6:26: warning: unsigned conversion from ‘int’ to ‘uint8_t’ {aka ‘unsigned char’} changes value from ‘256’ to ‘0’ [-Woverflow]
    6 |   uint8_t ui8_plus_one = UINT8_MAX + 1;
      |                          ^~~~~~~~~
carry.c:7:26: warning: unsigned conversion from ‘int’ to ‘uint8_t’ {aka ‘unsigned char’} changes value from ‘257’ to ‘1’ [-Woverflow]
    7 |   uint8_t ui8_plus_two = UINT8_MAX + 2;
      |                          ^~~~~~~~~
</code></pre></div></div>

<h2 id="conclusion">Conclusion</h2>
<p>Carry is not just a small detail in arithmetic—it is one of the <strong>core mechanisms that guarantee correctness in binary operations</strong>.<br />
It appears in almost every low-level system, from simple addition inside a CPU to advanced cryptographic algorithms and error-detection methods like CRC.</p>

<p>By understanding Carry, developers and engineers can:</p>
<ul>
  <li>Debug arithmetic bugs more effectively</li>
  <li>Write safer low-level code for embedded systems and compilers</li>
  <li>Optimize algorithms that rely on precise bit-level operations</li>
</ul>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="computer" /><category term="arithmetic" /><category term="carry" /><category term="binary operation" /><category term="arithmetic operation" /><category term="binary arithmetic" /><summary type="html"><![CDATA[Carry is a fundamental concept in computer arithmetic. It ensures the accuracy of operations and plays a critical role in hardware and software design, debugging, and advanced algorithm optimization. Especially in systems with limited bit space, understanding how Carry works is essential. In this article, we will explore the definition and concept of Carry, how it works in binary and decimal arithmetic, and how programming languages handle Carry.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #3 - Arithmetic Shift (ASR) and Sign Preservation</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/15/arm-arithmetic-shift.html" rel="alternate" type="text/html" title="ARM Assembly #3 - Arithmetic Shift (ASR) and Sign Preservation" /><published>2025-09-15T12:53:00+00:00</published><updated>2025-09-15T12:53:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/15/arm-arithmetic-shift</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/15/arm-arithmetic-shift.html"><![CDATA[<p>One of the key components of the CPU is the ALU — the Arithmetic Logic Unit. As the name suggests, it handles both logical and arithmetic operations.</p>

<p>In arithmetic operations, it’s important to consider whether a number is signed or unsigned. In the <a href="/arm/assembly/tutorial/2025/09/09/arm-logical-shift.html">previous post</a>, we explored logical shifts for bit-level manipulation.<br />
This time, we’ll look at arithmetic right shifts using the <code class="language-plaintext highlighter-rouge">asr</code> instruction and how they preserve the sign of a number.</p>

<h2 id="arithmetic-shift-in-arm">Arithmetic Shift in ARM</h2>

<p>ARM provides a single instruction for arithmetic shift:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Instruction</th>
      <th style="text-align: left">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">asr</code></td>
      <td style="text-align: left">Arithmetic Shift Right</td>
    </tr>
  </tbody>
</table>

<p>You can use <code class="language-plaintext highlighter-rouge">asr</code> in two ways:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">asr #&lt;imm&gt;</code>: Shift by an immediate value</li>
  <li><code class="language-plaintext highlighter-rouge">asr &lt;Rs&gt;</code>: Shift by the value in a register</li>
</ul>

<h3 id="how-sign-is-preserved">How Sign Is Preserved</h3>
<p>To preserve the sign of a number during an arithmetic shift,<br />
<strong>“The shift-in bits are filled with the value of the original sign bit.”</strong></p>

<p>That means:</p>
<ul>
  <li>If the most significant bit (MSB) is <code class="language-plaintext highlighter-rouge">0</code>, the shifted-in bits become <code class="language-plaintext highlighter-rouge">0</code></li>
  <li>If the MSB is <code class="language-plaintext highlighter-rouge">1</code>, the shifted-in bits become <code class="language-plaintext highlighter-rouge">1</code></li>
</ul>

<p><strong>Case: MSB is 0</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before (32-bit):  
0 1 0 1 1 1 0 0 ...  

ASR #3 →  

After (32-bit):  
0 0 0 0 1 0 1 1 ...
^ ^ ^  
| | |  
+---- Sign-preserving fill: 0
</code></pre></div></div>

<p><strong>Case: MSB is 1</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before (32-bit):  
1 0 1 0 0 0 1 1 ...

ASR #3 →  

After (32-bit):  
1 1 1 1 0 1 0 ...
^ ^ ^  
| | |  
+---- Sign-preserving fill: 1
</code></pre></div></div>

<h3 id="asr-instruction-example">ASR Instruction Example</h3>

<p><strong>asr.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#-</span><span class="mi">32</span>         <span class="no">@</span> <span class="o">-</span><span class="mi">32</span> <span class="o">=</span> <span class="mh">0xffffffe0</span>
</span><span class="hll">  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">asr</span> <span class="o">#</span><span class="mi">1</span>   <span class="no">@</span> <span class="nv">R1</span> <span class="o">=</span> <span class="mh">0xfffffff0</span> <span class="o">=</span> <span class="o">-</span><span class="mi">16</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p><code class="language-plaintext highlighter-rouge">-32</code> is represented as <code class="language-plaintext highlighter-rouge">0xffffffe0</code> in hexadecimal.</p>

<p>After a 1-bit arithmetic shift right:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0xffffffe0 &gt;&gt; 1 = 0xfffffff0 (-16)
</code></pre></div></div>
<p>This is equivalent to dividing by 2, while preserving the sign:<br />
<code class="language-plaintext highlighter-rouge">-32 / 2 = -16</code></p>

<h2 id="why-only-right-arithmetic-shift">Why Only Right Arithmetic Shift?</h2>
<p>There is no left arithmetic shift because <strong>left shifting changes the MSB</strong>, and that may be intentional.<br />
In other words, it doesn’t make sense to “preserve the sign” when you’re introducing new bits into the sign position.</p>

<p><strong>left-shift-for-negative.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mh">0x40000000</span>
</span><span class="hll">  <span class="nb">movs</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">1</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<p>Shifting <code class="language-plaintext highlighter-rouge">0x40000000</code> left by 1 gives <code class="language-plaintext highlighter-rouge">0x80000000</code>. Here’s the breakdown:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Binary</th>
      <th style="text-align: center">Hex</th>
      <th style="text-align: center">Decimal</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">0b0100…</td>
      <td style="text-align: center">0x40000000</td>
      <td style="text-align: center">1073741824</td>
    </tr>
    <tr>
      <td style="text-align: center">0b1000…</td>
      <td style="text-align: center">0x80000000</td>
      <td style="text-align: center">2147483648 or -2147483648</td>
    </tr>
  </tbody>
</table>

<p>This result can be interpreted as either positive or negative, depending on whether you’re treating it as a signed or unsigned integer.</p>

<p>To help the programmer decide, the ARM CPU sets the <strong>N (Negative)</strong> flag in the CPSR register when the result has MSB = 1.<br />
This lets you check the CPSR and decide how to interpret the result.</p>

<blockquote>
  <p>Later, we’ll also explore the <strong>V (Overflow)</strong> flag used in arithmetic operations like <code class="language-plaintext highlighter-rouge">add</code> and <code class="language-plaintext highlighter-rouge">sub</code>.
But for now, since we used <code class="language-plaintext highlighter-rouge">lsl</code>, only the Negative flag is relevant.
We’ll discuss flags in more detail in upcoming posts on arithmetic instructions.</p>
</blockquote>

<figure style="text-align: center;">
  <img src="/assets/img/non-add-sub-v-left-unchanged.png" alt="ARM manual: overflow flag not updated on logical shift" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">The ARM manual states that overflow flags are generally not updated for non-add/sub operations like logical shifts.</figcaption>
</figure>

<p>In short: left shifts in ARM are always logical (<code class="language-plaintext highlighter-rouge">lsl</code>).<br />
There’s no such thing as an “arithmetic left shift” because it doesn’t make sense to preserve the sign when the sign bit itself is being shifted.</p>

<h2 id="checking-flags-in-gdb">Checking Flags in GDB</h2>
<p>Let’s compile and run the <code class="language-plaintext highlighter-rouge">left-shift-for-negative.s</code> example and check the CPSR flags in GDB.</p>

<h3 id="1-compile">1. Compile</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>arm-none-eabi-gcc <span class="se">\</span>
    <span class="nt">-Ttext</span><span class="o">=</span>0x10000 <span class="se">\</span>
    <span class="nt">-nostdlib</span> <span class="se">\</span>
    left-shift-for-negative.s <span class="se">\</span>
    <span class="nt">-o</span> left-shift-for-negative.elf
</code></pre></div></div>
<h4 id="2-ruin-in-qemu">2. Ruin in QEMU</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
    <span class="nt">-machine</span> versatilepb <span class="se">\</span>
    <span class="nt">-nographic</span> <span class="se">\</span>
    <span class="nt">-S</span> <span class="se">\</span>
    <span class="nt">-s</span> <span class="se">\</span>
    <span class="nt">-kernel</span> left-shift-for-negative.elf
</code></pre></div></div>
<h4 id="3-connect-gdb">3. Connect GDB</h4>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ gdb-multiarch left-shift-for-negative.elf
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-gdb" data-lang="gdb">(gdb) target remote :1234
0x00010000 in _start ()    
(gdb) si                   # mov r0, #0x40000000 
0x00010004 in _start ()    
(gdb) p ($cpsr &gt;&gt; 31) &amp; 1
$2 = 0
<span class="hll">(gdb) si                   # movs r1, r0, lsl #1
</span>0x00010008 in _start ()
<span class="hll">(gdb) p ($cpsr &gt;&gt; 31) &amp; 1
</span><span class="hll">$2 = 1
</span></code></pre></figure>

<p>The second instruction(<code class="language-plaintext highlighter-rouge">movs r1, r0, lsl #1</code>) set the N (Negative) flag because the result of the shift was interpreted as a negative number.</p>

<h2 id="summary">Summary</h2>
<p>In this post, we learned:</p>

<ul>
  <li>How to use the <code class="language-plaintext highlighter-rouge">asr</code> instruction for arithmetic right shifts</li>
  <li>Why there is no <code class="language-plaintext highlighter-rouge">asl</code> (arithmetic left shift)</li>
  <li>How to check the Negative flag using GDB</li>
</ul>

<p>In the next chapter, we’ll explore the final type of shift:
rotate right (<code class="language-plaintext highlighter-rouge">ror</code>) and rotate with extend (<code class="language-plaintext highlighter-rouge">rrx</code>), which are used to rotate bits circularly.</p>

<p>Let me know if you found any mistakes or suggestions for improvement!</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="Assembly" /><category term="ASR" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[One of the key components of the CPU is the ALU — the Arithmetic Logic Unit. As the name suggests, it handles both logical and arithmetic operations.]]></summary></entry><entry xml:lang="en"><title type="html">ARM Assembly #2 - Understanding Logical Shifts (LSL, LSR)</title><link href="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/09/arm-logical-shift.html" rel="alternate" type="text/html" title="ARM Assembly #2 - Understanding Logical Shifts (LSL, LSR)" /><published>2025-09-09T15:13:00+00:00</published><updated>2025-09-09T15:13:00+00:00</updated><id>https://seojuncha.github.io/arm/assembly/tutorial/2025/09/09/arm-logical-shift</id><content type="html" xml:base="https://seojuncha.github.io/arm/assembly/tutorial/2025/09/09/arm-logical-shift.html"><![CDATA[<p>Computers frequently perform bit-level operations to manipulate data, and <strong>shift operations</strong> are one of the most fundamental among them. In this post, we’ll focus on <strong>Logical Shifts</strong> in ARM assembly.</p>

<p>Rather than going deep into abstract theory, we’ll walk through simple examples that show how logical shift works in practice. We’ll also look at how the <strong>Carry Flag</strong> (C flag) is affected by these operations.</p>

<blockquote>
  <p>You can find the example code on the <a href="https://github.com/seojuncha/arm-assembly-tutorial/tree/main/02_moving-data-around">GitHub repository</a>.</p>
</blockquote>

<h2 id="what-is-a-shift-operand">What is a shift operand?</h2>

<p>In ARM assembly, instructions like <code class="language-plaintext highlighter-rouge">mov</code>, <code class="language-plaintext highlighter-rouge">add</code>, and <code class="language-plaintext highlighter-rouge">sub</code> allow one of the operands to be shifted on the fly before being used.</p>

<p>This is called a <strong>shift operand</strong>.</p>

<p>Thanks to this feature, you can perform shift and assignment in a single instruction, making the code more concise.</p>

<figure style="text-align: center;">
  <img src="/assets/img/alut-shifter-usage.png" alt="Explanation of how ARM instructions control both the ALU and the shifter to maximize efficiency" style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">ARM data-processing instructions allow simultaneous use of the Arithmetic Logic Unit (ALU) and the shifter, enabling efficient execution of complex operations in a single instruction cycle.</figcaption>
</figure>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="o">#</span><span class="mi">2</span>   <span class="no">@</span> <span class="nv">Shift</span> <span class="nv">R0</span> <span class="nv">left</span> <span class="nv">by</span> <span class="mi">2</span> <span class="nv">bits</span> <span class="nv">and</span> <span class="nv">store</span> <span class="nv">in</span> <span class="nv">R1</span>
</code></pre></div></div>

<h2 id="what-is-a-logical-shift">What is a logical shift?</h2>
<p>A <strong>logical shift</strong> simply moves the bits to the left or right, filling empty bits with 0.</p>

<p>For example, using 4-bit data <code class="language-plaintext highlighter-rouge">0110</code>:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Left shift:     0110 &lt;&lt; 1 = 1100
Right shift:    0110 &gt;&gt; 1 = 0011
</code></pre></div></div>

<p>ARM provides two logical shift operations:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Instruction</th>
      <th style="text-align: center">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">lsl</code></td>
      <td style="text-align: center">Logical Shift Left</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">lsr</code></td>
      <td style="text-align: center">Logical Shift Right</td>
    </tr>
  </tbody>
</table>

<p>The amount of shift can be specified either by an immediate value or by a register.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">lsl #n</code>, <code class="language-plaintext highlighter-rouge">lsr #n</code> : Immediate shift</li>
  <li><code class="language-plaintext highlighter-rouge">lsl rN</code>, <code class="language-plaintext highlighter-rouge">lsr rN</code> : Register shift</li>
</ul>

<h3 id="example-code-logical-shift-in-action">Example Code: Logical Shift in Action</h3>

<p><strong>logical-shift.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#</span><span class="mi">3</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsl</span> <span class="nv">r0</span>
</span><span class="hll">  <span class="nb">mov</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r1</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<ul>
  <li><code class="language-plaintext highlighter-rouge">mov r0, #3</code> stores 3 in <code class="language-plaintext highlighter-rouge">R0</code></li>
  <li><code class="language-plaintext highlighter-rouge">mov r1, r0, lsl r0</code> shifts <code class="language-plaintext highlighter-rouge">R0</code> left by 3 bits and stores in <code class="language-plaintext highlighter-rouge">R1</code></li>
  <li><code class="language-plaintext highlighter-rouge">mov r2, r1, lsr #1</code> shifts <code class="language-plaintext highlighter-rouge">R1</code> right by 1 bit and stores in <code class="language-plaintext highlighter-rouge">R2</code></li>
</ul>

<blockquote>
  <p>For an introduction to the <code class="language-plaintext highlighter-rouge">mov</code> instruction, please refer to <a href="/arm/assembly/tutorial/2025/09/08/arm-mov-instruction.html">the previous post</a></p>
</blockquote>

<p>Example flow:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>r0 = 0x3 = 0b0...000011

# r1 = r0 &lt;&lt; 3
r1 = 0x18 = 0b0..011000

# r2 = 0x18 &gt;&gt; 1
r2= 0xC = 0b0...001100
</code></pre></div></div>

<h3 id="debugging-with-gdb">Debugging with GDB</h3>
<p>Let’s confirm the behavior using QEMU and GDB.</p>

<h4 id="1-run-qemu">1. Run QEMU</h4>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>qemu-system-arm <span class="se">\</span>
    <span class="nt">-machine</span> versatilepb <span class="se">\</span>
    <span class="nt">-nographic</span> <span class="se">\</span>
    <span class="nt">-S</span> <span class="se">\</span>
    <span class="nt">-s</span> <span class="se">\</span>
    <span class="nt">-kernel</span> logical-shift.elf
</code></pre></div></div>
<h4 id="2-connect-with-gdb">2. Connect with GDB</h4>
<pre><code class="language-gdb">$ gdb-multiarch logical-shift.elf
(gdb) target remote localhost:1234
</code></pre>
<h4 id="3-check-instructions">3. Check instructions</h4>
<pre><code class="language-gdb">(gdb) x/10i 0x10000
</code></pre>
<h4 id="4-check-registers">4. Check registers</h4>
<pre><code class="language-gdb">(gdb) info registers
(gdb) i r r0 r1 r2
</code></pre>
<h4 id="5-step-through">5. Step Through</h4>
<pre><code class="language-gdb">(gdb) stepi
</code></pre>
<p>After each step, check register values again to confirm the effect.</p>

<h2 id="when-is-the-carry-flag-set">When is the Carry Flag set?</h2>
<p>Shift operations push out bits that no longer fit in the range.
One of those bits may be captured in the <strong>Carry Flag (C)</strong> in the <strong>CPSR</strong> register.</p>

<figure style="text-align: center;">
  <img src="/assets/img/cpsr-condition-flags.png" alt="Description of the CPSR register and the four condition flags: Negative, Zero, Carry, and Overflow." style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">The Current Program Status Register (CPSR) holds the processor's condition flags—Negative (N), Zero (Z), Carry (C), and Overflow (V)—which reflect the results of arithmetic and logical operations.</figcaption>
</figure>

<p>By default, instructions like <code class="language-plaintext highlighter-rouge">mov</code> do not update the carry flag.
To update it, use the <code class="language-plaintext highlighter-rouge">s</code> suffix: <code class="language-plaintext highlighter-rouge">movs</code>, <code class="language-plaintext highlighter-rouge">adds</code>, <code class="language-plaintext highlighter-rouge">subs</code>, etc.</p>

<h3 id="example-code-checking-the-carry-flag">Example Code: Checking the Carry Flag</h3>
<p><strong>logical-right-shift-carry.s</strong></p>

<figure class="highlight"><pre><code class="language-armasm" data-lang="armasm">  <span class="err">.</span><span class="nb">text</span>
  <span class="err">.</span><span class="nb">global</span> <span class="nv">_start</span>
<span class="nl">_start</span><span class="err">:</span>
  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="mh">0x1</span>
<span class="hll">  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
</span><span class="hll">  <span class="nb">movs</span> <span class="nv">r2</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
</span>  <span class="nb">b</span> <span class="mf">.</span></code></pre></figure>

<ul>
  <li><code class="language-plaintext highlighter-rouge">mov</code> does <strong>not</strong> affect the Carry flag</li>
  <li><code class="language-plaintext highlighter-rouge">movs</code> updates the CPSR flags including Carry</li>
</ul>

<p>To check the carry flag in GDB:</p>

<pre><code class="language-gdb">(gdb) target remote localhost:1234
(gdb) info registers cpsr
(gdb) p ($cpsr &gt;&gt; 29) &amp; 1
</code></pre>
<p>This will show whether the Carry flag (bit 29) is set.</p>

<figure style="text-align: center;">
  <img src="/assets/img/cpsr-layout.png" alt="CPSR bit layout showing N, Z, C, V flags and mode bits." style="display: block; margin: auto;" />
  <figcaption style="margin-top: 0.5em; font-size: 0.9em; color: #666;">CPSR register layout with condition flags and mode bits.</figcaption>
</figure>

<h2 id="limitations-and-use-cases">Limitations and Use Cases</h2>
<p>Logical shift always fills in <code class="language-plaintext highlighter-rouge">0</code>s. This works well for <strong>unsigned integers</strong>,
but can cause issues with <strong>signed values</strong> where the highest bit represents the sign.</p>

<p>Example:</p>
<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nb">mov</span> <span class="nv">r0</span><span class="o">,</span> <span class="o">#-</span><span class="mi">4</span>
  <span class="nb">mov</span> <span class="nv">r1</span><span class="o">,</span> <span class="nv">r0</span><span class="o">,</span> <span class="nb">lsr</span> <span class="o">#</span><span class="mi">1</span>
</code></pre></div></div>

<p>Shifting a signed number right with <code class="language-plaintext highlighter-rouge">lsr</code> can destroy the sign bit.
In these cases, use <strong>ASR (Arithmetic Shift Right)</strong> instead.</p>

<p>Despite this limitation, logical shift is extremely useful in:</p>
<ul>
  <li>Array indexing</li>
  <li>Address calculations</li>
  <li>Bitmasking</li>
</ul>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, we covered:</p>
<ul>
  <li>How to use <code class="language-plaintext highlighter-rouge">lsl</code> and <code class="language-plaintext highlighter-rouge">lsr</code> in ARM assembly</li>
  <li>What a shift operand is</li>
  <li>How to check the Carry flag using <code class="language-plaintext highlighter-rouge">movs</code></li>
  <li>Why logical shifts are suitable for unsigned integers</li>
</ul>

<p>In the next post, we’ll explore other shift types such as <code class="language-plaintext highlighter-rouge">asr</code> and <code class="language-plaintext highlighter-rouge">ror</code>.</p>]]></content><author><name>차서준 (Cha Seojun)</name><email>sjcha36@gmail.com</email></author><category term="arm" /><category term="assembly" /><category term="tutorial" /><category term="Assembly" /><category term="Embedded" /><category term="low-level programming" /><category term="ARM" /><category term="QEMU" /><category term="GDB" /><summary type="html"><![CDATA[Computers frequently perform bit-level operations to manipulate data, and shift operations are one of the most fundamental among them. In this post, we’ll focus on Logical Shifts in ARM assembly.]]></summary></entry></feed>